# 工作日报 - 2026-03-20

## 今日完成工作

### Phase 7.48.57 - 逻辑控制面板 4 项修复（前序会话）

1. **默认启动顺序**：张紧控制 → 1号制动器 → 1号电机 → 2号电机
2. **字体1.5倍**：所有字体放大，卡片/按钮尺寸同比缩放
3. **调整顺序按钮**：修复 ◀▶ 按钮被 nodeMouseArea 遮挡（z-order 问题）
4. **保存确认弹窗**：点击保存弹出"确认保存当前配置？"对话框

**额外修复**：时间轴空白问题（QML Loader 时序 bug + 旧名称迁移）
- 根本原因：`Component.onCompleted` 在 `Loader.onLoaded` 之前触发，systemConfig 尚为 null
- 解决：添加 `onSystemConfigChanged` 处理器
- 旧名称迁移：config.ini 存储的"张紧"/"抱闸"自动映射为"张紧控制"/"1号制动器"

---

### Phase 7.48.58 - R键启动修复（今日）

**问题1：R/S 键完全无响应**
- 根本原因：`main.cpp` 创建 MaintenanceControl/LocalControl 后未调用 setCommonControl/setSystemConfig
- 日志证据：`❌ MaintenanceControl: CommonControl未设置`
- 修复：补充 4 行连接代码

**问题2：起车预警无语音 → 设备序列永不启动**
- 根本原因：`playWarningOnce()` TTS 代码被注释 + 音频文件不存在 → `onPlaybackFinished` 永不触发
- 修复：重写 playWarningOnce()
  - 优先：预制音频文件
  - 次选：TTS 实时合成 "本机名称准备启动，注意安全"
  - 兜底：直接执行启动序列（保证不卡死）

**问题3：设备名称不匹配**
- 根本原因：getDeviceChannel() 映射表只有旧名称（"张紧"/"抱闸"），LogicControlPanel 用新名称
- 修复：添加 "张紧控制" → 通道0，"1号制动器" → 通道1

---

### Phase 7.48.59 - 修复TTS UI卡住"正在加载"（今日）

**问题**：语音管理界面 TTS引擎 一直显示"正在加载PaddleSpeech模型..."，即使服务已退出

**根本原因**：
- `PaddleSpeechAdapter::onProcessFinished()` 只设 `m_isInitialized=false`，从不发送失败消息给 QML
- QML `onTtsInitializationProgress` 只检测"初始化完成"/"初始化成功"关键字才隐藏进度条

**修复**：
- `PaddleSpeechAdapter.cpp`：`onProcessFinished()` 中新增 `emit initializationProgress("初始化失败...")`
- `TTSConfigSection.qml`：`onTtsInitializationProgress` 新增"初始化失败"/"已退出"/"已停止"关键字检测

---

### Phase 7.48.59 - 修复PaddleSpeech初始化失败-损坏NLTK数据（今日）

**问题**：PaddleSpeech 初始化报错 `BadZipFile: File is not a zip file`

**根本原因**：
- 调用链：`TTSExecutor` → `paddlespeech.t2s.frontend.phonectic` → `g2p_en.G2p` → `nltk.data.find('taggers/averaged_perceptron_tagger.zip')`
- 设备上 `/root/nltk_data/taggers/averaged_perceptron_tagger.zip` 因中断下载而损坏
- 损坏文件存在 → NLTK 优先使用它 → BadZipFile → 整个 `TTSExecutor` 导入失败

**修复**：
1. `paddle_tts_service.py`：新增 `_remove_corrupt_nltk_zips()` 函数，服务启动时扫描所有 NLTK 路径，检测损坏 zip 并自动删除
2. **设备端数据上传**：上传有效的 NLTK 数据到设备挂载路径
   - `averaged_perceptron_tagger.zip`（2.5MB，2 files）→ `/home/linaro/.../nltk_data/taggers/`
   - `cmudict.zip`（876KB，3 files）→ `/home/linaro/.../nltk_data/corpora/`
   - 容器内 `/app/tts_models/nltk_data` 优先级最高，有效数据先于损坏文件被找到

---

### Phase 7.48.59 - 修复R键启动无限循环重启（今日）

**问题**：按R键启动后，程序进入无限循环（日志打印了15次"开始执行启动顺序"）

**根因1：warningTimer 是重复定时器（主因）**
- `startWarningPlayback()` 调用 `m_warningTimer->start(N秒)` 未设 `setSingleShot`
- QTimer 默认 `singleShot=false` → 每N秒重复触发 `onWarningTimerTimeout()` → 无限调用 `startDeviceSequence()`
- 修复：`m_warningTimer->setSingleShot(true)`

**根因2：fallback③ 未停止 warningTimer**
- `playWarningOnce()` TTS失败时，兜底路径直接调用 `startDeviceSequence()`，但没有停止 `m_warningTimer`
- `m_isWarningPlaying = false` 导致 `stopWarningPlayback()` 也无法停止它
- 修复：在 ③ 中先调用 `m_warningTimer->stop()`

**根因3：多个反馈定时器并发触发停止序列**
- 4个设备各有独立反馈定时器（3秒），第一个设备超时触发停止序列后，其余3个定时器仍在运行
- `中断当前序列` 只停止序列推进定时器，未取消反馈定时器
- 导致 `[1/4] 停止2号电机` 连续打印3次
- 修复：`stopDeviceSequence()` 开始时取消所有 `m_feedbackChecks` 中的定时器

---

### Phase 7.48.60 - 皮带音频来源配置（今日）

**问题**：`CommonControl::getAudioPath()` 硬编码查找 `/app/AUDIO/1#PD/`，而批量合成 TTS 输出到 `/home/linaro/belt-control-data/audio/paddlespeech-xxx/1#PD/`，路径不一致。

**解决方案**：新增"音频来源"配置项（0=默认, 1=TTS合成）

**修改**：
- `SystemConfig.h/.cpp`：新增 `beltAudioSource` 属性，保存/加载到 config.ini
- `CommonControl.cpp`：`getAudioPath()` 支持 TTS 路径优先（使用 TTSConfigManager 获取当前模型信息）
- `BasicParametersSection.qml`：新增"音频来源"下拉框
- `BasicConfigPage.qml`：`basicParams` 新增字段，保存/加载时同步 C++ systemConfig
- `BatchAudioGenerator.cpp`："皮带启动"TTS文字改为自然语言"一号皮带准备启动，注意安全"

---

### Phase 7.48.61 - 修复QDS预览不显示基本参数设置（今日）

**问题**：QDS 上运行时基本参数设置区域空白，设备上正常显示。

**根本原因**：Loader 使用 QRC 绝对路径（`qrc:/qt/qml/...`），QDS 不挂载 QRC 虚拟文件系统 → 静默失败。

**修复**：改为相对路径（`../../parameter_settings/...`）。

---

### Phase 7.48.62 - 修复三个音频Bug（今日，voip.md测试日志发现）

**Bug 1：音频来源UI显示"默认"（应是TTS）**
- 根因：SQLite 无记录时 `loadBasicParams()` 直接返回 false，未从 C++ systemConfig fallback
- 修复：补充 `basicParams.beltAudioSource = systemConfig.beltAudioSource || 0`

**Bug 2：起车预警音频仅播放约1秒**
- 根因：计时器竞态——`start(10000)` 后同步 TTS 合成耗时9.7秒，计时器剩余0.3秒触发
- 修复：TTS 合成完成 `playAudio()` 后，重置计时器为完整 `warningTimeSeconds`

**Bug 3：音频文件存在但程序判断未找到**
- 根因：批量生成文件在 `/home/linaro/belt-control-data/audio/1#PD/`（卷挂载），`getAudioPath()` 只搜索 `/app/AUDIO/1#PD/`
- 修复：添加 `DataPathConfig::getAudioBaseDirectory()/{N}#PD/` 作为第二搜索路径

---



| 提交 | 内容 |
|------|------|
| 19a280c | fix: Phase 7.48.57 修复逻辑控制时间轴空白 |
| 3d5c28b | fix: Phase 7.48.57 逻辑控制面板4项修复 |
| 58f4737 | fix: Phase 7.48.58 R键启动无响应和起车预警语音 |
| 1018257 | fix: Phase 7.48.59 修复R键启动无限循环重启 |
| 3fd0e96 | fix: Phase 7.48.59 修复TTS UI永久显示正在加载 |
| 57be9a6 | fix: Phase 7.48.59 修复PaddleSpeech初始化失败-损坏NLTK zip自动清理 |
| (待提交) | feat: Phase 7.48.60 皮带音频来源配置+TTS路径+批量合成TTS文字 |

---

## 明日计划

1. **编译部署**：执行 `build-ubuntu24-apt.ps1 151` 把今日所有修复部署到设备
2. 验证 R 键启动：只触发一次启动序列，不自动重启
3. 验证 TTS 合成后起车预警正常播放（NLTK 数据已修复，PaddleSpeech 应能正常初始化）
4. 验证设备按逻辑控制面板的顺序依次启动
5. 验证 音频来源=TTS合成 后，起车预警播放 TTS 合成路径的音频

---

## 遗留问题

- **停车预警音频**：`stopBelt()` 的停车音频路径 `/app/AUDIO/` 不存在，已有兜底逻辑
- ~~**批量生成与预制文件路径**：`getAudioPath()` 查找路径与批量生成输出路径不一致~~ ✅ 已修复（Phase 7.48.60）
- **NetworkTask 未连接**：测试环境中 Modbus 服务器不可达，所有反馈恒为0（正常测试现象）

