# Fix 97.2：编解码器库打包到镜像方案实施

**日期**: 2026-01-09 20:30
**问题**: Fix 97 添加硬件编码器后，应用程序缺少 8 个编解码器库
**方案**: 将所有编解码器库打包到 Docker 镜像中，使容器自包含
**状态**: ✅ **已完成 - 代码修改完成，待构建验证**

---

## 🎯 核心决策

### 用户明确要求

> "不要依赖宿主机"
> "容器部署，就必须包含所有的东西"
> "全部加上"

**选择方案**: 方案 B（库打包到镜像）
**拒绝方案**: 方案 A（从宿主机挂载卷）

---

## 📋 缺失的编解码器库列表

| 库名 | 用途 | 优先级 |
|------|------|--------|
| `libx264.so.160` | H.264 软件编码器 | 🔴 必需 |
| `librga.so.2` | Rockchip RGA 图形加速 | 🔴 必需 |
| `libyuv.so.2` | YUV 格式转换 | 🔴 必需 |
| `libvpx.so.6` | VP8/VP9 编解码器 | 🟡 可选 |
| `libwebp.so.6` | WebP 图像编解码器 | 🟡 可选 |
| `libcodec2.so.0.9` | Codec2 语音编码器 | 🟡 可选 |
| `libdav1d.so.4` | AV1 解码器 | 🟡 可选 |
| `libaom.so.0` | AOM AV1 编解码器 | 🟡 可选 |

---

## 🔧 代码修改详情

### 修改 1: 库复制逻辑（Line 1083-1124）

**文件**: [build-ubuntu24-apt.ps1](../../build-ubuntu24-apt.ps1)

**修改前**（旧代码已注释）:
```powershell
# ❌ 2026-01-09 之前：只复制少数几个库，且从 FFmpeg 编译输出复制
# 问题：FFmpeg 编译输出只包含 FFmpeg 核心库，不包含编解码器依赖库
$codecLibs = Get-ChildItem -Path $FFmpegHwLibPath -Filter $pattern -File
foreach ($lib in $codecLibs) {
    if ($lib.Length -gt 1KB) {
        Copy-Item $lib.FullName "$DockerContextDir\lib\" -Force
    }
}
```

**修改后**（Line 1083-1124）:
```powershell
# ✅ 2026-01-09 20:10 [Fix 97 配套] 从 rk3588-libs 复制所有编解码器库到镜像
# 原因：应用程序编译时链接了所有 FFmpeg 编解码器库，运行时必须提供
# 方案：将所有库打包到 Docker 镜像，避免运行时依赖宿主机
# 路径：docker/rk3588/rk3588-libs/lib/ → docker/rk3588/lib/ → Docker 镜像 /app/lib/
Write-Host "    Copying codec libraries from rk3588-libs (all dependencies)..." -ForegroundColor Yellow

# ⚠️ 2026-01-09 20:10 关键修复：从 rk3588-libs 复制，不是从 FFmpeg 编译输出
# FFmpeg 编译输出只包含 FFmpeg 库本身，不包含编解码器依赖库
$rk3588CodecLibDir = "$ProjectRoot\docker\rk3588\rk3588-libs\lib"

# 添加所有必需的编解码器库模式（包括 Fix 97 需要的库）
$codecPatterns = @("libvpx.so.*", "libx264.so.*", "libx265.so.*", "libmp3lame.so.*",
                   "libogg.so.*", "libspeex.so.*", "libtheora*.so.*", "libvorbis*.so.*",
                   "libyuv.so.*", "libwebp.so.*", "libwebpmux.so.*", "libwebpdemux.so.*",
                   "libaribb24.so.*", "libaom.so.*", "libcodec2.so.*", "libdav1d.so.*",
                   "libgsm.so.*", "libopencore-amrnb.so.*", "libopencore-amrwb.so.*",
                   "libopenjp2.so.*", "libshine.so.*", "libsnappy.so.*", "libtwolame.so.*",
                   "libvo-amrwbenc.so.*", "libwavpack.so.*", "libxvidcore.so.*", "libzvbi.so.*",
                   "libva.so.*", "libva-*.so.*", "libvdpau.so.*", "libOpenCL.so.*", "libsoxr.so.*",
                   "librockchip_mpp.so.*", "librga.so.*")

$codecCount = 0
$skippedCount = 0

foreach ($pattern in $codecPatterns) {
    # 优先从 rk3588-libs 复制
    $codecLibs = Get-ChildItem -Path $rk3588CodecLibDir -Filter $pattern -File -ErrorAction SilentlyContinue

    if ($codecLibs) {
        foreach ($lib in $codecLibs) {
            # ⚠️ 2026-01-03 修复：只复制实际文件（> 1KB），跳过符号链接
            # 原因：Windows PowerShell 复制 WSL 符号链接会导致 "file too short" 错误
            if ($lib.Length -gt 1KB) {
                Copy-Item $lib.FullName "$DockerContextDir\lib\" -Force -ErrorAction SilentlyContinue
                Write-Host "      ✓ $($lib.Name) ($([math]::Round($lib.Length / 1KB, 0)) KB)" -ForegroundColor Gray
                $codecCount++
            } else {
                $skippedCount++
            }
        }
    }
}

if ($codecCount -gt 0) {
    Write-Host "    OK: $codecCount codec libraries copied" -ForegroundColor Green
} else {
    Write-Host "    WARNING: No codec libraries found, may cause runtime ABI errors" -ForegroundColor Yellow
}
```

**关键变化**:
1. ✅ **源目录切换**: 从 `$FFmpegHwLibPath` 改为 `$rk3588CodecLibDir`（`docker/rk3588/rk3588-libs/lib/`）
2. ✅ **库模式扩展**: 添加所有编解码器库模式（32 种）
3. ✅ **符号链接过滤**: 只复制实际文件（> 1KB），跳过符号链接
4. ✅ **完整日志**: 显示复制的库数量和大小

---

### 修改 2: 简化运行时配置（X11 分支，Line 1430-1440）

**文件**: [build-ubuntu24-apt.ps1](../../build-ubuntu24-apt.ps1)

**修改前**（旧代码已注释）:
```bash
# ❌ 2026-01-09 之前：挂载所有编解码器库（libx264, libyuv, libwebp, librga 等）
# 问题：配置复杂，容器依赖宿主机库，版本不匹配风险高
HW_LIBS_VOLUME="-v /usr/lib/aarch64-linux-gnu/librockchip_mpp.so.1:/opt/hw-libs/librockchip_mpp.so.1:ro"
HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/libx264.so.160:/opt/hw-libs/libx264.so.160:ro"
HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/libyuv.so.2:/opt/hw-libs/libyuv.so.2:ro"
HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/libwebp.so.6:/opt/hw-libs/libwebp.so.6:ro"
HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/librga.so.2:/opt/hw-libs/librga.so.2:ro"
# ... 更多库挂载
HW_LIBS_ENV="-e LD_LIBRARY_PATH=/opt/hw-libs:/app/lib:/usr/lib/aarch64-linux-gnu:/lib/aarch64-linux-gnu"
```

**修改后**（Line 1430-1440）:
```bash
# ✅ 2026-01-09 20:35 [Fix 97.2 配套] 最简库挂载配置
# 原因：所有编解码器库（包括 librga.so.2）已打包到 Docker 镜像 /app/lib/ 中
# 方案：只挂载 RKMPP 硬件编解码器驱动库（必须依赖内核驱动）
# 结果：容器完全自包含所有依赖，不依赖宿主机任何编解码器库
# 只挂载 RKMPP 硬件编解码器驱动库（依赖内核驱动）
HW_LIBS_VOLUME="-v /usr/lib/aarch64-linux-gnu/librockchip_mpp.so.1:/opt/hw-libs/librockchip_mpp.so.1:ro"
# ❌ 2026-01-09 20:35 移除 librga.so.2 挂载（已包含在镜像 /app/lib/ 中）
# HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/librga.so.2:/opt/hw-libs/librga.so.2:ro"
HW_LIBS_VOLUME="$HW_LIBS_VOLUME -v /usr/lib/aarch64-linux-gnu/libgomp.so.1.0.0:/opt/hw-libs/libgomp.so.1:ro"
# LD_LIBRARY_PATH: 优先使用镜像内的库 (/app/lib)
HW_LIBS_ENV="-e LD_LIBRARY_PATH=/app/lib:/opt/hw-libs:/usr/lib/aarch64-linux-gnu:/lib/aarch64-linux-gnu"
```

**关键变化**:
1. ✅ **移除所有编解码器库挂载**: libx264、libyuv、libwebp、**librga** 等全部移除
2. ✅ **只保留 RKMPP 驱动库**: librockchip_mpp.so.1、libgomp.so.1
3. ✅ **LD_LIBRARY_PATH 优先级调整**: `/app/lib` 优先于 `/opt/hw-libs`

**⚠️ 2026-01-09 20:35 重要修正**: 用户指出 librga.so.2 也找不到，需要包含到镜像中。已从设备 188 下载 librga.so.2.1.0 到 rk3588-libs/lib/ 并创建 librga.so.2 副本，移除了 librga.so.2 的卷挂载。

---

### 修改 3: 简化运行时配置（EGLFS 分支，Line 1454-1463）

**文件**: [build-ubuntu24-apt.ps1](../../build-ubuntu24-apt.ps1)

**修改内容**: 与 X11 分支完全相同（Line 1454-1463）

---

## 📊 架构变化对比

### 修改前（方案 A 思路）

```
容器内部:
  /app/belt_control_system
  /app/lib/
    ├── libavcodec.so.58  (FFmpeg 核心库)
    └── libavformat.so.58 (FFmpeg 核心库)

运行时卷挂载:
  /opt/hw-libs/ ← 宿主机 /usr/lib/aarch64-linux-gnu/
    ├── libx264.so.160      ⚠️ 依赖宿主机
    ├── libyuv.so.2         ⚠️ 依赖宿主机
    ├── libwebp.so.6        ⚠️ 依赖宿主机
    └── ... (更多编解码器库)

LD_LIBRARY_PATH=/opt/hw-libs:/app/lib  ⚠️ 优先使用宿主机库
```

**问题**:
- ❌ 容器依赖宿主机编解码器库
- ❌ 版本不匹配风险高
- ❌ 配置复杂（需要挂载 8+ 个库）
- ❌ 不符合用户"不依赖宿主机"要求

---

### 修改后（方案 B）

```
容器内部（镜像构建时复制）:
  /app/belt_control_system
  /app/lib/
    ├── libavcodec.so.58      ✅ FFmpeg 核心库
    ├── libavformat.so.58     ✅ FFmpeg 核心库
    ├── libx264.so.160        ✅ H.264 软件编码器
    ├── libyuv.so.2           ✅ YUV 格式转换
    ├── libwebp.so.6          ✅ WebP 编解码器
    ├── libcodec2.so.0.9      ✅ Codec2 语音编码器
    ├── libdav1d.so.4         ✅ AV1 解码器
    ├── libaom.so.0           ✅ AOM AV1 编解码器
    ├── libvpx.so.6           ✅ VP8/VP9 编解码器
    └── ... (所有编解码器库)

运行时卷挂载（仅硬件驱动库）:
  /opt/hw-libs/ ← 宿主机 /usr/lib/aarch64-linux-gnu/
    ├── librockchip_mpp.so.1  (依赖内核驱动)
    ├── librga.so.2           (依赖内核驱动)
    └── libgomp.so.1          (OpenMP 运行时)

LD_LIBRARY_PATH=/app/lib:/opt/hw-libs  ✅ 优先使用镜像内库
```

**优势**:
- ✅ 容器完全自包含（所有编解码器库在镜像中）
- ✅ 不依赖宿主机编解码器库版本
- ✅ 配置简洁（只挂载 3 个硬件驱动库）
- ✅ 符合用户"容器包含所有东西"要求
- ✅ 版本一致性保证

---

## 🚀 部署流程

### 步骤 1: 重新构建镜像

```powershell
.\build-ubuntu24-apt.ps1 188
```

**预期输出**:
```
Step 2: Preparing Docker build context...
  Copying codec libraries from rk3588-libs (all dependencies)...
    ✓ libx264.so.160 (1024 KB)
    ✓ libwebp.so.6 (512 KB)
    ✓ libcodec2.so.0.9 (256 KB)
    ✓ libdav1d.so.4 (2048 KB)
    ✓ libaom.so.0 (3072 KB)
    ✓ librga.so.2 (128 KB)
    ✓ libyuv.so.2 (256 KB)
    ✓ libvpx.so.6 (1024 KB)
  OK: 32 codec libraries copied
```

**构建时间**: 约 15-20 分钟（首次构建或镜像变化）

---

### 步骤 2: 验证库依赖

**在设备 188 上运行**:
```bash
ssh linaro@192.168.10.188
docker run --rm belt-control:v3.5-apt ldd /app/belt_control_system | grep "not found"
```

**预期输出**: 无任何 "not found"（所有库都已满足）

---

### 步骤 3: 启动应用

```bash
ssh linaro@192.168.10.188
./run-ubuntu24-apt.sh
```

**预期结果**:
- ✅ 应用正常启动（无 "library not found" 错误）
- ✅ 软件编码器（libx264）可用
- ✅ 硬件编码器（h264_rkmpp）可用

---

### 步骤 4: 验证硬件编码器（可选）

**设置环境变量启用硬件编码器**:
```bash
docker run -e USE_HARDWARE_ENCODER=1 ... belt-control:v3.5-apt
```

**监控 CPU 使用率**（视频通话过程中）:
```bash
docker exec belt-control-app ps aux | grep belt_control
```

**预期结果**:
- 软件编码器（USE_HARDWARE_ENCODER=0）: CPU 60-80%
- 硬件编码器（USE_HARDWARE_ENCODER=1）: CPU 10-20%

---

## 📦 库来源路径

### 源库位置（宿主机）

```
docker/rk3588/rk3588-libs/lib/
├── libx264.so.160         (从设备 188 下载)
├── libwebp.so.6           (从设备 188 下载)
├── libcodec2.so.0.9       (从设备 188 下载)
├── libdav1d.so.4          (从设备 188 下载)
├── libaom.so.0            (从设备 188 下载)
├── librga.so.2            ⚠️ (2026-01-09 20:35 新增，从设备 188 下载)
├── librga.so.2.1.0        ⚠️ (2026-01-09 20:35 新增，从设备 188 下载)
├── libyuv.so.2            (从设备 188 下载)
├── libvpx.so.6            (从设备 188 下载)
└── ... (更多编解码器库)
```

**⚠️ 重要**: 这些库是从设备 188 的 `/usr/lib/aarch64-linux-gnu/` 下载的，确保与宿主机 ABI 兼容。

**⚠️ 2026-01-09 20:35 新增**: librga.so.2.1.0（实际文件，180KB）和 librga.so.2（副本），用于 Rockchip RGA 图形加速。

---

### 镜像内库位置

```
Docker 镜像 /app/lib/
├── libavcodec.so.58       (FFmpeg 核心库)
├── libavformat.so.58      (FFmpeg 核心库)
├── libavutil.so.56        (FFmpeg 核心库)
├── libswscale.so.5        (FFmpeg 核心库)
├── libx264.so.160         ← 从 rk3588-libs 复制
├── libwebp.so.6           ← 从 rk3588-libs 复制
├── libcodec2.so.0.9       ← 从 rk3588-libs 复制
├── libdav1d.so.4          ← 从 rk3588-libs 复制
├── libaom.so.0            ← 从 rk3588-libs 复制
├── librga.so.2            ⚠️ 从 rk3588-libs 复制（2026-01-09 20:35 新增）
├── librga.so.2.1.0        ⚠️ 从 rk3588-libs 复制（2026-01-09 20:35 新增）
├── libyuv.so.2            ← 从 rk3588-libs 复制
├── libvpx.so.6            ← 从 rk3588-libs 复制
└── ... (更多编解码器库)
```

**LD_LIBRARY_PATH**: `/app/lib:/opt/hw-libs:/usr/lib/aarch64-linux-gnu:/lib/aarch64-linux-gnu`

**⚠️ 2026-01-09 20:35 新增**: librga.so.2 和 librga.so.2.1.0 已包含在镜像中，不再需要从宿主机挂载。

---

## ⚠️ 注意事项

### 1. 库版本一致性

**关键**: 镜像中的库必须与应用程序编译时链接的库版本一致。

**检查方法**:
```bash
ldd /app/belt_control_system | grep libx264
# 输出: libx264.so.160 => /app/lib/libx264.so.160
```

如果版本不匹配（例如需要 libx264.so.160 但只有 libx264.so.165）：
- 在宿主机创建符号链接：`sudo ln -sf libx264.so.165 libx264.so.160`
- 重新从设备 188 下载到 rk3588-libs
- 重新构建镜像

---

### 2. 硬件驱动库仍需宿主机

**保留卷挂载的库**:
- `librockchip_mpp.so.1`: Rockchip MPP 硬件编解码器驱动
- `libgomp.so.1`: OpenMP 多线程运行时

**已移除卷挂载**（2026-01-09 20:35）:
- ~~`librga.so.2`: Rockchip RGA 图形加速驱动~~（已包含在镜像 /app/lib/ 中）

**原因**: librockchip_mpp.so.1 需要直接访问内核驱动（`/dev/mpp*`），必须与宿主机内核版本匹配。librga.so.2 已打包到镜像中，不再依赖宿主机。

---

### 3. 镜像大小增加

**预期镜像大小增加**: 约 50-100MB（32 个编解码器库）

**优势**: 换取部署简便性和版本一致性。

---

## 📖 相关文档

### Fix 97 系列
- [43-Fix97-添加硬件编码器开关.md](43-Fix97-添加硬件编码器开关.md) - 环境变量控制
- [44-Fix97.1-恢复RKMPP编码器硬件上下文.md](44-Fix97.1-恢复RKMPP编码器硬件上下文.md) - DRM Render Node 方案
- [49-Fix97.1重新诊断-编码器可能已经在使用硬件.md](49-Fix97.1重新诊断-编码器可能已经在使用硬件.md) - 重新分析
- [50-Fix97副作用-编解码器库依赖问题.md](50-Fix97副作用-编解码器库依赖问题.md) - 问题分析
- **51-Fix97.2-编解码器库打包到镜像方案实施.md** - 本文档（方案实施）

### 参考脚本
- [scripts/2026-01-09/14-fix-libx264-version.ps1](../../scripts/2026-01-09/14-fix-libx264-version.ps1) - libx264 版本修复
- [scripts/2026-01-09/15-auto-add-codec-libs.ps1](../../scripts/2026-01-09/15-auto-add-codec-libs.ps1) - 自动添加库（方案 A，已拒绝）

---

## ✅ 实施总结

### 核心变化

1. **库复制源切换**: `FFmpegHwLibPath` → `rk3588CodecLibDir`
2. **库模式扩展**: 8 个必需库 → 32 个编解码器库模式
3. **运行时配置简化**: 移除所有编解码器库挂载，只保留 3 个硬件驱动库
4. **LD_LIBRARY_PATH 优先级**: `/app/lib` 优先于 `/opt/hw-libs`

---

### 预期效果

- ✅ 容器完全自包含（不依赖宿主机编解码器库）
- ✅ 配置简洁（只挂载 3 个硬件驱动库）
- ✅ 版本一致性保证（所有库在镜像构建时确定）
- ✅ 软件编码器和硬件编码器都可正常使用

---

### 下一步

1. **重新构建镜像**: `.\build-ubuntu24-apt.ps1 188`（15-20 分钟）
2. **验证库依赖**: `ldd /app/belt_control_system | grep "not found"`（应无输出）
3. **测试应用启动**: `./run-ubuntu24-apt.sh`（应正常启动）
4. **可选：验证硬件编码器**: 设置 `USE_HARDWARE_ENCODER=1` 并监控 CPU 使用率

---

**创建时间**: 2026-01-09 20:30
**核心方案**: 将所有编解码器库打包到 Docker 镜像
**用户决策**: "不要依赖宿主机" + "容器部署，就必须包含所有的东西"
**状态**: ✅ **代码修改完成 - 等待用户构建验证**
