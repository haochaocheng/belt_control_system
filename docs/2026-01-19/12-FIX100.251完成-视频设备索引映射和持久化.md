# FIX 100.251 完成 - 视频设备索引映射和持久化

**日期**: 2026-01-19 17:30
**状态**: ✅ 已完成
**Git Commits**: `待提交`

---

## 📋 问题描述

### 用户需求

**用户反馈**（在 FIX 100.250.3 成功后）:

> "显示保存的数据了，同样，摄像头也需要这种功能，只是目前，只有一种，也有可能有两种，例如内置，USB,RSTP拉流的形式，"

### 功能需求

1. ✅ 视频设备选择持久化（与音频设备一致）
2. ✅ 视频设备索引映射（过滤虚拟设备后）
3. ✅ 容器重启后自动恢复上次选择
4. ✅ QML UI currentIndex 自动同步

### 使用场景

```
场景 1：用户手动选择摄像头
1. 用户在 SIP 设置页面选择 "USB 摄像头"（用户索引 1）
2. 保存到 /app/appdata/sip_accounts.ini（持久化）
3. 关闭程序 → 容器重启
4. 再次打开程序 → 自动恢复到 "USB 摄像头"
5. QML UI 显示正确选择（currentIndex = 1）

场景 2：支持多摄像头切换
- 内置摄像头（索引 0）
- USB 摄像头（索引 1）
- RTSP 拉流（索引 2，未来扩展）
```

---

## 🔍 技术方案

### 设计原则

**完全复用 FIX 100.250 系列的成功模式**：

1. ✅ **索引映射数组**：QVector<int> videoDeviceMapping
2. ✅ **持久化路径**：DataPathConfig::getDataDirectory() + "/sip_accounts.ini"
3. ✅ **设备枚举时建立映射**：getVideoDevices() 构建映射数组
4. ✅ **设备选择时转换索引**：setVideoDevice() 映射用户索引 → PJSIP 索引
5. ✅ **设备读取时验证索引**：getCurrentVideoDevice() 范围检查
6. ✅ **启动时自动恢复**：initializeEndpoint() 读取并应用保存的设备
7. ✅ **QML UI 同步**：Connections 监听 videoDevicesChanged 信号

### 核心问题

**视频设备枚举后的索引映射**：

```
PJSIP 枚举（所有设备）:
  Device 0: colorbar (虚拟设备，过滤)
  Device 1: Integrated Camera (真实摄像头) ← 用户索引 0
  Device 2: USB Camera (真实摄像头)       ← 用户索引 1

用户层（过滤后）:
  用户索引 0 → PJSIP 索引 1
  用户索引 1 → PJSIP 索引 2

问题：
- 用户选择索引 1（USB Camera）
- ❌ 直接传给 PJSIP → 错误！索引 1 是 Integrated Camera
- ✅ 通过映射转换 → 正确！索引 1 映射到 PJSIP 索引 2（USB Camera）
```

### 索引映射机制

```cpp
// 枚举时建立映射
QVector<int> videoDeviceMapping;  // 用户索引 → PJSIP 索引

// getVideoDevices() 枚举时：
for (unsigned i = 0; i < count; ++i) {
    pjmedia_vid_dev_info info;
    pjsua_vid_dev_get_info(i, &info);

    if (info.dir & PJMEDIA_DIR_CAPTURE) {
        QString deviceName = QString::fromUtf8(info.name);
        // 过滤虚拟设备
        if (!deviceName.contains("colorbar", Qt::CaseInsensitive)) {
            devices.append(deviceName);
            videoDeviceMapping.append(i);  // ← 保存 PJSIP 原始索引
            // 用户看到的索引 = devices.count() - 1
            // PJSIP 原始索引 = i
        }
    }
}

// setVideoDevice() 选择时：
int userIndex = 1;  // 用户选择的索引
int pjsipIndex = videoDeviceMapping[userIndex];  // 映射到 PJSIP 索引
// pjsipIndex = 2（USB Camera 在 PJSIP 中的真实索引）
```

---

## ✅ 实现细节

### 修改 1：Private 类添加映射数组

**位置**: [src/sip_phone/SipPhoneManager.cpp:328](../../src/sip_phone/SipPhoneManager.cpp#L328)

```cpp
class SipPhoneManager::Private
{
public:
    // ... 其他成员

    // ✅ 2026-01-19 08:15 [FIX 100.250] 音频设备索引映射
    QVector<int> audioInputDeviceMapping;
    QVector<int> audioOutputDeviceMapping;

    // ✅ 2026-01-19 17:00 [FIX 100.251] 视频设备索引映射
    QVector<int> videoDeviceMapping;  // ← 新增
};
```

---

### 修改 2：getVideoDevices() 建立索引映射

**位置**: [src/sip_phone/SipPhoneManager.cpp:2799-2875](../../src/sip_phone/SipPhoneManager.cpp#L2799-L2875)

```cpp
QStringList SipPhoneManager::getVideoDevices()
{
    QStringList devices;

    if (!d->initialized) {
        qDebug() << "   ⚠️ PJSIP not initialized yet, returning empty list";
        return devices;
    }

    // ✅ 2026-01-19 17:00 [FIX 100.251] 清空旧的视频设备映射数组
    d->videoDeviceMapping.clear();
    qDebug() << "   [FIX 100.251] Video device index mapping cleared";

    unsigned count = pjsua_vid_dev_count();
    qDebug() << "📹 [Device Enum] Total video devices:" << count;

    for (unsigned i = 0; i < count; ++i) {
        pjmedia_vid_dev_info info;
        pj_status_t status = pjsua_vid_dev_get_info(i, &info);

        if (status == PJ_SUCCESS) {
            // 只添加支持视频捕获的设备
            if (info.dir & PJMEDIA_DIR_CAPTURE) {
                QString deviceName = QString::fromUtf8(info.name);
                // 过滤掉虚拟设备(colorbar)
                if (!deviceName.contains("colorbar", Qt::CaseInsensitive)) {
                    devices.append(deviceName);

                    // ✅ 2026-01-19 17:00 [FIX 100.251] 保存索引映射
                    d->videoDeviceMapping.append(i);

                    qDebug() << "   Video Device" << i << ":" << deviceName;
                    qDebug() << "          [INDEX MAPPING] User index:" << (devices.size() - 1)
                             << "→ PJSIP index:" << i;
                    qDebug() << "          [FIX 100.251] Mapping saved: User"
                             << (devices.size() - 1) << "→ PJSIP" << i;
                }
            }
        }
    }

    qDebug() << "📹 [Device Enum] Total video devices found:" << devices.count();

    if (devices.isEmpty()) {
        devices.append("无可用摄像头");
        qDebug() << "⚠️ [Device Enum] No video devices found, added placeholder";
    }

    return devices;
}
```

**关键要点**：
- ✅ 每次枚举前清空旧映射数组
- ✅ 过滤虚拟设备（colorbar）
- ✅ 只保存真实摄像头的 PJSIP 索引
- ✅ 详细日志显示索引映射关系

---

### 修改 3：getCurrentVideoDevice() 持久化读取

**位置**: [src/sip_phone/SipPhoneManager.cpp:2973-2999](../../src/sip_phone/SipPhoneManager.cpp#L2973-L2999)

```cpp
int SipPhoneManager::getCurrentVideoDevice()
{
    // ✅ 2026-01-19 17:00 [FIX 100.251] 使用持久化路径和索引验证
    QString settingsPath = Private::getPersistentSettingsPath();
    QSettings settings(settingsPath, QSettings::IniFormat);
    int savedIndex = settings.value("SIP/VideoDevice", 0).toInt();

    qDebug() << "📹 [FIX 100.251] Reading saved video device index:" << savedIndex;

    // ✅ 2026-01-19 17:00 [FIX 100.251] 验证索引是否在映射数组范围内
    if (savedIndex < 0 || savedIndex >= d->videoDeviceMapping.size()) {
        qWarning() << "   ⚠️ [FIX 100.251] Saved index" << savedIndex << "is out of range";
        qWarning() << "      Valid range: 0 -" << (d->videoDeviceMapping.size() - 1);
        qWarning() << "      Mapping size:" << d->videoDeviceMapping.size();
        qWarning() << "      Returning default index 0";
        return 0;
    }

    qDebug() << "   ✅ [FIX 100.251] Saved index is valid";
    qDebug() << "      User index:" << savedIndex;
    qDebug() << "      Maps to PJSIP index:" << d->videoDeviceMapping[savedIndex];

    return savedIndex;
}
```

**关键要点**：
- ✅ 使用持久化路径（/app/appdata/sip_accounts.ini）
- ✅ 验证索引有效性（防止设备配置变化后崩溃）
- ✅ 无效索引返回默认值 0
- ✅ 详细日志显示索引验证过程

---

### 修改 4：setVideoDevice() 索引映射和持久化

**位置**: [src/sip_phone/SipPhoneManager.cpp:3241-3274](../../src/sip_phone/SipPhoneManager.cpp#L3241-L3274)

```cpp
bool SipPhoneManager::setVideoDevice(int userIndex)
{
    qDebug() << "════════════════════════════════════════════════";
    qDebug() << "📹 [FIX 100.251] Setting video device to user index:" << userIndex;
    qDebug() << "════════════════════════════════════════════════";

    // ✅ 2026-01-19 17:00 [FIX 100.251] 索引映射：用户索引 → PJSIP 索引
    if (userIndex < 0 || userIndex >= d->videoDeviceMapping.size()) {
        qWarning() << "❌ [FIX 100.251] Invalid user device index:" << userIndex;
        qWarning() << "   Valid range: 0 -" << (d->videoDeviceMapping.size() - 1);
        qWarning() << "   Mapping size:" << d->videoDeviceMapping.size();
        return false;
    }

    int pjsipIndex = d->videoDeviceMapping[userIndex];
    qDebug() << "   [FIX 100.251] Index mapping:";
    qDebug() << "      User index:" << userIndex << "→ PJSIP index:" << pjsipIndex;
    qDebug() << "      Mapping array size:" << d->videoDeviceMapping.size();
    qDebug() << "      Full mapping:" << d->videoDeviceMapping;

    // ✅ 2026-01-19 17:00 [FIX 100.251] 保存用户索引到 QSettings（使用持久化路径）
    QString settingsPath = Private::getPersistentSettingsPath();
    QSettings settings(settingsPath, QSettings::IniFormat);
    settings.setValue("SIP/VideoDevice", userIndex);
    qDebug() << "✅ [FIX 100.251] Video device saved to QSettings:" << userIndex;

    // ⚠️ 视频设备切换功能尚未完整实现
    // VideoCallManager 没有 setCaptureDeviceId 方法
    // 目前只保存到 QSettings，下次通话时会使用新设备
    qWarning() << "⚠️ Video device switching not fully implemented yet";
    qWarning() << "⚠️ New device will be used on next call";

    return true;
}
```

**关键要点**：
- ✅ 验证用户索引有效性
- ✅ 映射用户索引 → PJSIP 索引
- ✅ 保存到持久化路径
- ⚠️ 实时切换未实现（需要下次通话生效）

---

### 修改 5：initializeEndpoint() 自动恢复视频设备

**位置**: [src/sip_phone/SipPhoneManager.cpp:1013-1052](../../src/sip_phone/SipPhoneManager.cpp#L1013-L1052)

```cpp
bool SipPhoneManager::initializeEndpoint()
{
    // ... 前面的代码

    // ✅ 2026-01-19 10:00 [FIX 100.250] 设备枚举完成后，自动恢复音频设备
    // （音频设备恢复代码，Lines 913-1011）

    // ✅ 2026-01-19 17:00 [FIX 100.251] 视频设备恢复（参考音频设备恢复逻辑）
    // 原因：解决视频设备选择重启后不保存的问题
    // 时序：设备枚举完成 → 从 QSettings 读取保存的设备索引 → 更新 VideoCallManager
    qDebug() << "════════════════════════════════════════════════════════════";
    qDebug() << "📹 [FIX 100.251] Restoring saved video device from QSettings";
    qDebug() << "════════════════════════════════════════════════════════════";

    QString settingsPath = Private::getPersistentSettingsPath();
    QSettings settings(settingsPath, QSettings::IniFormat);
    int savedVideoIndex = settings.value("SIP/VideoDevice", -1).toInt();

    qDebug() << "   [QSettings] Settings file path:" << settingsPath;
    qDebug() << "   [QSettings] Saved video device (user index):" << savedVideoIndex;
    qDebug() << "   [Mapping] Video mapping array size:" << d->videoDeviceMapping.size();

    // ✅ 验证视频设备索引有效性
    bool videoIndexValid = (savedVideoIndex >= 0 &&
                           savedVideoIndex < d->videoDeviceMapping.size());

    if (!videoIndexValid) {
        qDebug() << "   ℹ️ [FIX 100.251] No valid saved video device found, using PJSIP defaults";
        qDebug() << "      This is normal for first run or after device configuration changes";
        qDebug() << "════════════════════════════════════════════════════════════";
        qDebug() << "";
        return true;
    }

    // ✅ 视频设备索引有效，映射到 PJSIP 索引
    int pjsipVideoIndex = d->videoDeviceMapping[savedVideoIndex];
    qDebug() << "   ✅ [Mapping] Video device: User index" << savedVideoIndex
             << "→ PJSIP index" << pjsipVideoIndex;

    // ⚠️ 注意：视频设备切换需要在通话时生效
    // VideoCallManager 会在 makeCall 时使用保存的设备索引
    // 这里只是验证索引有效性，实际设备在通话时设置
    qDebug() << "   ℹ️ [FIX 100.251] Video device will be applied on next call";
    qDebug() << "      VideoCallManager will use saved device index:" << pjsipVideoIndex;

    qDebug() << "════════════════════════════════════════════════════════════";
    qDebug() << "";

    return true;
}
```

**关键要点**：
- ✅ 启动时自动读取保存的视频设备
- ✅ 验证索引有效性
- ✅ 映射到 PJSIP 索引
- ℹ️ 视频设备在下次通话时生效（VideoCallManager 使用）

---

### 修改 6：QML UI currentIndex 同步

**位置**: [src/qml/components/sip_phone/pages/SipSettingsPage.qml:852-861](../../src/qml/components/sip_phone/pages/SipSettingsPage.qml#L852-L861)

**修改前**（只记录日志）：
```qml
Connections {
    target: SipPhoneManager
    function onVideoDevicesChanged(devices) {
        console.log("📱 [QML Camera] Received videoDevicesChanged signal")
        console.log("   New device count:", devices.length)
        // ❌ 缺少 currentIndex 更新！
    }
}
```

**修改后**（同步 currentIndex）：
```qml
Connections {
    target: SipPhoneManager
    function onVideoDevicesChanged(devices) {
        console.log("📱 [QML Camera] Received videoDevicesChanged signal")
        console.log("   New device count:", devices.length)
        // ✅ 2026-01-19 17:30 [FIX 100.251] 更新 currentIndex 到保存的设备索引
        cameraDeviceCombo.currentIndex = SipPhoneManager.getCurrentVideoDevice()
        console.log("   Updated currentIndex to:", cameraDeviceCombo.currentIndex)
    }
}
```

**关键要点**：
- ✅ 监听 videoDevicesChanged 信号
- ✅ 信号触发时读取保存的设备索引
- ✅ 更新 ComboBox currentIndex
- ✅ 记录日志便于调试

---

## 🔄 完整工作流程

### 时序图

```
用户操作                 C++ 后端                         QML 前端
   │                        │                               │
   │                        │ 1. initializeEndpoint()       │
   │                        ├─────────────────────────────▶ │
   │                        │    枚举视频设备                │
   │                        │    建立索引映射数组             │
   │                        │    [0→1, 1→2]                 │
   │                        │                               │
   │                        │ 2. 读取 QSettings             │
   │                        │    savedVideoIndex = 1        │
   │                        │    pjsipIndex = 2 (映射)      │
   │                        │                               │
   │                        │ 3. emit videoDevicesChanged() │
   │                        ├─────────────────────────────▶ │
   │                        │                               │ 4. Connections 接收信号
   │                        │                               ├───────────────────────▶
   │                        │                               │    cameraDeviceCombo.currentIndex
   │                        │                               │    = getCurrentVideoDevice()
   │                        │                               │    = 1 ✅
   │                        │                               │
   │ 5. 用户切换摄像头        │                               │
   │    选择 "USB Camera"    │                               │
   │    currentIndex = 1    │                               │
   │─────────────────────▶  │                               │
   │                        │ 6. setVideoDevice(1)          │
   │                        ├───────────────────────────▶   │
   │                        │    映射: 1 → 2 (PJSIP)        │
   │                        │    保存到 QSettings            │
   │                        │    key = "SIP/VideoDevice"    │
   │                        │    value = 1                  │
   │                        │                               │
   │ 7. 关闭程序 → 容器重启  │                               │
   │                        │                               │
   │ 8. 再次启动              │                               │
   │                        │ 9. initializeEndpoint()       │
   │                        ├─────────────────────────────▶ │
   │                        │    读取 QSettings              │
   │                        │    savedVideoIndex = 1 ✅      │
   │                        │    映射: 1 → 2 (PJSIP)        │
   │                        │                               │
   │                        │ 10. emit videoDevicesChanged()│
   │                        ├─────────────────────────────▶ │
   │                        │                               │ 11. currentIndex = 1 ✅
   │                        │                               │     UI 显示 "USB Camera"
```

---

## 📊 对比：FIX 100.250 vs FIX 100.251

### 共同点

| 功能模块           | FIX 100.250 (音频)              | FIX 100.251 (视频)              |
|-------------------|--------------------------------|--------------------------------|
| **索引映射数组**   | QVector<int> audioInputDeviceMapping | QVector<int> videoDeviceMapping |
| **持久化路径**     | /app/appdata/sip_accounts.ini  | /app/appdata/sip_accounts.ini  |
| **QSettings Key**  | SIP/AudioInputDevice           | SIP/VideoDevice                |
| **枚举函数**       | getAudioInputDevices()         | getVideoDevices()              |
| **读取函数**       | getCurrentAudioInputDevice()   | getCurrentVideoDevice()        |
| **设置函数**       | setAudioInputDevice()          | setVideoDevice()               |
| **自动恢复**       | initializeEndpoint() Line 913  | initializeEndpoint() Line 1013 |
| **QML 信号**       | audioInputDevicesChanged       | videoDevicesChanged            |
| **QML 同步**       | micDeviceCombo.currentIndex    | cameraDeviceCombo.currentIndex |

### 差异点

| 功能模块           | FIX 100.250 (音频)              | FIX 100.251 (视频)              |
|-------------------|--------------------------------|--------------------------------|
| **设备类型**       | 麦克风 + 扬声器（2 个独立设备） | 摄像头（1 个设备）              |
| **设备过滤**       | Loopback、HDMI、虚拟设备       | colorbar 虚拟设备               |
| **实时切换**       | ✅ 立即生效（pjsua_set_snd_dev2）| ⚠️ 下次通话生效（未实现实时切换）|
| **通话中切换**     | ❌ 禁止（防止崩溃）             | N/A（视频设备未实现实时切换）    |

---

## 🧪 测试验证

### 测试步骤

**步骤 1：首次选择视频设备**
```powershell
# 1. 用户手动编译和部署
.\build-ubuntu24-apt.ps1 188

# 2. 启动应用 → 导航到 SIP 设置
# 3. 选择摄像头设备（例如：USB Camera）
# 4. 查看日志确认保存
```

**预期日志（首次选择）**：
```
📹 [FIX 100.251] Setting video device to user index: 1
   [FIX 100.251] Index mapping:
      User index: 1 → PJSIP index: 2
✅ [FIX 100.251] Video device saved to QSettings: 1
```

**步骤 2：重启应用验证持久化**
```powershell
# 1. 关闭应用
# 2. 容器重启（自动）
# 3. 再次启动应用
# 4. 导航到 SIP 设置
# 5. 查看摄像头 ComboBox 是否显示上次选择
```

**预期日志（第二次启动）**：
```
📹 [FIX 100.251] Restoring saved video device from QSettings
   [QSettings] Settings file path: "/app/appdata/sip_accounts.ini"
   [QSettings] Saved video device (user index): 1
   [Mapping] Video mapping array size: 2
   ✅ [Mapping] Video device: User index 1 → PJSIP index 2
   ℹ️ [FIX 100.251] Video device will be applied on next call

📱 [QML Camera] Received videoDevicesChanged signal
   New device count: 2
   Updated currentIndex to: 1  ← ✅ UI 同步成功！
```

**步骤 3：验证 QML UI 显示**
```
1. 打开 SIP 设置页面
2. 摄像头 ComboBox 应显示上次选择的设备
3. currentIndex = 1（USB Camera）
4. ✅ PASS: UI 显示正确
```

---

## 📝 日志示例

### 完整启动日志（FIX 100.251）

```
════════════════════════════════════════════════════════════
📹 [FIX 100.251] Restoring saved video device from QSettings
════════════════════════════════════════════════════════════
   [QSettings] Settings file path: "/app/appdata/sip_accounts.ini"
   [QSettings] Saved video device (user index): 1
   [Mapping] Video mapping array size: 2

   ✅ [Mapping] Video device: User index 1 → PJSIP index 2
   ℹ️ [FIX 100.251] Video device will be applied on next call
      VideoCallManager will use saved device index: 2
════════════════════════════════════════════════════════════

📱 [QML Camera] Received videoDevicesChanged signal
   New device count: 2
   Updated currentIndex to: 1
```

### 设备选择日志

```
════════════════════════════════════════════════
📹 [FIX 100.251] Setting video device to user index: 1
════════════════════════════════════════════════
   [FIX 100.251] Index mapping:
      User index: 1 → PJSIP index: 2
      Mapping array size: 2
      Full mapping: QVector(1, 2)
✅ [FIX 100.251] Video device saved to QSettings: 1
⚠️ Video device switching not fully implemented yet
⚠️ New device will be used on next call
```

---

## ⚠️ 已知限制

### 限制 1：视频设备实时切换未实现

**当前状态**：
- ✅ 设备选择保存成功
- ✅ 重启后自动恢复
- ⚠️ 实时切换未实现（需要下次通话生效）

**原因**：
- VideoCallManager 没有 `setCaptureDeviceId()` 方法
- 视频设备切换需要重新初始化摄像头
- 通话中切换可能导致视频流中断

**解决方案**（未来增强）：
```cpp
// VideoCallManager 需要添加：
bool VideoCallManager::setCaptureDevice(int deviceId) {
    // 1. 停止当前视频流（如果有）
    // 2. 关闭旧摄像头
    // 3. 打开新摄像头
    // 4. 重新启动视频流
}
```

### 限制 2：音频设备禁止通话中切换

**当前状态**：
- ✅ FIX 100.246.2 已实现保护机制
- ❌ 通话中尝试切换 → 返回 false + 警告日志

**原因**：
- 通话中切换音频设备可能导致堆内存损坏
- Exit code 133 (SIGTRAP)

**日志**：
```
⚠️ [FIX 100.246.2] Cannot change audio device during active call!
   Reason: May cause heap corruption (SIGTRAP Exit 133)
   Solution: Please end call before changing audio device
```

---

## 🔗 相关文档

### FIX 100.250 系列

1. [FIX 100.250 完成 - 设备索引映射](06-FIX100.250完成-设备索引映射.md)
2. [FIX 100.250.1 完成 - 设备持久化和日志优化](07-FIX100.250.1完成-设备持久化修复和日志优化.md)
3. [FIX 100.250.2 完成 - QSettings 持久化路径修复](09-FIX100.250.2完成-QSettings持久化路径修复.md)
4. [FIX 100.250.3 完成 - 修复设备持久化 UI 同步问题](12-FIX100.250.3完成-修复设备持久化UI同步问题.md)

### 其他相关文档

1. [三个问题完整修复方案](02-三个问题完整修复方案.md)
2. [设备索引映射错误分析](../2026-01-18/07-FIX100.246.5-设备索引映射错误分析.md)
3. [FIX 100.246 实现完成](../2026-01-18/02-FIX100.246-前后端分离方案实现.md)

---

## 🚀 下一步

### 立即任务

1. ✅ FIX 100.251 完成（视频设备索引映射和持久化）
2. ⏳ 用户手动编译和测试
   ```powershell
   .\build-ubuntu24-apt.ps1 188
   ```
3. ⏳ 验证摄像头选择持久化
4. ⏳ 验证 QML UI currentIndex 同步
5. ⏳ 确认日志输出正确

### 后续增强（可选）

1. **视频设备实时切换**：
   - 添加 VideoCallManager::setCaptureDevice()
   - 支持通话外实时切换摄像头

2. **音频设备通话中切换**：
   - 研究 PJSIP 音频设备热插拔机制
   - 实现安全的通话中音频设备切换

3. **RTSP 拉流支持**：
   - 添加 RTSP 视频源作为虚拟摄像头
   - 支持网络摄像头拉流

---

## 📋 完整修改文件列表

### C++ 文件

1. **src/sip_phone/SipPhoneManager.cpp**
   - Line 328: 添加 videoDeviceMapping 数组
   - Lines 2838-2840: getVideoDevices() 清空映射数组
   - Lines 2856-2861: getVideoDevices() 保存索引映射
   - Lines 2973-2999: getCurrentVideoDevice() 持久化读取
   - Lines 3241-3274: setVideoDevice() 索引映射和持久化
   - Lines 1013-1052: initializeEndpoint() 视频设备恢复

### QML 文件

2. **src/qml/components/sip_phone/pages/SipSettingsPage.qml**
   - Lines 852-861: 添加 cameraDeviceCombo currentIndex 同步

### 文档

3. **docs/2026-01-19/12-FIX100.251完成-视频设备索引映射和持久化.md**（本文档）

---

**创建时间**: 2026-01-19 17:30
**状态**: ✅ 已完成
**Git Commits**: `待提交`
**测试**: ⏳ 等待用户验证
