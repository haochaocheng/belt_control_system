# FIX 100.250.3 完成 - 修复设备持久化 UI 同步问题

**日期**: 2026-01-19 16:45
**状态**: ✅ 已完成
**Git Commits**: `待提交`

---

## 📋 问题总结

### 用户反馈

"新的编译完成测试，100%是最新的。第二次启动软件扬声器和麦克风依然是旧设备"

### 表现

1. ✅ 第一次选择设备 → 保存成功
2. ❌ 关闭程序重启 → UI 显示旧设备（但实际 PJSIP 设备是正确的）

---

## 🔍 调试过程

### Step 1: 添加调试日志

**修改**: [SipPhoneManager.cpp:960-965](../../src/sip_phone/SipPhoneManager.cpp#L960-L965)

```cpp
📌 [CURRENT DEVICES] PJSIP current devices before restore:
   Current capture_dev (input): 22
   Current playback_dev (output): 22
```

### Step 2: 分析日志输出

从 [voip.md:3384-3402](../../docs/log/voip.md#L3384-L3402)：

```
[DEBUG]    [QSettings] Settings file path: "/app/appdata/sip_accounts.ini"
[DEBUG]    [QSettings] Saved input device (user index): 1
[DEBUG]    [QSettings] Saved output device (user index): 3
[DEBUG]    [Mapping] Input mapping array size: 2
[DEBUG]    [Mapping] Output mapping array size: 4
[DEBUG]    📌 [CURRENT DEVICES] PJSIP current devices before restore:
[DEBUG]       Current capture_dev (input): 22
[DEBUG]       Current playback_dev (output): 22
[DEBUG]    ✅ [Mapping] Input device: User index 1 → PJSIP index 22
[DEBUG]    ✅ [Mapping] Output device: User index 3 → PJSIP index 22
[DEBUG]    [PJSIP] Calling pjsua_set_snd_dev2() to restore devices...
[DEBUG]       capture_dev (input): 22
[DEBUG]       playback_dev (output): 22
08:24:46.674            pjsua_aud.c  Set sound device: capture=22, playback=22
08:24:46.674            pjsua_aud.c  .No changes in capture and playback devices
[DEBUG]    ✅ [SUCCESS] Audio devices restored from QSettings!
```

### Step 3: 根本原因分析

**关键发现**：
1. ✅ QSettings 正确读取：输入索引 1，输出索引 3
2. ✅ 持久化路径正确：`/app/appdata/sip_accounts.ini`
3. ✅ 索引映射正确：User 1 → PJSIP 22，User 3 → PJSIP 22
4. ✅ **PJSIP 当前设备已经是 22**（在恢复前）
5. ⚠️ PJSIP 返回 "No changes"（正常，因为设备已经是 22）
6. ❌ **但 UI 显示的是旧设备**

**根本原因**：

**PJSIP 设备是正确的，但 QML ComboBox 的 currentIndex 没有同步到保存的用户索引！**

### Step 4: 时序分析

```
1. QML SipSettingsPage 加载
   └─ ComboBox 创建: currentIndex = getCurrentAudioInputDevice()
      └─ 此时设备映射数组可能为空 → 返回默认值 0

2. PJSIP 初始化

3. 设备枚举
   └─ 建立设备映射数组（User index → PJSIP index）

4. 恢复保存的设备
   ├─ 从 QSettings 读取用户索引（1, 3）
   ├─ 映射到 PJSIP 索引（22, 22）
   ├─ 调用 pjsua_set_snd_dev2(22, 22)
   ├─ PJSIP 返回 "No changes"（当前已经是 22）
   └─ ✅ PJSIP 设备正确

5. 发射设备变化信号
   ├─ emit audioInputDevicesChanged(devices)
   ├─ emit audioOutputDevicesChanged(devices)
   └─ ❌ QML 只更新了 model，没有更新 currentIndex
```

---

## ✅ 解决方案

### 修改 1：更新扬声器 ComboBox currentIndex

**位置**: [SipSettingsPage.qml:728-739](../../src/qml/components/sip_phone/pages/SipSettingsPage.qml#L728-L739)

**修改前**:
```qml
Connections {
    target: SipPhoneManager
    function onAudioOutputDevicesChanged(devices) {
        console.log("📱 [QML Speaker] Received audioOutputDevicesChanged signal")
        console.log("   New device count:", devices.length)
    }
}
```

**修改后**:
```qml
Connections {
    target: SipPhoneManager
    function onAudioOutputDevicesChanged(devices) {
        console.log("📱 [QML Speaker] Received audioOutputDevicesChanged signal")
        console.log("   New device count:", devices.length)
        // ✅ 2026-01-19 16:45 [FIX 100.250.3] 更新 currentIndex 到保存的设备索引
        // 原因：设备枚举完成后，从 QSettings 读取保存的设备索引并更新 UI
        // 时序：QML 加载 → 设备枚举 → 发射信号 → 更新 currentIndex
        speakerDeviceCombo.currentIndex = SipPhoneManager.getCurrentAudioOutputDevice()
        console.log("   Updated currentIndex to:", speakerDeviceCombo.currentIndex)
    }
}
```

### 修改 2：更新麦克风 ComboBox currentIndex

**位置**: [SipSettingsPage.qml:790-801](../../src/qml/components/sip_phone/pages/SipSettingsPage.qml#L790-L801)

**修改前**:
```qml
Connections {
    target: SipPhoneManager
    function onAudioInputDevicesChanged(devices) {
        console.log("📱 [QML Mic] Received audioInputDevicesChanged signal")
        console.log("   New device count:", devices.length)
    }
}
```

**修改后**:
```qml
Connections {
    target: SipPhoneManager
    function onAudioInputDevicesChanged(devices) {
        console.log("📱 [QML Mic] Received audioInputDevicesChanged signal")
        console.log("   New device count:", devices.length)
        // ✅ 2026-01-19 16:45 [FIX 100.250.3] 更新 currentIndex 到保存的设备索引
        // 原因：设备枚举完成后，从 QSettings 读取保存的设备索引并更新 UI
        // 时序：QML 加载 → 设备枚举 → 发射信号 → 更新 currentIndex
        micDeviceCombo.currentIndex = SipPhoneManager.getCurrentAudioInputDevice()
        console.log("   Updated currentIndex to:", micDeviceCombo.currentIndex)
    }
}
```

---

## 🎯 修复效果

### 修复前

```
启动程序
  ↓
设备枚举
  ↓
PJSIP 设备恢复到 22 ✅（正确）
  ↓
UI currentIndex 仍然是 0 ❌（旧设备）
  ↓
用户看到旧设备名称
```

### 修复后

```
启动程序
  ↓
设备枚举
  ↓
PJSIP 设备恢复到 22 ✅
  ↓
发射 audioInputDevicesChanged 信号
  ↓
QML 更新 currentIndex = 1 ✅（保存的索引）
  ↓
用户看到保存的设备名称 ✅
```

---

## 🧪 测试验证

### 验证步骤

1. **重新编译和部署**：
   ```powershell
   .\build-ubuntu24-apt.ps1 188
   ```

2. **首次启动**：
   - 查看日志：`[CURRENT DEVICES]` 输出
   - 手动选择设备（输入 1，输出 3）
   - 关闭程序

3. **第二次启动**：
   - 查看日志：
     - `[CURRENT DEVICES]` 显示设备 22
     - `Updated currentIndex to: 1`（输入设备）
     - `Updated currentIndex to: 3`（输出设备）
   - **UI 应显示保存的设备名称**

### 预期日志

```
[DEBUG] 📱 [FIX 100.250] Restoring saved audio devices from QSettings
[DEBUG]    [QSettings] Settings file path: "/app/appdata/sip_accounts.ini"
[DEBUG]    [QSettings] Saved input device (user index): 1
[DEBUG]    [QSettings] Saved output device (user index): 3
[DEBUG]    📌 [CURRENT DEVICES] PJSIP current devices before restore:
[DEBUG]       Current capture_dev (input): 22
[DEBUG]       Current playback_dev (output): 22
[DEBUG]    ✅ [Mapping] Input device: User index 1 → PJSIP index 22
[DEBUG]    ✅ [Mapping] Output device: User index 3 → PJSIP index 22
[DEBUG]    [PJSIP] Calling pjsua_set_snd_dev2() to restore devices...
08:24:46.674            pjsua_aud.c  .No changes in capture and playback devices
[DEBUG]    ✅ [SUCCESS] Audio devices restored from QSettings!
[DEBUG] 📱 [QML Mic] Received audioInputDevicesChanged signal
[DEBUG]    New device count: 2
[DEBUG]    Updated currentIndex to: 1  ← ✅ UI 同步成功！
[DEBUG] 📱 [QML Speaker] Received audioOutputDevicesChanged signal
[DEBUG]    New device count: 4
[DEBUG]    Updated currentIndex to: 3  ← ✅ UI 同步成功！
```

---

## 📊 完整修复汇总

### FIX 100.250 系列

| 版本 | 问题 | 解决方案 | 状态 |
|------|------|----------|------|
| 100.250 | 设备索引映射错误 | 建立 User index → PJSIP index 映射数组 | ✅ 已完成 |
| 100.250.1 | 设备重启后不保存 | 添加 QSettings 持久化和启动恢复 | ✅ 已完成 |
| 100.250.2 | 容器重启后配置丢失 | 使用持久化路径 `/app/appdata/sip_accounts.ini` | ✅ 已完成 |
| **100.250.3** | **UI 不显示保存的设备** | **在设备信号中更新 QML currentIndex** | ✅ **已完成** |

### 技术架构

```
┌─────────────────────────────────────────────────┐
│ QML Layer                                        │
│                                                  │
│ ComboBox.currentIndex = getCurrentDevice()      │
│                ↓                                 │
│ onAudioInputDevicesChanged:                     │
│   ✅ currentIndex = getCurrentDevice()          │  ← FIX 100.250.3
│                                                  │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────┴──────────────────────────────┐
│ C++ Layer (SipPhoneManager)                     │
│                                                  │
│ getCurrentAudioInputDevice():                   │
│   ├─ Read QSettings                             │  ← FIX 100.250.2
│   │  Path: /app/appdata/sip_accounts.ini        │
│   ├─ Return user index (0-3)                    │
│   └─ Validate index range                       │
│                                                  │
│ initializeEndpoint():                           │
│   ├─ Enumerate devices                          │
│   ├─ Build mapping: User → PJSIP                │  ← FIX 100.250
│   ├─ Read saved user indices                    │  ← FIX 100.250.1
│   ├─ Map to PJSIP indices                       │
│   └─ Call pjsua_set_snd_dev2()                  │
│                                                  │
└──────────────────┬──────────────────────────────┘
                   │
┌──────────────────┴──────────────────────────────┐
│ PJSIP Layer                                      │
│                                                  │
│ pjsua_set_snd_dev2(capture=22, playback=22)     │
│   ├─ Check if current == requested              │
│   ├─ Return "No changes" if same                │
│   └─ Actually set device if different           │
│                                                  │
└─────────────────────────────────────────────────┘
```

---

## 🔗 相关文档

### 问题追踪
- [FIX 100.250.3 调试 PJSIP "No changes" 问题](11-FIX100.250.3-调试PJSIP-No-changes问题.md)
- [FIX 100.250.2 完成 - QSettings 持久化路径修复](09-FIX100.250.2完成-QSettings持久化路径修复.md)
- [FIX 100.250.1 完成 - 设备持久化和日志优化](07-FIX100.250.1完成-设备持久化修复和日志优化.md)
- [FIX 100.250 完成 - 设备索引映射](06-FIX100.250完成-设备索引映射.md)

### 技术参考
- [Qt QML ComboBox](https://doc.qt.io/qt-6/qml-qtquick-controls-combobox.html)
- [Qt Signals and Slots](https://doc.qt.io/qt-6/signalsandslots.html)
- [PJSIP Sound Device API](https://docs.pjsip.org/en/latest/api/pjsua.html#sound-device-api)

---

## 🚀 下一步

### 立即任务
1. ✅ 添加当前设备调试日志
2. ✅ 统一配置文件路径（sip_accounts.ini）
3. ✅ 修复 QML UI 同步问题
4. ⏳ 用户执行构建和测试
5. ⏳ 验证设备持久化完全正常

### 使用命令
```powershell
# 用户手动执行编译和部署（最终版本）
.\build-ubuntu24-apt.ps1 188
```

### 预期结果
- ✅ 第一次选择设备 → 保存成功
- ✅ 关闭程序重启 → **UI 显示保存的设备** ← 关键修复！
- ✅ 实际通话使用保存的设备

---

**创建时间**: 2026-01-19 16:45
**状态**: ✅ 已完成
**Git Commits**: `待提交`
