# 修复 89 实施完成 - 代码修改总结

**日期**: 2026-01-08 20:20
**状态**: ✅ **代码修改完成，等待编译测试**

---

## 📝 修改摘要

### 问题回顾
- **Fix 86 失败原因**: 转换代码在 `got_picture == TRUE` 块内（Line 3418）
- **根本原因**: `avcodec_receive_frame()` 返回错误 → `got_picture = FALSE` → 提前返回 `EBADBITSTREAM`
- **结果**: Fix 86 代码永远不会执行，程序 3-4 秒后崩溃 (exit 139)

### Fix 89 解决方案
**核心策略**: 在 `avcodec_receive_frame()` 之后**立即**转换，不等待 `got_picture` 检查

---

## 🔧 代码修改详情

### 修改文件
- **文件**: `cross-compile/src/pjproject-2.16/pjmedia/src/pjmedia-codec/ffmpeg_vid_codecs.c`
- **修改位置**: Line 3349 之后插入 70+ 行代码
- **新增代码**: Line 3351-3411

### 修改前（Fix 86，Line 3347-3357）
```c
err = avcodec_send_packet(ff->dec_ctx, &avpacket);
if (err == 0 || err == AVERROR(EAGAIN)) {
    err = avcodec_receive_frame(ff->dec_ctx, &avframe);
    if (err == 0) {
        got_picture = PJ_TRUE;
    } else if (err == AVERROR(EAGAIN) || err == AVERROR_EOF) {
        err = 0;
        got_picture = PJ_FALSE;
    }
}
// ← Fix 86 的转换代码在 Line 3418（too late!）
```

### 修改后（Fix 89，Line 3347-3421）
```c
err = avcodec_send_packet(ff->dec_ctx, &avpacket);
if (err == 0 || err == AVERROR(EAGAIN)) {
    err = avcodec_receive_frame(ff->dec_ctx, &avframe);

    /* ✅ 2026-01-08 20:10 [修复 89] 在 receive_frame 后立即转换 DRM_PRIME */
    if (avframe.format == AV_PIX_FMT_DRM_PRIME) {  // format 179
        AVFrame *sw_frame;
        int transfer_ret;

        PJ_LOG(3,(THIS_FILE, "🔍 [FIX 89] Detected DRM_PRIME immediately after receive_frame"));
        PJ_LOG(3,(THIS_FILE, "   Original err=%d, attempting format conversion...", err));

        sw_frame = av_frame_alloc();
        if (!sw_frame) {
            PJ_LOG(1,(THIS_FILE, "❌ [FIX 89] Failed to allocate sw_frame"));
            err = AVERROR(ENOMEM);
            got_picture = PJ_FALSE;
        } else {
            transfer_ret = av_hwframe_transfer_data(sw_frame, &avframe, 0);
            if (transfer_ret < 0) {
                char errbuf[128];
                av_strerror(transfer_ret, errbuf, sizeof(errbuf));
                PJ_LOG(1,(THIS_FILE, "❌ [FIX 89] av_hwframe_transfer_data failed: %d (%s)",
                          transfer_ret, errbuf));
                av_frame_free(&sw_frame);
                err = transfer_ret;
                got_picture = PJ_FALSE;
            } else {
                // 转换成功！
                PJ_LOG(3,(THIS_FILE, "✅ [FIX 89] DRM_PRIME → %s conversion SUCCESS",
                          av_get_pix_fmt_name(sw_frame->format) ?
                          av_get_pix_fmt_name(sw_frame->format) : "unknown"));

                // 复制元数据
                sw_frame->pts = avframe.pts;
                sw_frame->pkt_dts = avframe.pkt_dts;
                sw_frame->width = avframe.width;
                sw_frame->height = avframe.height;

                // 释放 GPU 帧
                av_frame_unref(&avframe);

                // 使用系统内存帧替换原始帧
                avframe = *sw_frame;
                av_free(sw_frame);

                // ✅ 修正状态：转换成功视为解码成功
                err = 0;
                got_picture = PJ_TRUE;

                PJ_LOG(3,(THIS_FILE, "   Corrected: err=0, got_picture=TRUE, format=%d (%s)",
                          avframe.format,
                          av_get_pix_fmt_name(avframe.format) ?
                          av_get_pix_fmt_name(avframe.format) : "unknown"));
            }
        }
    }

    // 原始逻辑继续（如果 Fix 89 没有设置 got_picture）
    if (err == 0) {
        got_picture = PJ_TRUE;
    } else if (err == AVERROR(EAGAIN) || err == AVERROR_EOF) {
        err = 0;
        got_picture = PJ_FALSE;
    }
}
```

---

## 📊 代码执行流程对比

### Fix 86（失败流程）
```
1. avcodec_receive_frame() → err != 0, format=179 (DRM_PRIME)
2. Line 3350-3356: err != 0 → got_picture = FALSE
3. Line 3362-3374: err < 0 → return EBADBITSTREAM
   ↓
   ❌ Fix 86 代码 (Line 3418) 永远不会执行
```

### Fix 89（修复流程）
```
1. avcodec_receive_frame() → err != 0, format=179 (DRM_PRIME)
2. ✅ Line 3358: 立即检测 avframe.format == DRM_PRIME
3. ✅ Line 3373: av_hwframe_transfer_data() GPU → CPU
4. ✅ Line 3402-3403: 修正 err=0, got_picture=TRUE
5. Line 3414-3420: 原始逻辑（已被 Fix 89 修正，不会失败）
6. ✅ 后续正常处理 NV12 帧
```

---

## 🎯 预期测试结果

### 成功标志

**关键日志**（按顺序出现）:
```
✅ [FIX 84] Detected RKMPP decoder: h264_rkmpp
   Strategy: Hybrid mode (VPU decode + system memory output)

✅ [FIX 84] RKMPP decoder opened successfully (hybrid mode)
   Decoder output format: 179 (expected: 23=NV12 or 0=I420)
   ⚠️ Unexpected format 179! May cause compatibility issues.

🔍 [FIX 89] Detected DRM_PRIME immediately after receive_frame
   Original err=-1094995529, attempting format conversion...

✅ [FIX 89] DRM_PRIME → nv12 conversion SUCCESS
   Corrected: err=0, got_picture=TRUE, format=23 (nv12)

🎉 双向视频通话正常建立！
   - 对方看到本机视频 ✅
   - 本机看到对方视频 ✅（Fix 89 修复）
   - 无崩溃 ✅
   - CPU 占用 ~10-15% ✅
```

### 成功指标

| 指标 | Fix 86 (失败) | Fix 89 (预期) |
|------|--------------|--------------|
| **[FIX 89] 日志** | ❌ 无 | ✅ **必须出现** |
| **格式转换** | ❌ 未执行 | ✅ **DRM_PRIME → NV12** |
| **解码错误** | ❌ 251 个 | ✅ **0 个** |
| **程序崩溃** | ❌ 3-4秒后崩溃 | ✅ **不崩溃** |
| **本机看远端** | ❌ 看不到 | ✅ **看得到** |
| **CPU 占用** | - | ✅ **~10-15%** |

---

## ⚠️ 潜在风险和缓解

### 风险 1: av_hwframe_transfer_data() 失败
- **概率**: 🟨 中等
- **症状**: 返回错误码（如 -12 ENOMEM, -22 EINVAL）
- **日志**: `❌ [FIX 89] av_hwframe_transfer_data failed: -12 (Cannot allocate memory)`
- **缓解**:
  - 代码已设置 `err = transfer_ret`, `got_picture = FALSE`
  - 后续会返回 `EBADBITSTREAM`（与 Fix 86 相同）
  - 不会比 Fix 86 更差

### 风险 2: 转换后格式不是 NV12
- **概率**: 🟩 低
- **可能格式**: I420 (format 0) 或其他
- **影响**: 轻微（I420 也是可接受的）
- **日志**: `✅ [FIX 89] DRM_PRIME → yuv420p conversion SUCCESS`

### 风险 3: 内存泄漏
- **概率**: 🟩 极低
- **原因**: 代码已正确调用 `av_frame_unref()` 和 `av_free()`
- **缓解**: 与 Fix 86 相同的内存管理逻辑

---

## 📁 相关文件

### 修改的文件
1. **ffmpeg_vid_codecs.c** - Line 3351-3411: Fix 89 核心代码

### 相关文档
1. [16-修复89-在receive_frame后立即转换DRM_PRIME.md](16-修复89-在receive_frame后立即转换DRM_PRIME.md) - Fix 89 设计方案
2. [14-修复86-使用av_hwframe_transfer_data转换格式.md](14-修复86-使用av_hwframe_transfer_data转换格式.md) - Fix 86 原理
3. [12-修复84-移除hwdevice使用混合模式.md](12-修复84-移除hwdevice使用混合模式.md) - Fix 84 背景

---

## 🚀 下一步

### 1. 用户编译测试（由用户执行）
```powershell
.\build-ubuntu24-apt.ps1 188
```

### 2. 查看测试日志
- **位置**: `docs/log/voip.md`
- **关键搜索**: `[FIX 89]`

### 3. 验证要点

**必须出现的日志**:
- ✅ `[FIX 89] Detected DRM_PRIME immediately`
- ✅ `[FIX 89] DRM_PRIME → nv12 conversion SUCCESS` 或 `yuv420p`
- ✅ `Corrected: err=0, got_picture=TRUE`

**不应出现的错误**:
- ❌ `codec decode() error: Bad or corrupted bitstream`
- ❌ `Application exited with code: 139`

**功能验证**:
- ✅ 双向视频通话正常
- ✅ 本机可以看到远端视频
- ✅ 远端仍然可以看到本机视频
- ✅ 无崩溃

---

## 📊 性能预期

### CPU 占用分解（720x480 @ 25fps）
- VPU 硬件解码：~0-2%
- GPU → CPU 格式转换：**~5-10ms/frame** (5-10%)
- Qt/SDL 渲染：~2-3%
- **总计**：**~10-15%**

### 对比其他方案
- ✅ Fix 89（硬件解码 + 转换）：**~10-15%** ← **推荐**
- ⚠️ Fix 85（纯软件解码）：~25-35%
- ❌ Fix 86（硬件解码，转换未执行）：崩溃

---

**文档版本**: v1.0
**创建时间**: 2026-01-08 20:20
**状态**: ✅ 代码修改完成，等待用户编译测试
**修改行数**: +70 行（Line 3351-3411）
