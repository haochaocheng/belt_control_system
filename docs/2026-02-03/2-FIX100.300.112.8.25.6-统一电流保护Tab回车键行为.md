# FIX 100.300.112.8.25.6 - 统一电流保护Tab的回车键行为与基本配置Tab

**日期**: 2026-02-02
**任务编号**: FIX 100.300.112.8.25.6
**状态**: ✅ 已完成

---

## 🎯 问题描述

用户反馈：**电流保护Tab按回车键的方式和基本配置Tab不一致**

**问题分析**：
- **基本配置Tab**: 按回车键会打开虚拟键盘进行输入
- **电流保护Tab**: 按回车键只是调用 `forceActiveFocus()`，没有打开虚拟键盘

**根本原因**：
电流保护Tab的 `triggerParamInput()` 函数实现不完整，只调用了 `forceActiveFocus()`，而没有像基本配置Tab一样打开虚拟键盘。

---

## 🔧 修改内容

### CurrentProtectionTab.qml - 重写 triggerParamInput() 函数

**修改位置**: Line 747-826

**修改前**：
```qml
function triggerParamInput(paramIndex) {
    console.log("✅ [CurrentProtectionTab] 触发参数输入 - 索引:", paramIndex)

    switch(paramIndex) {
    case 0:  // 是否投入（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 切换是否投入")
        enabledField.forceActiveFocus()  // ❌ 只调用 forceActiveFocus
        break
    case 1:  // 报警类型（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 切换报警类型")
        alarmTypeField.forceActiveFocus()  // ❌ 只调用 forceActiveFocus
        break
    // ... 其他参数类似
    }
}
```

**修改后**：
```qml
function triggerParamInput(paramIndex) {
    console.log("✅ [CurrentProtectionTab] 触发参数输入 - 索引:", paramIndex)

    // ✅ 2026-02-02 [FIX 100.300.112.8.25.6]: 改为打开虚拟键盘，与 BasicConfigTab 保持一致
    var inputField = null
    var inputMode = "numeric"  // 默认数字模式

    switch(paramIndex) {
    case 0:  // 是否投入（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 切换是否投入")
        // ComboBox 不需要虚拟键盘，直接切换选项
        enabledField.currentIndex = (enabledField.currentIndex + 1) % enabledField.model.length
        break
    case 1:  // 报警类型（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 切换报警类型")
        // ComboBox 不需要虚拟键盘，直接切换选项
        alarmTypeField.currentIndex = (alarmTypeField.currentIndex + 1) % alarmTypeField.model.length
        break
    case 2:  // 次数设置（CustomSpinBox，条件启用）
        console.log("✅ [CurrentProtectionTab] 次数设置")
        if (alarmTypeField.currentIndex === 0) {
            inputField = countSettingField
            inputMode = "numeric"
        }
        break
    case 3:  // 时间设置（CustomSpinBox，条件启用）
        console.log("✅ [CurrentProtectionTab] 时间设置")
        if (alarmTypeField.currentIndex === 1) {
            inputField = timeSettingField
            inputMode = "numeric"
        }
        break
    case 4:  // 动作保护类型（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 动作保护类型")
        // ComboBox 不需要虚拟键盘，直接切换选项
        actionProtectionField.currentIndex = (actionProtectionField.currentIndex + 1) % actionProtectionField.model.length
        break
    case 5:  // 故障保护类型（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 故障保护类型")
        // ComboBox 不需要虚拟键盘，直接切换选项
        faultProtectionField.currentIndex = (faultProtectionField.currentIndex + 1) % faultProtectionField.model.length
        break
    case 6:  // 电流量程（CustomSpinBox）
        console.log("✅ [CurrentProtectionTab] 电流量程")
        inputField = currentRangeField
        inputMode = "numeric"
        break
    case 7:  // 电流上限（CustomSpinBox）
        console.log("✅ [CurrentProtectionTab] 电流上限")
        inputField = currentUpperField
        inputMode = "numeric"
        break
    case 8:  // 电流下限（CustomSpinBox）
        console.log("✅ [CurrentProtectionTab] 电流下限")
        inputField = currentLowerField
        inputMode = "numeric"
        break
    case 9:  // 输入点选择（CustomComboBox）
        console.log("✅ [CurrentProtectionTab] 输入点选择")
        // ComboBox 不需要虚拟键盘，直接切换选项
        inputPointField.currentIndex = (inputPointField.currentIndex + 1) % inputPointField.model.length
        break
    case 10:  // 过滤干扰延时（CustomSpinBox）
        console.log("✅ [CurrentProtectionTab] 过滤干扰延时")
        inputField = filterDelayField
        inputMode = "numeric"
        break
    }

    // 打开虚拟键盘
    if (virtualKeyboard && inputField) {
        console.log("✅ [CurrentProtectionTab] 打开 Qt 虚拟键盘 - 控件:", inputField, "模式:", inputMode)
        virtualKeyboard.openForField(inputField, function(newValue) {
            console.log("✅ [CurrentProtectionTab] 虚拟键盘输入完成:", newValue)
        }, inputMode, root)
    } else if (inputField) {
        console.log("⚠️ [CurrentProtectionTab] 虚拟键盘不可用，使用 forceActiveFocus")
        inputField.forceActiveFocus()
    }
}
```

---

## ✅ 修改效果

### 参数类型和回车键行为

| 参数索引 | 参数名称 | 控件类型 | 回车键行为 |
|---------|---------|---------|-----------|
| 0 | 是否投入 | CustomComboBox | 切换选项（投入 ↔ 禁用） |
| 1 | 报警类型 | CustomComboBox | 切换选项（按次数 ↔ 按时间） |
| 2 | 次数设置 | CustomSpinBox | 打开虚拟键盘（数字模式） |
| 3 | 时间设置 | CustomSpinBox | 打开虚拟键盘（数字模式） |
| 4 | 动作保护类型 | CustomComboBox | 切换选项（预警停机 → 预警不停机 → 立即停机） |
| 5 | 故障保护类型 | CustomComboBox | 切换选项（预警停机 → 预警不停机 → 立即停机） |
| 6 | 电流量程 | CustomSpinBox | 打开虚拟键盘（数字模式） |
| 7 | 电流上限 | CustomSpinBox | 打开虚拟键盘（数字模式） |
| 8 | 电流下限 | CustomSpinBox | 打开虚拟键盘（数字模式） |
| 9 | 输入点选择 | CustomComboBox | 切换选项（AI0.0 → AI0.1 → ... → AI1.7） |
| 10 | 过滤干扰延时 | CustomSpinBox | 打开虚拟键盘（数字模式） |

### 与基本配置Tab的一致性

| 特性 | 基本配置Tab | 电流保护Tab（修改前） | 电流保护Tab（修改后） |
|------|-----------|-------------------|-------------------|
| **CustomSpinBox** | 打开虚拟键盘 ✅ | 只调用 forceActiveFocus ❌ | 打开虚拟键盘 ✅ |
| **CustomComboBox** | 切换选项 ✅ | 只调用 forceActiveFocus ❌ | 切换选项 ✅ |
| **条件启用参数** | N/A | 条件判断 ✅ | 条件判断 ✅ |
| **虚拟键盘回调** | 有 ✅ | 无 ❌ | 有 ✅ |

---

## 💡 设计说明

### 为什么 CustomComboBox 不打开虚拟键盘？

**原因**：
- CustomComboBox 是下拉选择框，用户从预定义的选项中选择
- 不需要输入数字或文本，只需要切换选项
- 按回车键直接切换到下一个选项更直观

**实现**：
```qml
enabledField.currentIndex = (enabledField.currentIndex + 1) % enabledField.model.length
```
- 使用模运算实现循环切换
- 例如：投入 → 禁用 → 投入 → ...

### 为什么 CustomSpinBox 打开虚拟键盘？

**原因**：
- CustomSpinBox 是数字输入框，用户需要输入具体的数值
- 虚拟键盘提供更好的输入体验
- 支持快速输入大范围的数值

**实现**：
```qml
if (virtualKeyboard && inputField) {
    virtualKeyboard.openForField(inputField, function(newValue) {
        console.log("✅ [CurrentProtectionTab] 虚拟键盘输入完成:", newValue)
    }, inputMode, root)
}
```

### 条件启用参数的处理

**次数设置和时间设置**：
```qml
case 2:  // 次数设置（条件启用）
    if (alarmTypeField.currentIndex === 0) {  // 只在"按次数"时启用
        inputField = countSettingField
        inputMode = "numeric"
    }
    break
```

**说明**：
- 只有在满足条件时才设置 `inputField`
- 如果不满足条件，`inputField` 保持为 `null`，不会打开虚拟键盘
- 这与参数的 `enabled` 属性保持一致

---

## 📄 修改文件

- `src/qml/components/device_info/pages/CurrentProtectionTab.qml`
  - Line 747-826: 重写 `triggerParamInput()` 函数

---

## 🔗 相关文档

- [37-FIX100.300.112.8.25.5-修复NavigationManager导航索引硬编码问题.md](./37-FIX100.300.112.8.25.5-修复NavigationManager导航索引硬编码问题.md)
- [36-FIX100.300.112.8.25.4-更新导航函数和调整禁用颜色.md](./36-FIX100.300.112.8.25.4-更新导航函数和调整禁用颜色.md)
- [32-FIX100.300.112.8.25-电流保护参数区细化需求完成总结.md](./32-FIX100.300.112.8.25-电流保护参数区细化需求完成总结.md)

---

## 📦 下一步行动

### 立即完成

1. ✅ 统一电流保护Tab的回车键行为（已完成）
2. ⏳ 更新数据加载和保存逻辑（loadProtectionData, saveProtectionData）
3. ⏳ 在 QDS 中测试所有控件和导航功能

---

**创建日期**: 2026-02-02
**状态**: ✅ 已完成
**编写人员**: Claude Sonnet 4.5
