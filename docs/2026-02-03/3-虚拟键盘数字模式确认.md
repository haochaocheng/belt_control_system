# 虚拟键盘数字模式确认

**日期**: 2026-02-02
**状态**: ✅ 已确认

---

## 🎯 用户反馈

用户测试完成后反馈：
- ✅ 功能满足需求
- ✅ 虚拟键盘弹出纯数字输入
- ✅ 不需要英文键盘

---

## 📋 当前实现

### 1. CurrentProtectionTab.qml - triggerParamInput()

所有CustomSpinBox参数都设置为 `"numeric"` 模式：

```qml
function triggerParamInput(paramIndex) {
    var inputField = null
    var inputMode = "numeric"  // 默认数字模式

    switch(paramIndex) {
    case 2:  // 次数设置
        inputField = countSettingField
        inputMode = "numeric"  // ✅ 数字模式
        break
    case 3:  // 时间设置
        inputField = timeSettingField
        inputMode = "numeric"  // ✅ 数字模式
        break
    case 6:  // 电流量程
        inputField = currentRangeField
        inputMode = "numeric"  // ✅ 数字模式
        break
    case 7:  // 电流上限
        inputField = currentUpperField
        inputMode = "numeric"  // ✅ 数字模式
        break
    case 8:  // 电流下限
        inputField = currentLowerField
        inputMode = "numeric"  // ✅ 数字模式
        break
    case 10:  // 过滤干扰延时
        inputField = filterDelayField
        inputMode = "numeric"  // ✅ 数字模式
        break
    }

    // 打开虚拟键盘
    if (virtualKeyboard && inputField) {
        virtualKeyboard.openForField(inputField, function(newValue) {
            console.log("✅ [CurrentProtectionTab] 虚拟键盘输入完成:", newValue)
        }, inputMode, root)  // ✅ 传递 "numeric" 模式
    }
}
```

### 2. QtVirtualKeyboardIntegration.qml - openForField()

根据 `mode` 参数设置输入法提示：

```qml
function openForField(textField, callback, mode, parentPageRef) {
    targetTextField = textField
    updateCallback = callback
    inputMode = mode || "numeric"
    parentPage = parentPageRef || null

    // Set input method hints based on mode
    if (textField) {
        if (mode === "numeric") {
            textField.inputMethodHints = Qt.ImhDigitsOnly  // ✅ 只显示数字键盘
        } else if (mode === "english") {
            textField.inputMethodHints = Qt.ImhNoPredictiveText | Qt.ImhPreferLowercase
        } else if (mode === "chinese") {
            textField.inputMethodHints = Qt.ImhNone
        }

        textField.forceActiveFocus()
    }

    root.open()
}
```

### 3. Qt InputPanel

使用Qt自带的 `InputPanel` 组件：

```qml
contentItem: InputPanel {
    id: inputPanel
    z: 99
    anchors.fill: parent
}
```

**Qt.ImhDigitsOnly 的作用**：
- 告诉Qt的InputPanel只显示数字键盘
- 隐藏字母键盘和其他非数字按键
- 只显示：0-9、小数点、负号（如果需要）

---

## ✅ 测试结果

根据用户反馈：
- ✅ 虚拟键盘弹出时只显示数字键盘
- ✅ 没有英文字母键盘
- ✅ 功能满足需求

---

## 📊 参数和键盘模式映射

| 参数索引 | 参数名称 | 控件类型 | 回车键行为 | 键盘模式 |
|---------|---------|---------|-----------|---------|
| 0 | 是否投入 | CustomComboBox | 切换选项 | 无键盘 |
| 1 | 报警类型 | CustomComboBox | 切换选项 | 无键盘 |
| 2 | 次数设置 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |
| 3 | 时间设置 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |
| 4 | 动作保护类型 | CustomComboBox | 切换选项 | 无键盘 |
| 5 | 故障保护类型 | CustomComboBox | 切换选项 | 无键盘 |
| 6 | 电流量程 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |
| 7 | 电流上限 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |
| 8 | 电流下限 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |
| 9 | 输入点选择 | CustomComboBox | 切换选项 | 无键盘 |
| 10 | 过滤干扰延时 | CustomSpinBox | 打开虚拟键盘 | **numeric** ✅ |

---

## 💡 设计说明

### 为什么使用 Qt.ImhDigitsOnly？

**Qt.ImhDigitsOnly** 是Qt提供的输入法提示标志，专门用于数字输入：

1. **自动选择键盘布局**：
   - Qt的InputPanel会根据这个标志自动选择数字键盘布局
   - 隐藏字母键盘和其他非数字按键

2. **跨平台一致性**：
   - 在不同平台（Windows、Linux、Android、iOS）上都能正确显示数字键盘
   - Qt会根据平台特性自动适配

3. **用户体验**：
   - 用户不需要手动切换键盘布局
   - 直接显示数字键盘，提高输入效率

### 其他可用的输入法提示

Qt提供了多种输入法提示标志：

| 标志 | 说明 | 键盘布局 |
|------|------|---------|
| `Qt.ImhDigitsOnly` | 只允许数字 | 数字键盘（0-9、.、-） |
| `Qt.ImhFormattedNumbersOnly` | 格式化数字 | 数字键盘（0-9、.、-、,） |
| `Qt.ImhNoPredictiveText` | 禁用预测文本 | 标准键盘 |
| `Qt.ImhPreferLowercase` | 优先小写 | 小写字母键盘 |
| `Qt.ImhPreferUppercase` | 优先大写 | 大写字母键盘 |
| `Qt.ImhNone` | 无限制 | 标准键盘 |

---

## 🔗 相关文档

- [38-FIX100.300.112.8.25.6-统一电流保护Tab回车键行为.md](./38-FIX100.300.112.8.25.6-统一电流保护Tab回车键行为.md)
- [37-FIX100.300.112.8.25.5-修复NavigationManager导航索引硬编码问题.md](./37-FIX100.300.112.8.25.5-修复NavigationManager导航索引硬编码问题.md)

---

## 📦 总结

### 已确认

- ✅ 所有CustomSpinBox参数都使用 `"numeric"` 模式
- ✅ Qt.ImhDigitsOnly 正确设置，只显示数字键盘
- ✅ 用户测试通过，功能满足需求
- ✅ 虚拟键盘弹出时不显示英文字母键盘

### 无需修改

当前实现已经满足用户需求，无需进行任何修改。

---

**创建日期**: 2026-02-02
**状态**: ✅ 已确认
**编写人员**: Claude Sonnet 4.5
