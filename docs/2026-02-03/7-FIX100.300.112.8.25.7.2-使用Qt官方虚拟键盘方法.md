# FIX 100.300.112.8.25.7.2 - 使用Qt官方虚拟键盘方法

**日期**: 2026-02-02
**任务编号**: FIX 100.300.112.8.25.7.2
**状态**: ✅ 已完成

---

## 🎯 问题描述

用户反馈：**我们选择的是自定义的虚拟键盘，不是Qt官方的虚拟键盘使用方法**

用户提供了Qt官方示例路径：`C:\Qt\Examples\Qt-6.5.3\virtualkeyboard\basic`

**根本原因**：
- 我们创建了自定义的 QtVirtualKeyboardIntegration Popup
- 需要手动调用 `virtualKeyboard.openForField()`
- 而Qt官方的方式是：**TextField获得焦点时，Qt会自动显示虚拟键盘**

---

## 🔧 修改内容

### 1. CustomSpinBox.qml - 改为 Qt.ImhDigitsOnly

**修改位置**: Line 68-69

**修改前**：
```qml
inputMethodHints: Qt.ImhFormattedNumbersOnly
```

**修改后**：
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.25.7.2]: 改为 Qt.ImhDigitsOnly，Qt会自动显示纯数字键盘
inputMethodHints: Qt.ImhDigitsOnly
```

### 2. CustomSpinBox.qml - 移除手动键盘调用（MouseArea）

**修改位置**: Line 74-84

**修改前**：
```qml
// ✅ 2026-01-28 [触摸屏支持]: 点击文本区域时弹出键盘
MouseArea {
    anchors.fill: parent
    enabled: root.editable && root.enabled
    onClicked: {
        root.forceActiveFocus()
        if (keyboardManager) {
            keyboardManager.openKeyboardForField(root, true)  // true = 触摸模式
        }
    }
    // 允许文本选择
    onPressed: mouse.accepted = false
}
```

**修改后**：
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.25.7.2]: Qt会自动显示虚拟键盘，只需要设置焦点
MouseArea {
    anchors.fill: parent
    enabled: root.editable && root.enabled
    onClicked: {
        root.forceActiveFocus()  // Qt会自动显示虚拟键盘
        // ❌ 2026-02-02 [移除]: 不再需要手动调用 keyboardManager.openKeyboardForField
    }
    // 允许文本选择
    onPressed: mouse.accepted = false
}
```

### 3. CustomSpinBox.qml - 移除 Enter/Space 键处理

**修改位置**: Line 132-134

**修改前**：
```qml
// ✅ 2026-01-28 [键盘导航]: Enter/Space 键弹出虚拟键盘
Keys.onReturnPressed: {
    if (keyboardManager && root.editable && root.enabled) {
        keyboardManager.openKeyboardForField(root, false)  // false = 键盘导航模式
    }
}

Keys.onSpacePressed: {
    if (keyboardManager && root.editable && root.enabled) {
        keyboardManager.openKeyboardForField(root, false)  // false = 键盘导航模式
        event.accepted = true  // 阻止默认行为
    }
}
```

**修改后**：
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.25.7.2]: Qt会自动显示虚拟键盘，移除手动调用
// ❌ 2026-02-02 [移除]: Keys.onReturnPressed 和 Keys.onSpacePressed 不再需要
// Qt会在TextField获得焦点时自动显示虚拟键盘
```

### 4. CurrentProtectionTab.qml - 简化 triggerParamInput()

**修改位置**: Line 750-815

**修改前**：
```qml
function triggerParamInput(paramIndex) {
    var inputField = null
    var inputMode = "numeric"  // 默认数字模式

    switch(paramIndex) {
    case 2:  // 次数设置
        if (alarmTypeField.currentIndex === 0) {
            inputField = countSettingField
            inputMode = "numeric"
        }
        break
    // ... 其他case
    }

    // 打开虚拟键盘
    if (virtualKeyboard && inputField) {
        virtualKeyboard.openForField(inputField, function(newValue) {
            console.log("✅ [CurrentProtectionTab] 虚拟键盘输入完成:", newValue)
        }, inputMode, root)
    } else if (inputField) {
        inputField.forceActiveFocus()
    }
}
```

**修改后**：
```qml
function triggerParamInput(paramIndex) {
    // ✅ 2026-02-02 [FIX 100.300.112.8.25.7.2]: Qt会自动显示虚拟键盘，只需要设置焦点
    var inputField = null

    switch(paramIndex) {
    case 2:  // 次数设置
        if (alarmTypeField.currentIndex === 0) {
            inputField = countSettingField
        }
        break
    // ... 其他case
    }

    // ✅ 2026-02-02 [FIX 100.300.112.8.25.7.2]: Qt会自动显示虚拟键盘
    // 只需要设置焦点，Qt会根据 inputMethodHints 自动显示纯数字键盘
    if (inputField) {
        console.log("✅ [CurrentProtectionTab] 设置焦点，Qt会自动显示虚拟键盘")
        inputField.forceActiveFocus()
    }
}
```

---

## ✅ 修改效果

### Qt官方虚拟键盘工作原理

1. **TextField 获得焦点**（点击或键盘导航）
2. **Qt 检测 inputMethodHints**（Qt.ImhDigitsOnly）
3. **Qt 自动显示纯数字键盘**
4. **用户输入完成后，键盘自动隐藏**

### 代码简化对比

| 特性 | 自定义Popup方式（旧） | Qt官方方式（新） |
|------|---------------------|-----------------|
| **代码量** | 300+ 行QML | 1行属性 |
| **复杂度** | 高（Popup、openForField） | 低（只需inputMethodHints） |
| **维护性** | 差（自定义逻辑多） | 好（Qt官方维护） |
| **自动化** | 需要手动调用 | 自动显示/隐藏 |
| **兼容性** | 可能有问题 | 完全兼容 |

---

## 📊 Qt.ImhDigitsOnly 的作用

**Qt.ImhDigitsOnly** 是Qt提供的输入法提示标志：

1. **自动选择键盘布局**：
   - Qt的InputPanel会根据这个标志自动选择数字键盘布局
   - 隐藏字母键盘和其他非数字按键

2. **跨平台一致性**：
   - 在不同平台（Windows、Linux、Android、iOS）上都能正确显示数字键盘
   - Qt会根据平台特性自动适配

3. **用户体验**：
   - 用户不需要手动切换键盘布局
   - 直接显示数字键盘，提高输入效率

### 其他可用的输入法提示

| 标志 | 说明 | 键盘布局 |
|------|------|---------| | `Qt.ImhDigitsOnly` | 只允许数字 | 数字键盘（0-9、.、-） |
| `Qt.ImhFormattedNumbersOnly` | 格式化数字 | 数字键盘（0-9、.、-、,） |
| `Qt.ImhDialableCharactersOnly` | 电话号码 | 电话键盘（0-9、+、-、#、*） |
| `Qt.ImhNoPredictiveText` | 禁用预测文本 | 标准键盘 |
| `Qt.ImhPreferLowercase` | 优先小写 | 小写字母键盘 |
| `Qt.ImhPreferUppercase` | 优先大写 | 大写字母键盘 |
| `Qt.ImhNone` | 无限制 | 标准键盘 |

---

## 💡 为什么Qt官方方式更好？

### 1. 简单性

**自定义方式**：
```qml
// 需要创建Popup（300+ 行代码）
Popup {
    id: virtualKeyboard
    // ...
}

// 需要手动调用
function triggerParamInput(paramIndex) {
    virtualKeyboard.openForField(inputField, callback, mode, parent)
}
```

**Qt官方方式**：
```qml
// 只需要一行
TextField {
    inputMethodHints: Qt.ImhDigitsOnly
}
```

### 2. 自动化

**自定义方式**：
- 需要手动检测焦点
- 需要手动打开/关闭键盘
- 需要手动管理键盘状态

**Qt官方方式**：
- Qt自动检测焦点
- Qt自动显示/隐藏键盘
- Qt自动管理键盘状态

### 3. 兼容性

**自定义方式**：
- 可能与Qt更新不兼容
- 可能与其他组件冲突
- 需要自己维护

**Qt官方方式**：
- Qt官方维护
- 完全兼容Qt生态
- 自动适配平台

---

## 📄 修改文件

- `src/qml/components/device_info/CustomSpinBox.qml`
  - Line 68-69: 改为 Qt.ImhDigitsOnly
  - Line 74-84: 移除手动键盘调用（MouseArea）
  - Line 132-134: 移除 Enter/Space 键处理

- `src/qml/components/device_info/pages/CurrentProtectionTab.qml`
  - Line 750-815: 简化 triggerParamInput()，只保留 forceActiveFocus()

---

## 🔗 相关文档

- Qt官方文档：[Qt Virtual Keyboard](https://doc.qt.io/qt-6/qtvirtualkeyboard-index.html)
- Qt官方示例：`C:\Qt\Examples\Qt-6.5.3\virtualkeyboard\basic`
- [42-Qt虚拟键盘正确使用方法分析.md](./42-Qt虚拟键盘正确使用方法分析.md)
- [40-FIX100.300.112.8.25.7-使用自定义NumericKeyboard.md](./40-FIX100.300.112.8.25.7-使用自定义NumericKeyboard.md)
- [41-FIX100.300.112.8.25.7.1-使用参数设置界面纯数字键盘设计.md](./41-FIX100.300.112.8.25.7.1-使用参数设置界面纯数字键盘设计.md)

---

## 📦 下一步行动

### 立即测试

1. ✅ 修改 CustomSpinBox 使用 Qt.ImhDigitsOnly（已完成）
2. ✅ 移除手动键盘调用逻辑（已完成）
3. ⏳ 测试数字键盘功能
   - 点击数字输入框
   - 检查是否自动弹出纯数字键盘
   - 检查键盘是否只显示0-9、.、-

### 后续优化

1. ⏳ 移除自定义Popup（可选）
   - 删除 `QtVirtualKeyboardIntegration.qml`
   - 删除 `NumericKeyboard.qml`

2. ⏳ 统一所有输入字段
   - 所有数字输入：`Qt.ImhDigitsOnly`
   - 所有文本输入：`Qt.ImhNoPredictiveText`

---

**创建日期**: 2026-02-02
**状态**: ✅ 已完成
**编写人员**: Claude Sonnet 4.5
