# PJSIP编译日志

## ✅ 2026-02-15 20:35 - 编译错误已修复

### 问题描述

CommonControl.cpp 编译失败，两个主要错误：

1. **`m_tts` 未声明**
   - 错误位置：175, 187, 639, 873, 884, 895, 935行
   - 原因：`m_tts` 在 CommonControl.h:209 被注释掉了

2. **函数重复定义**
   - `switchTTSModel(int)` - 定义在893行和1399行
   - `getMaxSpeakerId(int)` - 定义在947行和1432行
   - `testTTS(...)` - 定义在871行和1452行

### 根本原因

代码中存在两套TTS实现：
- **旧实现**（871-968行）：使用 `m_tts`（SherpaOnnxTTS，单引擎）
- **新实现**（1399-1452行）：使用 `m_ttsEngineManager`（多引擎管理器）

### 修复方案

1. **删除旧的TTS实现**（871-968行）
2. **注释构造函数中的旧代码**（175-206行）
3. **注释playWarningOnce中的旧代码**（646-648行）

### 修复结果

✅ 编译错误已解决
✅ 保留了新的多引擎实现
✅ 添加了TODO标记，便于后续实现

### 提交信息

```
commit b95c2c6c
fix: 修复CommonControl编译错误
```
