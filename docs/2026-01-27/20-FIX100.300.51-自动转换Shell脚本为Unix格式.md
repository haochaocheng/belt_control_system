# FIX 100.300.51 - 自动转换 Shell 脚本为 Unix 格式

**日期**: 2026-01-27 20:50
**问题**: build-ubuntu24-apt.ps1 缺少自动转换 CRLF 为 LF 的逻辑
**影响**: 如果源文件是 Windows 格式，容器启动会失败

## 问题分析

### 已有的修复

✅ **编译失败不退出** (line 1145-1153)
- 编译失败时继续使用现有二进制文件
- 避免因非关键错误导致部署中断

✅ **Dockerfile 复制** (line 1430)
- 自动复制最新的 Dockerfile.ubuntu24-apt

✅ **启动脚本复制** (line 1443-1444)
- 复制 detect-audio-device.sh 和 app-entrypoint.sh

### 缺失的修复

❌ **CRLF 转换**
- 脚本复制后没有转换行尾格式
- 如果源文件是 CRLF，容器启动会失败
- 错误信息：`exec /app/app-entrypoint.sh: no such file or directory`

## 修复方案

### 修改位置

**文件**: `build-ubuntu24-apt.ps1`
**位置**: 第 1446-1460 行（复制脚本后）

### 添加的代码

```powershell
# ✅ 2026-01-27 20:50 [FIX 100.300.51] 自动转换 Shell 脚本为 Unix 格式（LF）
# 原因：Windows 环境下文件可能是 CRLF 格式，Linux 无法执行
# 效果：确保脚本在容器中可以正常执行
Write-Host "  Converting scripts to Unix format (LF)..." -ForegroundColor Yellow
$scripts = @(
    "$DockerContextDir\detect-audio-device.sh",
    "$DockerContextDir\app-entrypoint.sh"
)
foreach ($script in $scripts) {
    if (Test-Path $script) {
        $content = Get-Content $script -Raw
        $content = $content -replace "`r`n", "`n"
        [System.IO.File]::WriteAllText($script, $content, [System.Text.UTF8Encoding]::new($false))
    }
}
```

## 工作原理

1. **读取文件内容**：`Get-Content $script -Raw`
2. **替换 CRLF 为 LF**：`$content -replace "`r`n", "`n"`
3. **写回文件（UTF-8 无 BOM）**：`[System.IO.File]::WriteAllText(...)`

## 效果

- ✅ 自动检测并转换 Windows 格式文件
- ✅ 确保脚本在 Linux 容器中可执行
- ✅ 无需手动运行 dos2unix
- ✅ 每次构建自动处理

## 验证

下次运行 `.\build-ubuntu24-apt.ps1 192.168.1.8` 时：

1. 脚本会显示：`Converting scripts to Unix format (LF)...`
2. 自动转换所有 Shell 脚本
3. 容器启动不会再出现 "no such file or directory" 错误

## 相关修复

- FIX 100.300.50: 修复编译失败导致部署中断
- FIX 100.300.14: 添加音频设备自动检测脚本
- 19-FIX100.300.50-最终诊断报告.md: CRLF 问题根本原因分析

## 状态

✅ **已完成** - build-ubuntu24-apt.ps1 已更新

**修改文件**:
- build-ubuntu24-apt.ps1 (line 1446-1460)

**代码变更**: 1 个文件，15 行新增

## 下一步

现在可以运行完整的构建和部署流程：

```powershell
.\build-ubuntu24-apt.ps1 192.168.1.8
```

脚本会自动：
1. 检测源码变化
2. 交叉编译（如需要）
3. 复制启动脚本
4. **自动转换为 Unix 格式** ⭐
5. 构建 Docker 镜像
6. 部署到设备

然后处理程序输出的 QML 加载错误。
