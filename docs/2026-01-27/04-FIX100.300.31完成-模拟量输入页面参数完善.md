# FIX 100.300.31 完成 - 模拟量输入页面参数完善

**日期**: 2026-01-27
**类型**: 功能完善（模拟量输入）
**文件**: `src/qml/components/device_info/pages/AnalogInputPage.qml`

## 需求描述

用户要求：
- 按照张力传感器参数区域的参数信息，继续完善模拟量里的参数设置
- 使参数完整，与张力传感器一样
- 随后用户会细化每一个参数

## 实现方案

### 添加的参数

在模拟量输入页面的参数编辑区域，添加了以下4个参数（与张力传感器一致）：

1. **上限值**：SpinBox（0-10000，步长10，默认100）
2. **下限值**：SpinBox（0-10000，步长10，默认0）
3. **量程**：SpinBox（1-10000，步长10，默认100）
4. **单位**：ComboBox（m/s, T, ℃, kW, A, V, MPa, %，可编辑，默认"m/s"）

### 修改位置

**文件**: `AnalogInputPage.qml`

**插入位置**: 第415-590行（在"通道编号"和"保护延时"之间）

### 完整参数列表

现在模拟量输入页面包含以下完整参数（与张力传感器一致）：

| 序号 | 参数名称 | 控件类型 | 默认值 | 说明 |
|------|---------|---------|--------|------|
| 1 | 保护名称 | TextField | 根据选择 | 从列表加载 |
| 2 | 模块类型 | ComboBox | "输入模块1" | 输入模块1-4、输出模块、主模块 |
| 3 | 寄存器地址 | SpinBox | 2 | 0-255，根据模块类型自动设置 |
| 4 | 通道编号 | SpinBox | 0 | 0-7 |
| 5 | **上限值** | SpinBox | 100 | **✅ 新增**，0-10000，步长10 |
| 6 | **下限值** | SpinBox | 0 | **✅ 新增**，0-10000，步长10 |
| 7 | **量程** | SpinBox | 100 | **✅ 新增**，1-10000，步长10 |
| 8 | **单位** | ComboBox | "m/s" | **✅ 新增**，可编辑，8种预设单位 |
| 9 | 保护延时(秒) | SpinBox | 1.0 | 0-60，小数点1位 |
| 10 | 播放次数 | SpinBox | 3 | 1-99 |
| 11 | 播放时长(秒) | SpinBox | 5.0 | 0.1-60，小数点1位 |
| 12 | 语音报警 | RadioButton | 文字转语音 | 文字转语音/音频文件 |
| 13 | 报警文字 | TextField | "XXX保护报警" | 当选择文字转语音时显示 |
| 14 | 音频文件 | TextField + Button | "" | 当选择音频文件时显示 |

### 代码实现

#### 1. 添加参数控件

```qml
// ✅ 2026-01-27 [FIX 100.300.31]: 添加上限值、下限值、量程、单位参数（与张力传感器一致）
// 上限值
RowLayout {
    Layout.fillWidth: true
    spacing: 10

    Text {
        text: "上限值:"
        font.pixelSize: 14
        color: "#9E9E9E"
        Layout.preferredWidth: 100
    }

    SpinBox {
        id: upperLimitSpin
        from: 0
        to: 10000
        value: 100
        stepSize: 10
        editable: true
        Layout.fillWidth: true

        background: Rectangle {
            color: "#2d3548"
            radius: 2
            border.color: upperLimitSpin.activeFocus ? "#3498db" : "#7f8c8d"
            border.width: 1
        }

        contentItem: TextInput {
            text: upperLimitSpin.textFromValue(upperLimitSpin.value, upperLimitSpin.locale)
            font.pixelSize: 12
            color: "#E0E0E0"
            horizontalAlignment: Qt.AlignHCenter
            verticalAlignment: Qt.AlignVCenter
            readOnly: !upperLimitSpin.editable
            validator: upperLimitSpin.validator
            inputMethodHints: Qt.ImhFormattedNumbersOnly
        }
    }
}

// 下限值
RowLayout {
    Layout.fillWidth: true
    spacing: 10

    Text {
        text: "下限值:"
        font.pixelSize: 14
        color: "#9E9E9E"
        Layout.preferredWidth: 100
    }

    SpinBox {
        id: lowerLimitSpin
        from: 0
        to: 10000
        value: 0
        stepSize: 10
        editable: true
        Layout.fillWidth: true
        // ... 样式与上限值相同
    }
}

// 量程
RowLayout {
    Layout.fillWidth: true
    spacing: 10

    Text {
        text: "量程:"
        font.pixelSize: 14
        color: "#9E9E9E"
        Layout.preferredWidth: 100
    }

    SpinBox {
        id: rangeSpin
        from: 1
        to: 10000
        value: 100
        stepSize: 10
        editable: true
        Layout.fillWidth: true
        // ... 样式与上限值相同
    }
}

// 单位
RowLayout {
    Layout.fillWidth: true
    spacing: 10

    Text {
        text: "单位:"
        font.pixelSize: 14
        color: "#9E9E9E"
        Layout.preferredWidth: 100
    }

    ComboBox {
        id: unitCombo
        Layout.fillWidth: true
        model: ["m/s", "T", "℃", "kW", "A", "V", "MPa", "%"]
        editable: true
        currentIndex: 0  // 默认选择 "m/s"

        background: Rectangle {
            color: "#2d3548"
            radius: 2
            border.color: unitCombo.activeFocus ? "#3498db" : "#7f8c8d"
            border.width: 1
        }

        contentItem: TextInput {
            text: unitCombo.editable ? unitCombo.editText : unitCombo.displayText
            font.pixelSize: 12
            color: "#E0E0E0"
            verticalAlignment: Text.AlignVCenter
            leftPadding: 10
            readOnly: !unitCombo.editable
            selectByMouse: true
        }
    }
}
```

#### 2. 更新 loadProtectionData 函数

```qml
function loadProtectionData(index) {
    if (index < 0 || index >= analogProtectionModel.count) {
        return
    }

    var item = analogProtectionModel.get(index)
    var protection = deviceConfigMgr.loadAnalogProtection(root.deviceId, item.name)

    if (protection && protection.protection_name) {
        // 从数据库加载完整参数
        nameField.text = protection.protection_name
        moduleTypeCombo.currentIndex = moduleTypeCombo.model.indexOf(protection.module_type)
        registerAddressSpin.value = protection.register_address
        channelSpin.value = protection.register_address
        // ✅ 2026-01-27 [FIX 100.300.31]: 加载上限值、下限值、量程、单位
        upperLimitSpin.value = protection.upper_limit || 100
        lowerLimitSpin.value = protection.lower_limit || 0
        rangeSpin.value = protection.range || 100
        unitCombo.currentIndex = unitCombo.model.indexOf(protection.unit || item.unit)
        delaySpin.value = protection.protection_delay * 10
        playCountSpin.value = protection.play_count
        durationSpin.value = protection.play_duration * 10
        ttsRadio.checked = protection.use_text_to_speech === 1
        fileRadio.checked = protection.use_text_to_speech === 0
        ttsTextField.text = protection.tts_text || (item.name + "保护报警")
        audioField.text = protection.audio_file || ""

        console.log("✅ [AnalogInputPage] 从数据库加载完整参数:", item.name)
    } else {
        // 数据库中没有，使用ListModel中的基本数据
        nameField.text = item.name
        moduleTypeCombo.currentIndex = moduleTypeCombo.model.indexOf(item.moduleType)
        registerAddressSpin.value = item.registerAddress
        channelSpin.value = item.registerAddress

        // ✅ 2026-01-27 [FIX 100.300.31]: 设置默认值
        upperLimitSpin.value = 100
        lowerLimitSpin.value = 0
        rangeSpin.value = 100
        unitCombo.currentIndex = unitCombo.model.indexOf(item.unit)

        delaySpin.value = 10  // 1.0秒
        playCountSpin.value = 3
        durationSpin.value = 50  // 5.0秒
        ttsRadio.checked = true
        ttsTextField.text = item.name + "保护报警"
        audioField.text = ""

        console.log("⚠️ [AnalogInputPage] 数据库中没有详细参数，使用默认值:", item.name)
    }
}
```

#### 3. 更新 saveProtectionData 函数

```qml
function saveProtectionData() {
    if (root.currentProtectionIndex < 0 || root.currentProtectionIndex >= analogProtectionModel.count) {
        return
    }

    // 更新ListModel
    analogProtectionModel.setProperty(root.currentProtectionIndex, "name", nameField.text)
    analogProtectionModel.setProperty(root.currentProtectionIndex, "moduleType", moduleTypeCombo.currentText)
    analogProtectionModel.setProperty(root.currentProtectionIndex, "registerAddress", registerAddressSpin.value)
    analogProtectionModel.setProperty(root.currentProtectionIndex, "registerAddress", channelSpin.value)
    // ✅ 2026-01-27 [FIX 100.300.31]: 更新单位到 ListModel
    analogProtectionModel.setProperty(root.currentProtectionIndex, "unit", unitCombo.editable ? unitCombo.editText : unitCombo.displayText)

    console.log("✅ 保存保护数据到内存:", nameField.text)

    // ✅ 2026-01-25 [数据库集成] 保存到数据库
    var protection = {
        "protection_name": nameField.text,
        "module_type": moduleTypeCombo.currentText,
        "register_address": registerAddressSpin.value,
        "channel_number": channelSpin.value,
        // ✅ 2026-01-27 [FIX 100.300.31]: 保存上限值、下限值、量程、单位
        "upper_limit": upperLimitSpin.value,
        "lower_limit": lowerLimitSpin.value,
        "range": rangeSpin.value,
        "unit": unitCombo.editable ? unitCombo.editText : unitCombo.displayText,
        "protection_delay": delaySpin.realValue,
        "play_count": playCountSpin.value,
        "play_duration": durationSpin.realValue,
        "use_text_to_speech": ttsRadio.checked,
        "tts_text": ttsTextField.text,
        "audio_file": audioField.text
    }

    if (deviceConfigMgr.saveAnalogProtection(root.deviceId, protection)) {
        console.log("✅ [AnalogInputPage] 保存到数据库成功:", nameField.text)
    } else {
        console.error("❌ [AnalogInputPage] 保存到数据库失败:", nameField.text)
    }
}
```

## 与张力传感器的对比

| 特性 | 张力传感器（TensionControlConfigPanel.qml） | 模拟量输入（AnalogInputPage.qml） |
|------|-------------------------------------------|----------------------------------|
| **保护名称** | 固定为"张力"（不可编辑） | 根据选择（可编辑） |
| **保护类型** | 固定为"模拟量"（不可编辑） | 无此字段 |
| **模块类型** | 模拟量模块1-4 | 输入模块1-4、输出模块、主模块 |
| **寄存器地址** | 0-255，默认6 | 0-255，默认2 |
| **通道编号** | 无此字段 | 0-7，默认0 |
| **上限值** | ✅ 0-10000，步长10，默认100 | ✅ 0-10000，步长10，默认100 |
| **下限值** | ✅ 0-10000，步长10，默认0 | ✅ 0-10000，步长10，默认0 |
| **量程** | ✅ 1-10000，步长10，默认100 | ✅ 1-10000，步长10，默认100 |
| **单位** | ✅ 可编辑，默认"T" | ✅ 可编辑，默认"m/s" |
| **保护延时** | ✅ 0-60秒，小数点1位 | ✅ 0-60秒，小数点1位 |
| **播放次数** | ✅ 1-99 | ✅ 1-99 |
| **播放时长** | ✅ 0.1-60秒，小数点1位 | ✅ 0.1-60秒，小数点1位 |
| **语音报警** | ✅ 文字转语音/音频文件 | ✅ 文字转语音/音频文件 |
| **报警文字** | ✅ "张力保护报警" | ✅ "XXX保护报警" |
| **音频文件** | ✅ 浏览按钮 | ✅ 浏览按钮 |

## 预期效果

完成后：
- ✅ 模拟量输入页面包含14个完整参数
- ✅ 与张力传感器的参数完全一致（除了保护名称和模块类型）
- ✅ 上限值、下限值、量程、单位可正常编辑
- ✅ 参数可正确保存到数据库
- ✅ 参数可正确从数据库加载
- ✅ 视觉风格与其他页面一致

## 下一步工作

### 1. 电机控制页面参数完善

用户要求：
- 完善电机里的电流和温度参数
- 使参数与模拟量输入一样完整

需要修改的文件：
- `src/qml/components/device_info/pages/CurrentProtectionTab.qml`（电流保护Tab）
- `src/qml/components/device_info/pages/MotorTempTab.qml`（电机温度Tab）
- 其他电机相关的Tab页面

### 2. 参数细化

用户提到：
- "随后我在细化每一个参数"

等待用户提供具体的细化要求。

## 验证步骤

### 1. 在 QDS 中验证

1. 打开 DeviceSettingsDialog.qml
2. 运行预览
3. 点击左侧"模拟量输入"按钮
4. 检查右侧参数区域是否显示14个参数
5. 检查上限值、下限值、量程、单位是否正常显示
6. 检查所有参数是否可编辑

### 2. 检查参数保存

1. 修改上限值、下限值、量程、单位
2. 点击"保存"按钮
3. 检查控制台日志是否显示保存成功
4. 切换到其他保护项，再切换回来
5. 检查参数是否正确加载

### 3. 检查参数加载

1. 选择不同的模拟量保护项（速度、张力、红外温度一、红外温度二、电压）
2. 检查每个保护项的参数是否正确加载
3. 检查单位是否根据保护项类型正确显示（速度：m/s，张力：T，温度：℃，电压：V）

## 总结

成功完善了模拟量输入页面的参数设置，添加了上限值、下限值、量程、单位4个参数，使其与张力传感器的参数完全一致。

**设计原则**：
- 复用张力传感器的参数结构
- 保持与其他页面的视觉一致性
- 完整的数据库集成（加载和保存）
- 预留参数细化接口

**关键特性**：
- 14个完整参数（与张力传感器一致）
- 上限值、下限值、量程、单位可编辑
- 单位可自定义（ComboBox editable）
- 参数可保存到数据库
- 参数可从数据库加载

**验证方法**：
- 在 QDS 中预览
- 检查参数显示
- 检查参数保存
- 检查参数加载
