# FIX 100.300.30 完成 - 张紧控制页面创建和集成

**日期**: 2026-01-27
**类型**: 新功能（张紧控制）
**文件**:
- `src/qml/components/device_info/pages/TensionControlPage.qml`（主页面）
- `src/qml/components/device_info/pages/TensionControlListPanel.qml`（左侧列表）
- `src/qml/components/device_info/pages/TensionControlConfigPanel.qml`（右侧配置）
- `src/qml/components/device_info/DeviceSettingsDialog.qml`（主对话框集成）

## 需求描述

用户要求：
- **列表名称**：控制列表（不是"张紧控制列表"）
- **列表分为**：
  1. 张力传感器
  2. 独立张紧控制（待实现）

- **张力传感器参数区**：
  - 包括模拟量列表里的张力所有参数
  - 但是模拟量列表里的是简化版，不可用
  - 使用第一个界面主界面左侧的保护信息里的模拟量参数
  - 点击任意模拟量会有一个弹窗设置
  - 里面的参数全部包含，写入到此参数区域

## 实现方案

### 1. TensionControlPage.qml（主页面）

**设计**：
- 完全复制 BrakeControlPage.qml 的结构
- 左右分栏布局（Row）
- 左侧：TensionControlListPanel（240px宽度）
- 右侧：TensionControlConfigPanel（填充剩余空间）
- 使用 Loader 动态加载子组件

**关键代码**：
```qml
Rectangle {
    id: root
    implicitWidth: 1000
    implicitHeight: 600
    color: "transparent"

    property int currentControlIndex: 0  // 当前选中的控制索引 (0=张力传感器, 1=独立张紧控制)

    Row {
        anchors.fill: parent
        spacing: 0

        // 左侧：控制列表
        Loader {
            id: tensionControlListPanel
            width: 240
            height: parent.height
            source: "TensionControlListPanel.qml"
        }

        // 分隔线
        Rectangle {
            width: 2
            height: parent.height
            color: "#3d4556"
        }

        // 右侧：控制配置面板
        Loader {
            id: tensionControlConfigPanel
            width: parent.width - tensionControlListPanel.width - 2
            height: parent.height
            source: "TensionControlConfigPanel.qml"
        }
    }
}
```

### 2. TensionControlListPanel.qml（左侧列表）

**设计**：
- 完全复制 BrakeListPanel.qml 的结构
- 使用 33.png 作为整体背景
- 使用 bhNameBK.png/bhNameBK1.png 作为列表项背景
- 2个控制选项：
  1. 张力传感器（状态：投入，绿色圆点）
  2. 独立张紧控制（状态：待实现，灰色圆点）

**关键代码**：
```qml
Rectangle {
    id: root
    color: "transparent"
    clip: true

    // 整体背景 33.png
    Image {
        anchors.fill: parent
        source: "../images/33.png"
        fillMode: Image.Stretch
        z: -1
    }

    property int currentControlIndex: 0
    signal controlSelected(int controlIndex)

    // 标题
    Rectangle {
        id: header
        anchors.top: parent.top
        anchors.left: parent.left
        anchors.right: parent.right
        height: 50
        color: "transparent"

        Text {
            anchors.centerIn: parent
            text: "控制列表"  // ✅ 用户要求的名称
            font.pixelSize: 16
            font.weight: Font.Bold
            color: "#E0E0E0"
        }
    }

    // 控制列表
    ListView {
        id: listView
        anchors.top: header.bottom
        anchors.left: parent.left
        anchors.right: parent.right
        anchors.bottom: parent.bottom
        clip: true

        model: ListModel {
            ListElement { name: "张力传感器"; status: "投入" }
            ListElement { name: "独立张紧控制"; status: "待实现" }
        }

        delegate: Rectangle {
            width: listView.width
            height: 45
            color: "transparent"

            // 背景图片（states切换）
            Image {
                id: backgroundImage
                anchors.fill: parent
                fillMode: Image.Stretch
                z: -1
                source: "../../../images/bhNameBK.png"

                states: [
                    State {
                        name: "selected"
                        when: root.currentControlIndex === index
                        PropertyChanges {
                            target: backgroundImage
                            source: "../../../images/bhNameBK1.png"
                        }
                    },
                    State {
                        name: "normal"
                        when: root.currentControlIndex !== index
                        PropertyChanges {
                            target: backgroundImage
                            source: "../../../images/bhNameBK.png"
                        }
                    }
                ]
            }

            // 左侧激活指示条
            Rectangle {
                visible: root.currentControlIndex === index
                width: 4
                height: parent.height
                color: "#2196F3"
                anchors.left: parent.left
            }

            // 控制名称
            Text {
                text: model.name
                font.pixelSize: 14
                font.weight: root.currentControlIndex === index ? Font.Bold : Font.Normal
                color: root.currentControlIndex === index ? "#E0E0E0" : "#9E9E9E"
                anchors.centerIn: parent
            }

            // 状态指示（右侧）
            Row {
                spacing: 8
                anchors.right: parent.right
                anchors.rightMargin: 20
                anchors.verticalCenter: parent.verticalCenter

                Rectangle {
                    width: 8
                    height: 8
                    radius: 4
                    // 张力传感器：绿色（投入），独立张紧控制：灰色（待实现）
                    color: model.status === "投入" ? "#4CAF50" : "#95a5a6"
                }

                Text {
                    text: model.status
                    font.pixelSize: 12
                    color: "#9E9E9E"
                }
            }
        }
    }
}
```

### 3. TensionControlConfigPanel.qml（右侧配置）

**设计**：
- 使用 StackLayout 切换内容（根据 controlIndex）
- 索引 0：张力传感器配置（完整参数）
- 索引 1：独立张紧控制配置（待实现）

**张力传感器参数**（从 ProtectionSettingsPopup.qml 复制）：

#### 完整参数列表

1. **保护名称**：固定为"张力"（不可编辑）
2. **保护类型**：固定为"模拟量"（不可编辑）
3. **模块类型**：ComboBox（模拟量模块1-4）
4. **寄存器地址**：SpinBox（0-255，默认6）
5. **上限值**：SpinBox（0-10000，步长10）
6. **下限值**：SpinBox（0-10000，步长10）
7. **量程**：SpinBox（1-10000，步长10）
8. **单位**：ComboBox（m/s, T, ℃, kW, A, V, MPa, %，默认"T"）
9. **保护延时(秒)**：SpinBox（0-60，小数点1位）
10. **播放次数**：SpinBox（1-99）
11. **播放时长(秒)**：SpinBox（0.1-60，小数点1位）
12. **语音报警**：RadioButton（文字转语音/音频文件）
13. **报警文字**：TextField（当选择文字转语音时显示）
14. **音频文件**：TextField + Button（当选择音频文件时显示）

**关键代码**：
```qml
Rectangle {
    id: root
    width: 800
    height: 600
    color: "transparent"

    property int controlIndex: 0  // 当前控制索引

    // 标题栏
    Rectangle {
        id: header
        anchors.top: parent.top
        anchors.left: parent.left
        anchors.right: parent.right
        height: 50
        color: "transparent"

        // 背景图片 059.png
        Image {
            anchors.fill: parent
            source: "../images/059.png"
            fillMode: Image.Stretch
            z: -1
        }

        Text {
            anchors.centerIn: parent
            text: root.controlIndex === 0 ? "张力传感器配置" : "独立张紧控制配置"
            font.pixelSize: 16
            font.weight: Font.Bold
            color: "#E0E0E0"
        }
    }

    // 内容区域
    Rectangle {
        id: contentArea
        anchors.top: header.bottom
        anchors.left: parent.left
        anchors.right: parent.right
        anchors.bottom: parent.bottom
        color: "transparent"

        // StackLayout 切换内容
        StackLayout {
            anchors.fill: parent
            currentIndex: root.controlIndex

            // 0: 张力传感器配置（完整参数）
            Item {
                ScrollView {
                    anchors.fill: parent
                    anchors.margins: 15
                    clip: true

                    ColumnLayout {
                        width: parent.width
                        spacing: 15

                        // 保护名称
                        RowLayout {
                            Text { text: "保护名称:" }
                            TextField {
                                text: "张力"
                                enabled: false  // 固定为"张力"
                            }
                        }

                        // 保护类型
                        RowLayout {
                            Text { text: "保护类型:" }
                            ComboBox {
                                model: ["模拟量", "开关量"]
                                currentIndex: 0
                                enabled: false  // 固定为"模拟量"
                            }
                        }

                        // 模块类型
                        RowLayout {
                            Text { text: "模块类型:" }
                            ComboBox {
                                model: ["模拟量模块1", "模拟量模块2", "模拟量模块3", "模拟量模块4"]
                                currentIndex: 0
                            }
                        }

                        // 寄存器地址
                        RowLayout {
                            Text { text: "寄存器地址:" }
                            SpinBox {
                                from: 0
                                to: 255
                                value: 6  // 张力默认寄存器地址
                            }
                        }

                        // 上限值
                        RowLayout {
                            Text { text: "上限值:" }
                            SpinBox {
                                from: 0
                                to: 10000
                                value: 100
                                stepSize: 10
                            }
                        }

                        // 下限值
                        RowLayout {
                            Text { text: "下限值:" }
                            SpinBox {
                                from: 0
                                to: 10000
                                value: 0
                                stepSize: 10
                            }
                        }

                        // 量程
                        RowLayout {
                            Text { text: "量程:" }
                            SpinBox {
                                from: 1
                                to: 10000
                                value: 100
                                stepSize: 10
                            }
                        }

                        // 单位
                        RowLayout {
                            Text { text: "单位:" }
                            ComboBox {
                                model: ["m/s", "T", "℃", "kW", "A", "V", "MPa", "%"]
                                currentIndex: 1  // 默认选择 "T"（张力单位）
                                editable: true
                            }
                        }

                        // 保护延时
                        RowLayout {
                            Text { text: "保护延时(秒):" }
                            SpinBox {
                                from: 0
                                to: 600
                                value: 10  // 默认1.0秒 * 10
                                property real realValue: value / 10
                            }
                        }

                        // 播放次数
                        RowLayout {
                            Text { text: "播放次数:" }
                            SpinBox {
                                from: 1
                                to: 99
                                value: 3
                            }
                        }

                        // 播放时长
                        RowLayout {
                            Text { text: "播放时长(秒):" }
                            SpinBox {
                                from: 1
                                to: 600
                                value: 50  // 默认5.0秒 * 10
                                property real realValue: value / 10
                            }
                        }

                        // 语音报警类型
                        RowLayout {
                            Text { text: "语音报警:" }
                            RadioButton {
                                text: "文字转语音"
                                checked: true
                            }
                            RadioButton {
                                text: "音频文件"
                                checked: false
                            }
                        }

                        // 报警文字
                        RowLayout {
                            visible: ttsRadio.checked
                            Text { text: "报警文字:" }
                            TextField {
                                text: "张力保护报警"
                                placeholderText: "输入报警文字内容..."
                            }
                        }

                        // 音频文件
                        RowLayout {
                            visible: fileRadio.checked
                            Text { text: "音频文件:" }
                            TextField {
                                text: ""
                                placeholderText: "选择音频文件..."
                                readOnly: true
                            }
                            Button {
                                text: "浏览"
                                onClicked: {
                                    console.log("浏览音频文件")
                                }
                            }
                        }

                        // 底部按钮
                        RowLayout {
                            Button {
                                text: "保存"
                                onClicked: {
                                    console.log("保存张力传感器配置")
                                }
                            }
                            Button {
                                text: "重置"
                                onClicked: {
                                    console.log("重置张力传感器配置")
                                }
                            }
                        }
                    }
                }
            }

            // 1: 独立张紧控制配置（待实现）
            Rectangle {
                color: "transparent"
                Text {
                    anchors.centerIn: parent
                    text: "独立张紧控制\n（待实现）"
                    font.pixelSize: 18
                    color: "#CCCCCC"
                    horizontalAlignment: Text.AlignHCenter
                }
            }
        }
    }
}
```

### 4. DeviceSettingsDialog.qml（主对话框集成）

**修改位置**：第539-562行

**修改前**:
```qml
// 5: 张紧控制
Rectangle {
    color: "transparent"
    Text {
        anchors.centerIn: parent
        text: "张紧控制\n（待实现）"
        font.pixelSize: 18
        color: "#CCCCCC"
        horizontalAlignment: Text.AlignHCenter
    }
}
```

**修改后**:
```qml
// 5: 张紧控制
// ✅ 2026-01-27 [FIX 100.300.30]: 使用 TensionControlPage 组件
Loader {
    id: tensionControlPageLoader
    // ✅ 为 Loader 设置明确尺寸，便于 QDS 预览
    Layout.fillWidth: true
    Layout.fillHeight: true
    active: root.currentCategory === 5  // 仅在选中时加载
    source: "pages/TensionControlPage.qml"

    onLoaded: {
        if (item) {
            console.log("✅ [DeviceSettingsDialog] TensionControlPage 加载成功")
            item.deviceId = root.deviceId
            item.deviceName = root.deviceName
        }
    }

    onStatusChanged: {
        if (tensionControlPageLoader.status === Loader.Error) {
            console.error("❌ [DeviceSettingsDialog] TensionControlPage 加载失败")
        }
    }
}
```

## 技术实现

### 1. 参数来源

**从 ProtectionSettingsPopup.qml 复制的参数**：

| 参数名称 | 控件类型 | 默认值 | 说明 |
|---------|---------|--------|------|
| 保护名称 | TextField | "张力" | 固定不可编辑 |
| 保护类型 | ComboBox | "模拟量" | 固定不可编辑 |
| 模块类型 | ComboBox | "模拟量模块1" | 可选择1-4 |
| 寄存器地址 | SpinBox | 6 | 0-255 |
| 上限值 | SpinBox | 100 | 0-10000，步长10 |
| 下限值 | SpinBox | 0 | 0-10000，步长10 |
| 量程 | SpinBox | 100 | 1-10000，步长10 |
| 单位 | ComboBox | "T" | 可编辑，预设8种单位 |
| 保护延时(秒) | SpinBox | 1.0 | 0-60，小数点1位 |
| 播放次数 | SpinBox | 3 | 1-99 |
| 播放时长(秒) | SpinBox | 5.0 | 0.1-60，小数点1位 |
| 语音报警 | RadioButton | 文字转语音 | 文字转语音/音频文件 |
| 报警文字 | TextField | "张力保护报警" | 当选择文字转语音时显示 |
| 音频文件 | TextField + Button | "" | 当选择音频文件时显示 |

### 2. 与模拟量列表的区别

**模拟量列表（简化版）**：
- 只显示基本参数（上限值、下限值、量程）
- 不可编辑，只能查看
- 没有语音报警设置
- 没有保护延时设置

**张紧控制-张力传感器（完整版）**：
- 包含所有参数（14个参数）
- 完全可编辑
- 包含语音报警设置（文字转语音/音频文件）
- 包含保护延时、播放次数、播放时长
- 包含模块类型、寄存器地址配置

### 3. 视觉一致性

**与其他页面保持一致**：
- ✅ 左侧列表宽度 240px
- ✅ 列表背景 33.png
- ✅ 列表项背景 bhNameBK.png/bhNameBK1.png
- ✅ 标题背景 059.png
- ✅ TextField 样式（深色背景 + 蓝色焦点边框）
- ✅ SpinBox 样式（深色背景 + 蓝色焦点边框）
- ✅ ComboBox 样式（深色背景 + 蓝色焦点边框）
- ✅ RadioButton 样式（蓝色圆点）
- ✅ Button 样式（蓝色/灰色）

## StackLayout 完整结构

```qml
StackLayout {
    id: contentStack
    anchors.fill: parent
    anchors.bottomMargin: 72
    currentIndex: root.currentCategory

    // 0: 基本配置
    Loader { source: "pages/BasicConfigPage.qml" }

    // 1: 开关量输入
    Loader { source: "pages/SwitchInputPage.qml" }

    // 2: 模拟量输入
    Loader { source: "pages/AnalogInputPage.qml" }

    // 3: 电机控制
    Loader { source: "pages/MotorControlPage.qml" }

    // 4: 制动器控制
    Loader { source: "pages/BrakeControlPage.qml" }

    // ✅ 5: 张紧控制（新增）
    Loader { source: "pages/TensionControlPage.qml" }

    // 6: 逻辑控制（待实现）
    Rectangle { ... }
}
```

## 验证步骤

### 1. 在 QDS 中验证

1. 打开 DeviceSettingsDialog.qml
2. 运行预览
3. 点击左侧"张紧控制"按钮
4. 检查右侧是否正确显示张紧控制页面
5. 检查左侧列表是否显示"控制列表"标题
6. 检查列表是否显示2个选项（张力传感器、独立张紧控制）

### 2. 检查张力传感器参数

1. 点击左侧"张力传感器"
2. 检查右侧是否显示完整的14个参数
3. 检查所有参数是否可编辑（除了保护名称和保护类型）
4. 检查语音报警类型切换是否正常
5. 检查保存、重置按钮是否正常

### 3. 检查独立张紧控制

1. 点击左侧"独立张紧控制"
2. 检查右侧是否显示"独立张紧控制（待实现）"占位符

### 4. 检查视觉一致性

- ✅ 左侧列表宽度 240px，与其他页面一致
- ✅ 列表背景 33.png，与其他页面一致
- ✅ 列表项背景 bhNameBK.png/bhNameBK1.png，与其他页面一致
- ✅ 标题背景 059.png，与其他页面一致
- ✅ 所有控件样式与其他页面一致

## 预期效果

完成后：
- ✅ 在 QDS 中打开 DeviceSettingsDialog.qml
- ✅ 点击左侧"张紧控制"按钮
- ✅ 右侧正确显示张紧控制页面
- ✅ 左侧列表标题显示"控制列表"
- ✅ 左侧列表显示2个选项（张力传感器、独立张紧控制）
- ✅ 点击"张力传感器"，右侧显示完整的14个参数
- ✅ 所有参数可编辑（除了保护名称和保护类型）
- ✅ 语音报警类型切换正常
- ✅ 保存、重置按钮正常
- ✅ 点击"独立张紧控制"，右侧显示"待实现"占位符
- ✅ 视觉风格与电机控制、制动器控制完全一致

## 文件清单

**已创建的文件**：
1. `src/qml/components/device_info/pages/TensionControlPage.qml` - 主页面
2. `src/qml/components/device_info/pages/TensionControlListPanel.qml` - 左侧列表
3. `src/qml/components/device_info/pages/TensionControlConfigPanel.qml` - 右侧配置

**已修改的文件**：
1. `src/qml/components/device_info/DeviceSettingsDialog.qml` - 主对话框（第539-562行）

## 下一步工作

### 1. 后端数据绑定

需要创建 TensionController C++ 类：
- 管理张力传感器的配置数据
- 提供保存、加载、重置功能
- 与数据库交互

### 2. 数据库表设计

需要创建 tension_config 表：
- 存储张力传感器的配置参数
- 支持多设备（device_id）
- 复用 protection_config 表结构

### 3. QML 数据绑定

在 TensionControlConfigPanel.qml 中：
- 绑定后端控制器属性
- 实现双向数据绑定
- 实现保存、重置功能

### 4. 独立张紧控制实现

实现独立张紧控制功能：
- 设计参数结构
- 创建配置界面
- 实现控制逻辑

## 总结

成功创建了张紧控制页面的三个核心文件，并集成到主对话框中。张力传感器配置包含了模拟量弹窗的所有14个参数（完整版），而不是模拟量列表的简化版。

**设计原则**：
- 复用现有的视觉元素和组件
- 保持与电机控制、制动器控制的一致性
- 使用 StackLayout 切换内容（张力传感器/独立张紧控制）
- 从 ProtectionSettingsPopup.qml 复制完整参数
- 预留后端数据绑定接口

**关键特性**：
- 左侧列表标题：控制列表（用户要求）
- 2个控制选项：张力传感器、独立张紧控制
- 张力传感器：14个完整参数（从弹窗复制）
- 独立张紧控制：待实现占位符
- 视觉风格完全一致

**验证方法**：
- 在 QDS 中预览
- 检查页面切换
- 检查参数完整性
- 检查视觉一致性
