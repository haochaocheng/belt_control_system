# FIX 100.300.32 完成 - 创建自定义输入框组件并替换模拟量输入页面

**日期**: 2026-01-27
**类型**: UI组件优化
**文件**:
- `src/qml/components/device_info/CustomTextField.qml`（新建）
- `src/qml/components/device_info/pages/AnalogInputPage.qml`（修改）

## 需求描述

用户要求：
- 设备参数弹窗里所有使用的输入框要修改
- 利用 034.png 作为背景图片
- 新建输入框组件
- 用这新组件代替所有的输入框
- **注意**：只是输入框（TextField），不包括 ComboBox 和 SpinBox

## 实现方案

### 1. 创建 CustomTextField.qml 组件

**文件位置**: `src/qml/components/device_info/CustomTextField.qml`

**设计特点**:
- 继承 TextField，保留所有原生属性和方法
- 使用 034.png 作为背景图片
- 焦点时显示蓝色边框（增强视觉效果）
- 统一的内边距和字体样式
- 支持鼠标选择文本

**完整代码**:
```qml
import QtQuick 2.15
import QtQuick.Controls 2.15

// ✅ 2026-01-27 [FIX 100.300.32]: 自定义输入框组件，使用 034.png 作为背景
// 用于替换设备参数弹窗里所有的 TextField
TextField {
    id: root

    // ========== 公开属性 ==========
    // 继承 TextField 的所有属性，可以直接使用：
    // - text: 文本内容
    // - placeholderText: 占位符文本
    // - enabled: 是否启用
    // - readOnly: 是否只读
    // - validator: 验证器
    // - inputMethodHints: 输入法提示
    // - font: 字体
    // - color: 文字颜色
    // 等等...

    // ========== 默认样式 ==========
    font.pixelSize: 14
    color: "#E0E0E0"
    selectByMouse: true  // 允许鼠标选择文本
    selectedTextColor: "#FFFFFF"
    selectionColor: "#2196F3"

    // ========== 背景：使用 034.png 图片 ==========
    background: Rectangle {
        color: "transparent"  // 透明，显示图片

        // 背景图片 034.png
        Image {
            anchors.fill: parent
            source: "../images/034.png"
            fillMode: Image.Stretch  // 拉伸填充
            z: -1  // 放在最底层
        }

        // 焦点边框（可选，增强视觉效果）
        Rectangle {
            anchors.fill: parent
            color: "transparent"
            border.color: root.activeFocus ? "#2196F3" : "transparent"
            border.width: root.activeFocus ? 2 : 0
            radius: 4
        }
    }

    // ========== 内边距 ==========
    leftPadding: 12
    rightPadding: 12
    topPadding: 8
    bottomPadding: 8
}
```

### 2. 在 AnalogInputPage.qml 中使用

**修改步骤**:

#### Step 1: 导入自定义组件

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15
import QtQuick.Layouts 1.15
import ".." as DeviceInfo  // ✅ 2026-01-27 [FIX 100.300.32]: 导入自定义组件
```

#### Step 2: 替换保护名称的 TextField

**修改前**:
```qml
TextField {
    id: nameField
    Layout.fillWidth: true
    font.pixelSize: 12

    background: Rectangle {
        color: "#2d3548"
        radius: 2
        border.color: nameField.activeFocus ? "#3498db" : "#7f8c8d"
        border.width: 1
    }

    color: "#ecf0f1"
}
```

**修改后**:
```qml
// ✅ 2026-01-27 [FIX 100.300.32]: 使用自定义 TextField（034.png 背景）
DeviceInfo.CustomTextField {
    id: nameField
    Layout.fillWidth: true
}
```

#### Step 3: 替换报警文字的 TextField

**修改前**:
```qml
TextField {
    id: ttsTextField
    placeholderText: "输入报警文字内容..."
    Layout.fillWidth: true
    font.pixelSize: 12

    background: Rectangle {
        color: "#2d3548"
        radius: 2
        border.color: ttsTextField.activeFocus ? "#3498db" : "#7f8c8d"
        border.width: 1
    }

    color: "#ecf0f1"
}
```

**修改后**:
```qml
// ✅ 2026-01-27 [FIX 100.300.32]: 使用自定义 TextField（034.png 背景）
DeviceInfo.CustomTextField {
    id: ttsTextField
    placeholderText: "输入报警文字内容..."
    Layout.fillWidth: true
}
```

#### Step 4: 替换音频文件的 TextField

**修改前**:
```qml
TextField {
    id: audioField
    placeholderText: "选择音频文件..."
    Layout.fillWidth: true
    readOnly: true
    font.pixelSize: 12

    background: Rectangle {
        color: "#2d3548"
        radius: 2
        border.color: "#3d4556"
        border.width: 1
    }

    color: "#ecf0f1"
}
```

**修改后**:
```qml
// ✅ 2026-01-27 [FIX 100.300.32]: 使用自定义 TextField（034.png 背景）
DeviceInfo.CustomTextField {
    id: audioField
    placeholderText: "选择音频文件..."
    Layout.fillWidth: true
    readOnly: true
}
```

## 技术实现

### 1. 组件继承

CustomTextField 直接继承 TextField，保留所有原生属性和方法：
- ✅ text: 文本内容
- ✅ placeholderText: 占位符文本
- ✅ enabled: 是否启用
- ✅ readOnly: 是否只读
- ✅ validator: 验证器
- ✅ inputMethodHints: 输入法提示
- ✅ font: 字体
- ✅ color: 文字颜色
- ✅ selectByMouse: 鼠标选择
- ✅ selectedTextColor: 选中文字颜色
- ✅ selectionColor: 选中背景颜色

### 2. 背景图片

使用 034.png 作为背景：
```qml
background: Rectangle {
    color: "transparent"  // 透明，显示图片

    // 背景图片 034.png
    Image {
        anchors.fill: parent
        source: "../images/034.png"
        fillMode: Image.Stretch  // 拉伸填充
        z: -1  // 放在最底层
    }

    // 焦点边框（可选，增强视觉效果）
    Rectangle {
        anchors.fill: parent
        color: "transparent"
        border.color: root.activeFocus ? "#2196F3" : "transparent"
        border.width: root.activeFocus ? 2 : 0
        radius: 4
    }
}
```

### 3. 代码简化

**替换前**（每个 TextField 需要 15-20 行代码）:
```qml
TextField {
    id: nameField
    Layout.fillWidth: true
    font.pixelSize: 12

    background: Rectangle {
        color: "#2d3548"
        radius: 2
        border.color: nameField.activeFocus ? "#3498db" : "#7f8c8d"
        border.width: 1
    }

    color: "#ecf0f1"
}
```

**替换后**（只需要 3-5 行代码）:
```qml
DeviceInfo.CustomTextField {
    id: nameField
    Layout.fillWidth: true
}
```

**代码减少**: 约 70-80%

## 已替换的页面

### 1. AnalogInputPage.qml（模拟量输入页面）

已替换 3 个 TextField：
- ✅ 保护名称（nameField）
- ✅ 报警文字（ttsTextField）
- ✅ 音频文件（audioField）

## 待替换的页面

### 1. 开关量输入页面
- `src/qml/components/device_info/pages/SwitchInputPage.qml`

### 2. 电机控制页面
- `src/qml/components/device_info/pages/BasicConfigTab.qml`
- `src/qml/components/device_info/pages/CurrentProtectionTab.qml`
- `src/qml/components/device_info/pages/FrontBearingTempTab.qml`
- `src/qml/components/device_info/pages/RearBearingTempTab.qml`
- `src/qml/components/device_info/pages/PhaseAWindingTab.qml`
- `src/qml/components/device_info/pages/PhaseBWindingTab.qml`
- `src/qml/components/device_info/pages/PhaseCWindingTab.qml`
- `src/qml/components/device_info/pages/MotorTempTab.qml`
- `src/qml/components/device_info/pages/XAxisVibrationTab.qml`
- `src/qml/components/device_info/pages/YAxisVibrationTab.qml`

### 3. 制动器控制页面
- `src/qml/components/device_info/pages/BrakeConfigPanel.qml`

### 4. 张紧控制页面
- `src/qml/components/device_info/pages/TensionControlConfigPanel.qml`

### 5. 基本配置页面
- `src/qml/components/device_info/pages/BasicConfigPage.qml`

## 预期效果

完成后：
- ✅ 所有输入框使用 034.png 作为背景
- ✅ 视觉风格统一
- ✅ 代码大幅简化（减少 70-80%）
- ✅ 易于维护（修改 CustomTextField.qml 即可全局生效）
- ✅ 焦点时显示蓝色边框
- ✅ 支持鼠标选择文本

## 验证步骤

### 1. 在 QDS 中验证

1. 打开 DeviceSettingsDialog.qml
2. 运行预览
3. 点击左侧"模拟量输入"按钮
4. 检查右侧参数区域的输入框是否使用 034.png 背景
5. 点击输入框，检查焦点边框是否显示
6. 输入文字，检查文字颜色和字体是否正确

### 2. 检查功能

1. 在保护名称输入框中输入文字
2. 在报警文字输入框中输入文字
3. 检查音频文件输入框是否只读
4. 检查所有输入框是否可以正常输入和编辑

### 3. 检查视觉效果

1. 检查 034.png 背景是否正确显示
2. 检查焦点边框是否正确显示（蓝色，2px）
3. 检查文字颜色是否正确（#E0E0E0）
4. 检查选中文字的颜色是否正确（白色文字，蓝色背景）

## 下一步工作

### 1. 批量替换其他页面

需要替换以下页面的所有 TextField：
- 开关量输入页面
- 电机控制页面（10个Tab）
- 制动器控制页面
- 张紧控制页面
- 基本配置页面

### 2. 创建 CustomSpinBox 和 CustomComboBox

用户提到：
- "SpinBox 和 ComboBox 我需要另找图片"

等待用户提供 SpinBox 和 ComboBox 的背景图片，然后创建对应的自定义组件。

## 总结

成功创建了 CustomTextField 组件，使用 034.png 作为背景图片，并在模拟量输入页面替换了所有 TextField。

**设计原则**：
- 组件继承，保留所有原生功能
- 统一视觉风格
- 代码简化，易于维护
- 增强用户体验（焦点边框、鼠标选择）

**关键特性**：
- 使用 034.png 作为背景
- 焦点时显示蓝色边框
- 支持鼠标选择文本
- 代码减少 70-80%
- 全局统一样式

**验证方法**：
- 在 QDS 中预览
- 检查背景图片
- 检查焦点效果
- 检查输入功能
