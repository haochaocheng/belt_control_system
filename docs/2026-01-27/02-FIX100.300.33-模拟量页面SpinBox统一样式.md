# FIX 100.300.33 - 模拟量输入页面 SpinBox 统一样式

**日期**: 2026-01-27
**状态**: ✅ 完成
**文件**: `src/qml/components/device_info/pages/AnalogInputPage.qml`

## 📋 任务目标

将模拟量输入页面中的所有 SpinBox 替换为自定义的 `DeviceInfo.CustomSpinBox`，使用统一的 034.png 背景样式。

## 🔧 修改内容

### 替换的 SpinBox（共 8 个）

1. **registerAddressSpin**（寄存器地址）
   - 范围: 0-255
   - 可编辑

2. **channelSpin**（通道编号）
   - 范围: 0-7
   - 可编辑

3. **upperLimitSpin**（上限值）
   - 范围: 0-10000
   - 步长: 10
   - 默认值: 100
   - 可编辑

4. **lowerLimitSpin**（下限值）
   - 范围: 0-10000
   - 步长: 10
   - 默认值: 0
   - 可编辑

5. **rangeSpin**（量程）
   - 范围: 1-10000
   - 步长: 10
   - 默认值: 100
   - 可编辑

6. **delaySpin**（保护延时）
   - 范围: 0-600
   - 步长: 1
   - 默认值: 10
   - 可编辑
   - **特殊属性**:
     - `property int decimals: 1`
     - `property real realValue: value / 10`
     - 自定义 `textFromValue` 和 `valueFromText` 函数（0.1秒精度）

7. **playCountSpin**（播放次数）
   - 范围: 1-99
   - 默认值: 3
   - 可编辑

8. **durationSpin**（播放时长）
   - 范围: 1-600
   - 步长: 5
   - 默认值: 50
   - 可编辑
   - **特殊属性**:
     - `property int decimals: 1`
     - `property real realValue: value / 10`
     - 自定义 `textFromValue` 和 `valueFromText` 函数（0.1秒精度）

## 📝 修改细节

### 替换前（旧代码）
```qml
SpinBox {
    id: registerAddressSpin
    from: 0
    to: 255
    editable: true
    Layout.fillWidth: true

    background: Rectangle {
        color: "#2d3548"
        radius: 2
        border.color: registerAddressSpin.activeFocus ? "#3498db" : "#7f8c8d"
        border.width: 1
    }

    contentItem: TextInput {
        text: registerAddressSpin.textFromValue(registerAddressSpin.value, registerAddressSpin.locale)
        font.pixelSize: 12
        color: "#E0E0E0"
        horizontalAlignment: Qt.AlignHCenter
        verticalAlignment: Qt.AlignVCenter
        readOnly: !registerAddressSpin.editable
        validator: registerAddressSpin.validator
        inputMethodHints: Qt.ImhFormattedNumbersOnly
    }
}
```

### 替换后（新代码）
```qml
// ✅ 2026-01-27 [FIX 100.300.33]: 使用自定义 SpinBox（034.png 背景）
DeviceInfo.CustomSpinBox {
    id: registerAddressSpin
    from: 0
    to: 255
    editable: true
    Layout.fillWidth: true
}
```

### 特殊 SpinBox（带自定义函数）
```qml
// ✅ 2026-01-27 [FIX 100.300.33]: 使用自定义 SpinBox（034.png 背景）
DeviceInfo.CustomSpinBox {
    id: delaySpin
    from: 0
    to: 600
    value: 10
    stepSize: 1
    editable: true
    Layout.fillWidth: true

    property int decimals: 1
    property real realValue: value / 10

    textFromValue: function(value, locale) {
        return Number(value / 10).toLocaleString(locale, 'f', 1)
    }

    valueFromText: function(text, locale) {
        return Number.fromLocaleString(locale, text) * 10
    }
}
```

## ✅ 修改原则

1. **保留所有属性**: id, from, to, value, stepSize, editable, Layout 属性
2. **删除样式属性**: background 和 contentItem（已在 CustomSpinBox 中定义）
3. **保留特殊属性**: property、textFromValue、valueFromText（delaySpin 和 durationSpin）
4. **添加注释**: 每个替换都添加了 `// ✅ 2026-01-27 [FIX 100.300.33]: 使用自定义 SpinBox（034.png 背景）`

## 🎯 效果

- ✅ 所有 SpinBox 使用统一的 034.png 背景图片
- ✅ 保持原有功能不变（范围、步长、编辑性）
- ✅ 保持特殊 SpinBox 的自定义转换函数（0.1秒精度）
- ✅ 代码更简洁，易于维护
- ✅ 与其他页面（开关量、电机列表）样式统一

## 📊 代码统计

- **修改文件**: 1 个
- **替换 SpinBox**: 8 个
- **删除代码行数**: 约 200 行（background 和 contentItem 定义）
- **新增注释**: 8 行

## 🔍 验证

使用 Grep 验证所有 SpinBox 都已替换：
```bash
grep "SpinBox {" AnalogInputPage.qml
```

结果：所有 8 个 SpinBox 都已替换为 `DeviceInfo.CustomSpinBox`

## 📚 相关文档

- [FIX 100.300.32 - 开关量页面 SpinBox 统一样式](./01-FIX100.300.32-开关量页面SpinBox统一样式.md)
- [CustomSpinBox 组件定义](../../src/qml/components/device_info/CustomSpinBox.qml)

## 🎉 总结

成功将模拟量输入页面的所有 SpinBox 替换为自定义组件，实现了：
1. 统一的视觉样式（034.png 背景）
2. 简化的代码结构
3. 保持完整的功能特性
4. 与其他页面的样式一致性

至此，设备信息弹窗的三个页面（开关量、模拟量、电机列表）的 SpinBox 样式已全部统一。
