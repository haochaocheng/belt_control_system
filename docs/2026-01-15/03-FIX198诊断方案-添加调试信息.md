# FIX 100.198 诊断方案 - 添加调试信息定位崩溃根因

**时间**: 2026-01-15 01:15（北京时间）
**目的**: 在关键位置添加诊断信息，不改变功能逻辑
**原则**: 只读取和打印，不修改任何变量或执行流程

---

## 诊断策略

### 目标

1. 验证 `x4->reordered_opaque` 数组是否正确分配
2. 检查 `opaque->frame_opaque_ref` 的值
3. 检查 PJSIP 传递的 `avframe.opaque_ref` 的值
4. 确定崩溃的精确位置

---

## 修改位置1：FFmpeg libx264.c - opaque_uninit()

**文件**: `cross-compile/src/ffmpeg-rockchip-6.0/libavcodec/libx264.c`
**位置**: Line 149-153

### 原始代码

```c
static void opaque_uninit(X264Opaque *o)
{
    av_buffer_unref(&o->frame_opaque_ref);
    memset(o, 0, sizeof(*o));
}
```

### 修改后

```c
static void opaque_uninit(X264Opaque *o)
{
    /* ✅ 2026-01-15 01:15 [FIX 100.198 DIAG-1] 诊断崩溃根因 */
    fprintf(stderr, "[FIX 100.198 DIAG-1] opaque_uninit() called\n");
    fprintf(stderr, "    opaque pointer: %p\n", (void*)o);

    if (o) {
        fprintf(stderr, "    frame_opaque_ref: %p\n", (void*)o->frame_opaque_ref);
        fprintf(stderr, "    frame_opaque: %p\n", (void*)o->frame_opaque);

        /* 检查 frame_opaque_ref 是否有效 */
        if (o->frame_opaque_ref) {
            fprintf(stderr, "    frame_opaque_ref->data: %p\n", (void*)o->frame_opaque_ref->data);
            fprintf(stderr, "    frame_opaque_ref->size: %d\n", o->frame_opaque_ref->size);
        } else {
            fprintf(stderr, "    frame_opaque_ref is NULL (safe to unref)\n");
        }
    } else {
        fprintf(stderr, "    ❌ opaque pointer is NULL!\n");
    }

    fflush(stderr);

    /* 原始代码 */
    av_buffer_unref(&o->frame_opaque_ref);
    memset(o, 0, sizeof(*o));

    fprintf(stderr, "[FIX 100.198 DIAG-1] opaque_uninit() completed\n");
    fflush(stderr);
}
```

**诊断信息**：
- opaque 指针地址
- frame_opaque_ref 指针值
- 如果非NULL，打印其内部字段
- 执行完成标记

---

## 修改位置2：FFmpeg libx264.c - X264_init()

**文件**: `cross-compile/src/ffmpeg-rockchip-6.0/libavcodec/libx264.c`
**位置**: Line 1308-1313（av_calloc之后）

### 修改后

```c
x4->reordered_opaque = av_calloc(x4->nb_reordered_opaque,
                                sizeof(*x4->reordered_opaque));
if (!x4->reordered_opaque) {
    x4->nb_reordered_opaque = 0;
    return AVERROR(ENOMEM);
}

/* ✅ 2026-01-15 01:20 [FIX 100.198 DIAG-2] 验证数组分配 */
fprintf(stderr, "[FIX 100.198 DIAG-2] reordered_opaque array allocated\n");
fprintf(stderr, "    array pointer: %p\n", (void*)x4->reordered_opaque);
fprintf(stderr, "    array size: %d elements\n", x4->nb_reordered_opaque);
fprintf(stderr, "    element size: %zu bytes\n", sizeof(*x4->reordered_opaque));
fprintf(stderr, "    total size: %zu bytes\n",
        x4->nb_reordered_opaque * sizeof(*x4->reordered_opaque));

/* 验证第一个元素是否清零 */
fprintf(stderr, "    [0].frame_opaque_ref: %p (should be NULL)\n",
        (void*)x4->reordered_opaque[0].frame_opaque_ref);
fprintf(stderr, "    [0].frame_opaque: %p (should be NULL)\n",
        (void*)x4->reordered_opaque[0].frame_opaque);
fflush(stderr);
```

**诊断信息**：
- 数组指针地址
- 数组大小（69个元素）
- 验证第一个元素是否清零

---

## 修改位置3：FFmpeg libx264.c - setup_frame()

**文件**: `cross-compile/src/ffmpeg-rockchip-6.0/libavcodec/libx264.c`
**位置**: Line 475（获取opaque之后）

### 修改后

```c
static int setup_frame(AVCodecContext *ctx, const AVFrame *frame,
                       x264_picture_t **ppic)
{
    X264Context *x4 = ctx->priv_data;
    X264Opaque  *opaque = &x4->reordered_opaque[x4->next_reordered_opaque];

    /* ✅ 2026-01-15 01:25 [FIX 100.198 DIAG-3] 诊断 opaque 获取 */
    static int first_call = 1;
    if (first_call) {
        fprintf(stderr, "[FIX 100.198 DIAG-3] setup_frame() FIRST CALL\n");
        fprintf(stderr, "    x4->reordered_opaque: %p\n", (void*)x4->reordered_opaque);
        fprintf(stderr, "    x4->nb_reordered_opaque: %d\n", x4->nb_reordered_opaque);
        fprintf(stderr, "    x4->next_reordered_opaque: %d\n", x4->next_reordered_opaque);
        fprintf(stderr, "    opaque (from array): %p\n", (void*)opaque);

        if (frame) {
            fprintf(stderr, "    frame->opaque: %p\n", (void*)frame->opaque);
            fprintf(stderr, "    frame->opaque_ref: %p\n", (void*)frame->opaque_ref);
        }
        fflush(stderr);
        first_call = 0;
    }

    /* 原始代码继续... */
    x264_picture_t *pic = &x4->pic;
    // ...
```

**诊断信息**：
- 数组指针和索引
- opaque 指针地址
- frame->opaque_ref 的值（来自PJSIP）

---

## 修改位置4：PJSIP ffmpeg_vid_codecs.c - avcodec_send_frame()调用前

**文件**: `cross-compile/src/pjproject-2.16/pjmedia/src/pjmedia-codec/ffmpeg_vid_codecs.c`
**位置**: Line 4366（avcodec_send_frame之前）

### 修改后

```c
/* ✅ 2026-01-15 01:30 [FIX 100.198 DIAG-4] 检查 AVFrame 状态 */
static int first_encode_call = 1;
if (first_encode_call) {
    PJ_LOG(1,(THIS_FILE, "[FIX 100.198 DIAG-4] === FIRST avcodec_send_frame() CALL ==="));
    PJ_LOG(1,(THIS_FILE, "    avframe.format: %d", avframe.format));
    PJ_LOG(1,(THIS_FILE, "    avframe.width: %d, height: %d", avframe.width, avframe.height));
    PJ_LOG(1,(THIS_FILE, "    avframe.data[0]: %p", avframe.data[0]));
    PJ_LOG(1,(THIS_FILE, "    avframe.linesize[0]: %d", avframe.linesize[0]));
    PJ_LOG(1,(THIS_FILE, "    avframe.opaque: %p", avframe.opaque));
    PJ_LOG(1,(THIS_FILE, "    avframe.opaque_ref: %p", avframe.opaque_ref));

    if (avframe.opaque_ref) {
        PJ_LOG(1,(THIS_FILE, "    ⚠️ avframe.opaque_ref is NOT NULL!"));
        PJ_LOG(1,(THIS_FILE, "       opaque_ref->data: %p", avframe.opaque_ref->data));
        PJ_LOG(1,(THIS_FILE, "       opaque_ref->size: %d", avframe.opaque_ref->size));
    } else {
        PJ_LOG(1,(THIS_FILE, "    ✅ avframe.opaque_ref is NULL (expected for first frame)"));
    }

    PJ_LOG(1,(THIS_FILE, "    ff->enc_ctx: %p", ff->enc_ctx));
    PJ_LOG(1,(THIS_FILE, "    ff->enc_ctx->codec: %p", ff->enc_ctx->codec));
    if (ff->enc_ctx->codec) {
        PJ_LOG(1,(THIS_FILE, "    ff->enc_ctx->codec->name: %s", ff->enc_ctx->codec->name));
    }

    first_encode_call = 0;
}

// 原始代码
err = avcodec_send_frame(ff->enc_ctx, &avframe);
```

**诊断信息**：
- AVFrame 的所有关键字段
- 特别关注 `opaque_ref`（可能是野指针？）
- 编码器上下文状态

---

## 预期结果

### 如果 opaque_ref 是 NULL（正常）

```
[FIX 100.198 DIAG-4] avframe.opaque_ref: (nil)  ← NULL正常
[FIX 100.198 DIAG-3] setup_frame() FIRST CALL
[FIX 100.198 DIAG-1] opaque_uninit() called
    frame_opaque_ref: (nil)                      ← NULL正常
    av_buffer_unref() 应该安全返回
```

### 如果 opaque_ref 是野指针（异常）

```
[FIX 100.198 DIAG-4] avframe.opaque_ref: 0xdeadbeef  ← 野指针！
[FIX 100.198 DIAG-3] opaque 被赋值了这个野指针
[FIX 100.198 DIAG-1] frame_opaque_ref: 0xdeadbeef   ← 野指针！
    av_buffer_unref() 崩溃！
```

### 如果数组分配失败（极不可能）

```
[FIX 100.198 DIAG-2] array pointer: (nil)  ← 应该不会到这里
或
[FIX 100.198 DIAG-3] opaque (from array): 0x invalid  ← 野指针
```

---

## 实施步骤

### 1. 修改 FFmpeg libx264.c

```bash
# 编辑文件
vi cross-compile/src/ffmpeg-rockchip-6.0/libavcodec/libx264.c

# 修改3个位置：
# - Line 149-153: opaque_uninit()
# - Line 1308-1313: X264_init() 的 av_calloc 之后
# - Line 475: setup_frame() 获取 opaque 之后
```

### 2. 增量编译 FFmpeg

```powershell
.\scripts\2026-01-15\incremental-compile-libx264-new.ps1
```

### 3. 修改 PJSIP ffmpeg_vid_codecs.c

```bash
# 编辑文件
vi cross-compile/src/pjproject-2.16/pjmedia/src/pjmedia-codec/ffmpeg_vid_codecs.c

# 修改 Line 4366 之前，添加 DIAG-4 诊断
```

### 4. 完整编译和部署

```powershell
.\build-ubuntu24-apt.ps1 192.168.1.192
```

### 5. 测试并收集日志

```bash
# 在设备上测试
# 日志会显示所有诊断信息
```

---

## 根据诊断结果的下一步

### 场景A：opaque_ref 是 NULL，但仍然崩溃

**结论**：`av_buffer_unref(NULL)` 本身有问题（不太可能）
**行动**：
1. 检查 FFmpeg 版本是否有 bug
2. 检查编译选项
3. 尝试临时绕过 `av_buffer_unref`

### 场景B：opaque_ref 是野指针

**结论**：PJSIP 传递了错误的 AVFrame
**行动**：
1. 检查 PJSIP 如何初始化 AVFrame
2. 查找是谁设置了 `avframe.opaque_ref`
3. 修复 PJSIP 代码

### 场景C：数组分配有问题

**结论**：`x4->reordered_opaque` 指针无效
**行动**：
1. 检查内存分配器
2. 检查是否有内存损坏
3. 添加更多保护检查

---

## 参考文档

- docs/2026-01-15/02-崩溃根因深度分析-第一次编码时崩溃.md
- libx264.c Line 149-153: opaque_uninit()
- libx264.c Line 471-569: setup_frame()
- ffmpeg_vid_codecs.c Line 4366: avcodec_send_frame()
