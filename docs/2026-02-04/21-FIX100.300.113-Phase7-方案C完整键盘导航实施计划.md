# FIX 100.300.113 Phase 7 - 方案C完整键盘导航实施计划

**日期**: 2026-02-04
**任务编号**: FIX 100.300.113 Phase 7
**状态**: 📋 规划中

---

## 🎯 用户需求

**用户明确要求**：
> "我们先考虑使用全导航键的方式，现场实际场景只有简单的导航加数字，还有触摸屏。但是最主要的是使用导航键，**必须完成方案C**"

**关键点**：
1. ✅ 必须完成方案C（完整键盘导航）
2. ✅ 现场主要使用导航键
3. ✅ 辅助使用触摸屏
4. ✅ 简单的导航加数字输入

---

## 📊 当前问题分析

### 方案B的限制（已实现）

**当前实现**：
- ✅ 右键：从串口列表进入参数区域（显示蓝色边框）
- ✅ 左键：从参数区域返回串口列表
- ❌ 参数区域作为整体，无法用键盘导航到具体输入框
- ❌ 按下键时，focusSubArea 从 1 跳到 2（跳过参数区域）

**日志证据**（pjsip.md）：
```
行301-305: focusSubArea 从 1 变为 2（跳过参数区域，直接到按钮区域）
行306, 325, 353, 378, 428: 警告 "Cannot assign to non-existent property 'focusButtonIndex'"
行325, 378: 警告 "Cannot assign to non-existent property 'focusParamIndex'"
```

**根本原因**：
- SerialPortControlPage 缺少 `focusParamIndex` 属性
- SerialPortControlPage 缺少 `focusButtonIndex` 属性
- SerialPortControlPage 没有 NavigationManager
- DeviceSettingsDialog 尝试设置这些不存在的属性，导致警告

---

## 🏗️ 方案C架构设计

### 参考实现：MotorControlPage

**MotorControlPage 的成功经验**：
1. ✅ 使用 NavigationManager 管理4个区域
2. ✅ 定义 focusParamIndex 和 focusButtonIndex 属性
3. ✅ 实现 triggerParamInput() 函数触发虚拟键盘
4. ✅ 实现 getParamFieldCount() 函数返回参数数量
5. ✅ 监听 NavigationManager 的信号，同步焦点状态

### SerialPortControlPage 需要的架构

**4个导航区域**：
```
区域0: 串口列表 (focusSubArea = 0)
  ↓ 右键
区域1: 参数配置区 (focusSubArea = 1, focusParamIndex = 0-N)
  ↓ 下键
区域2: 按钮区域 (focusSubArea = 2, focusButtonIndex = 0-M)
  ↓ 左键
返回串口列表
```

**NavigationManager 区域定义**：
- `areaSerialList`: 串口列表区域
- `areaParams`: 参数配置区域（包含4个子区域）
- `areaButtons`: 按钮区域

---

## 📋 实施步骤

### Phase 7.1: 分析参数区域结构

**目标**：确定参数区域的所有可导航元素

**SerialPortConfigPanel 结构**：
```
1. SerialPortParamsSection (参数配置区)
   - 波特率 (ComboBox) - 索引0
   - 数据位 (ComboBox) - 索引1
   - 停止位 (ComboBox) - 索引2
   - 校验位 (ComboBox) - 索引3
   - 打开串口 (Button) - 索引4
   - 关闭串口 (Button) - 索引5

2. SerialPortSendSection (发送区)
   - 发送数据 (TextArea) - 索引6
   - 格式选择 (ComboBox) - 索引7
   - 发送按钮 (Button) - 索引8
   - 清空按钮 (Button) - 索引9

3. SerialPortReceiveSection (接收区)
   - 清空按钮 (Button) - 索引10
   - 暂停/继续按钮 (Button) - 索引11
   - 格式选择 (ComboBox) - 索引12

4. ModbusRegisterSection (MODBUS 区)
   - 从站地址 (TextField) - 索引13
   - 功能码 (ComboBox) - 索引14
   - 起始地址 (TextField) - 索引15
   - 数量 (SpinBox) - 索引16
   - 写入值 (TextField) - 索引17
   - 读取按钮 (Button) - 索引18
   - 写入按钮 (Button) - 索引19
```

**总计**：20个可导航元素

**简化方案**（推荐）：
- 只导航到主要输入控件（ComboBox, TextField, SpinBox）
- 按钮通过 Enter 键触发，不单独导航
- **简化后总计**：13个可导航元素（索引0-12）

### Phase 7.2: 添加 NavigationManager 到 SerialPortControlPage

**目标**：集成 NavigationManager，管理3个区域

**修改文件**: SerialPortControlPage.qml

**添加代码**（第15-50行）：
```qml
import "../" as DeviceInfo  // ✅ 导入 NavigationManager

Rectangle {
    id: root

    // ========== 公开属性 ==========
    property int currentSerialIndex: 0
    property int focusItemIndex: -1
    property int focusSubArea: 0  // 0:列表 1:参数 2:按钮
    property int focusParamIndex: 0  // ✅ Phase 7.2: 新增参数焦点索引
    property int focusButtonIndex: 0  // ✅ Phase 7.2: 新增按钮焦点索引

    // ✅ Phase 7.2: 暴露 navigationManager 供外部访问
    property alias navigationManager: navigationManager

    // ✅ Phase 7.2: Qt 虚拟键盘引用
    property var virtualKeyboard: null

    // ========== NavigationManager 实例 ==========
    DeviceInfo.NavigationManager {
        id: navigationManager

        // 初始化：从串口列表区开始
        Component.onCompleted: {
            currentArea = areaSerialList
            serialListIndex = 0
            paramIndex = 0
            buttonIndex = 0
            console.log("✅ [SerialPortControlPage] NavigationManager 初始化完成")

            // 同步初始状态到root
            root.currentSerialIndex = 0
            root.focusItemIndex = 0
            root.focusSubArea = 0
            root.focusParamIndex = 0
            root.focusButtonIndex = 0
        }

        // 监听串口列表索引变化
        onSerialListIndexChanged: {
            console.log("✅ [SerialPortControlPage] 串口列表索引变化:", serialListIndex)
            root.currentSerialIndex = serialListIndex
            root.focusItemIndex = serialListIndex
        }

        // 监听参数索引变化
        onParamIndexChanged: {
            console.log("✅ [SerialPortControlPage] 参数索引变化:", paramIndex)
            root.focusParamIndex = paramIndex
        }

        // 监听按钮索引变化
        onButtonIndexChanged: {
            console.log("✅ [SerialPortControlPage] 按钮索引变化:", buttonIndex)
            root.focusButtonIndex = buttonIndex
        }

        // 监听区域变化
        onAreaChanged: function(newArea) {
            console.log("✅ [SerialPortControlPage] 区域变化:", newArea)
            switch(newArea) {
            case areaSerialList:
                root.focusSubArea = 0
                root.focusItemIndex = serialListIndex
                root.focusParamIndex = -1
                root.focusButtonIndex = -1
                break
            case areaParams:
                root.focusSubArea = 1
                root.focusParamIndex = paramIndex
                root.focusItemIndex = -1
                root.focusButtonIndex = -1
                break
            case areaButtons:
                root.focusSubArea = 2
                root.focusButtonIndex = buttonIndex
                root.focusItemIndex = -1
                root.focusParamIndex = -1
                break
            }
        }
    }

    // ... 现有内容 ...
}
```

### Phase 7.3: 实现 getParamFieldCount() 函数

**目标**：返回当前可导航的参数数量

**修改文件**: SerialPortControlPage.qml

**修改代码**（第23-28行）：
```qml
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.3]: 修改 getParamFieldCount 函数
// 返回参数区域的可导航元素数量
function getParamFieldCount() {
    // 简化方案：13个可导航元素（ComboBox, TextField, SpinBox）
    return 13
}
```

### Phase 7.4: 实现 triggerParamInput() 函数

**目标**：触发虚拟键盘，聚焦到指定参数

**修改文件**: SerialPortControlPage.qml

**添加代码**（第30-80行）：
```qml
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.4]: 添加 triggerParamInput 函数
function triggerParamInput(index) {
    console.log("✅ [SerialPortControlPage] triggerParamInput - 参数索引:", index)

    if (!serialConfigPanel.item) {
        console.error("❌ [SerialPortControlPage] SerialPortConfigPanel 未加载")
        return
    }

    // 根据索引触发对应的输入控件
    switch(index) {
    case 0:  // 波特率 (ComboBox)
        if (serialConfigPanel.item.paramsSection.item) {
            serialConfigPanel.item.paramsSection.item.focusBaudRate()
        }
        break
    case 1:  // 数据位 (ComboBox)
        if (serialConfigPanel.item.paramsSection.item) {
            serialConfigPanel.item.paramsSection.item.focusDataBits()
        }
        break
    case 2:  // 停止位 (ComboBox)
        if (serialConfigPanel.item.paramsSection.item) {
            serialConfigPanel.item.paramsSection.item.focusStopBits()
        }
        break
    case 3:  // 校验位 (ComboBox)
        if (serialConfigPanel.item.paramsSection.item) {
            serialConfigPanel.item.paramsSection.item.focusParity()
        }
        break
    case 4:  // 发送数据 (TextArea)
        if (serialConfigPanel.item.sendSection.item) {
            serialConfigPanel.item.sendSection.item.focusSendData()
        }
        break
    case 5:  // 发送格式 (ComboBox)
        if (serialConfigPanel.item.sendSection.item) {
            serialConfigPanel.item.sendSection.item.focusSendFormat()
        }
        break
    case 6:  // 接收格式 (ComboBox)
        if (serialConfigPanel.item.receiveSection.item) {
            serialConfigPanel.item.receiveSection.item.focusReceiveFormat()
        }
        break
    case 7:  // 从站地址 (TextField)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusSlaveAddress()
        }
        break
    case 8:  // 功能码 (ComboBox)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusFunctionCode()
        }
        break
    case 9:  // 起始地址 (TextField)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusStartAddress()
        }
        break
    case 10:  // 数量 (SpinBox)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusQuantity()
        }
        break
    case 11:  // 写入值 (TextField)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusWriteValue()
        }
        break
    case 12:  // 寄存器列表 (ListView)
        if (serialConfigPanel.item.modbusSection.item) {
            serialConfigPanel.item.modbusSection.item.focusRegisterList()
        }
        break
    default:
        console.warn("⚠️ [SerialPortControlPage] 未知的参数索引:", index)
        break
    }
}
```

### Phase 7.5: 修改 SerialPortConfigPanel 添加焦点管理

**目标**：为每个输入控件添加焦点指示器和 focus 函数

**修改文件**: SerialPortConfigPanel.qml

**修改代码**（第15-30行）：
```qml
Rectangle {
    id: root
    color: "transparent"

    // ========== 公开属性 ==========
    property var currentSerialPort: null
    property int focusSubArea: 0  // 0=列表 1=参数 2=按钮
    property int focusParamIndex: -1  // ✅ Phase 7.5: 新增参数焦点索引

    // ✅ Phase 7.5: 移除整体焦点指示器（改为每个输入控件单独显示）
    // Rectangle { ... }  // 删除

    // ✅ Phase 7.5: 暴露子区域的 Loader，供 triggerParamInput 访问
    property alias paramsSection: paramsSection
    property alias sendSection: sendSection
    property alias receiveSection: receiveSection
    property alias modbusSection: modbusSection

    // ... 现有内容 ...
}
```

### Phase 7.6: 修改 SerialPortParamsSection 添加焦点函数

**目标**：为每个输入控件添加 focus 函数和焦点指示器

**修改文件**: SerialPortParamsSection.qml

**添加代码**：
```qml
Rectangle {
    id: root

    // ✅ Phase 7.6: 添加焦点函数
    function focusBaudRate() {
        baudRateCombo.forceActiveFocus()
        console.log("✅ [SerialPortParamsSection] 波特率获得焦点")
    }

    function focusDataBits() {
        dataBitsCombo.forceActiveFocus()
        console.log("✅ [SerialPortParamsSection] 数据位获得焦点")
    }

    function focusStopBits() {
        stopBitsCombo.forceActiveFocus()
        console.log("✅ [SerialPortParamsSection] 停止位获得焦点")
    }

    function focusParity() {
        parityCombo.forceActiveFocus()
        console.log("✅ [SerialPortParamsSection] 校验位获得焦点")
    }

    // ... 现有内容 ...

    // ✅ Phase 7.6: 为每个输入控件添加焦点指示器
    ComboBox {
        id: baudRateCombo
        // ... 现有属性 ...

        // 焦点指示器
        Rectangle {
            anchors.fill: parent
            color: "transparent"
            border.color: parent.activeFocus ? "#2196F3" : "transparent"
            border.width: parent.activeFocus ? 3 : 0
            radius: 4
            z: 10
        }
    }

    // ... 其他输入控件类似 ...
}
```

### Phase 7.7: 修改 DeviceSettingsDialog 支持方案C

**目标**：修改键盘事件处理，支持参数区域内的导航

**修改文件**: DeviceSettingsDialog.qml

**修改代码**（Keys.onDownPressed）：
```qml
Keys.onDownPressed: function(event) {
    // ... 其他页面的处理 ...

    // ✅ 串口控制页面特殊处理
    if (currentCategory === 6 && currentFocusArea === 2) {
        var serialPage = serialPortControlPageLoader.item
        if (serialPage) {
            console.log("🔍 [串口控制调试] 下键 - focusSubArea:", serialPage.focusSubArea)

            if (serialPage.focusSubArea === 0) {
                // 在串口列表中，使用默认处理（上下键移动）
                // 不做特殊处理，让默认逻辑处理
            } else if (serialPage.focusSubArea === 1) {
                // ✅ Phase 7.7: 在参数区域，使用 NavigationManager 导航
                if (serialPage.navigationManager) {
                    serialPage.navigationManager.moveDown()
                    event.accepted = true
                    return
                }
            } else if (serialPage.focusSubArea === 2) {
                // 在按钮区域，使用默认处理
                // 不做特殊处理，让默认逻辑处理
            }
        }
    }

    // ... 默认处理 ...
}
```

**修改代码**（Keys.onUpPressed）：
```qml
Keys.onUpPressed: function(event) {
    // ... 其他页面的处理 ...

    // ✅ 串口控制页面特殊处理
    if (currentCategory === 6 && currentFocusArea === 2) {
        var serialPage = serialPortControlPageLoader.item
        if (serialPage) {
            console.log("🔍 [串口控制调试] 上键 - focusSubArea:", serialPage.focusSubArea)

            if (serialPage.focusSubArea === 0) {
                // 在串口列表中，使用默认处理
            } else if (serialPage.focusSubArea === 1) {
                // ✅ Phase 7.7: 在参数区域，使用 NavigationManager 导航
                if (serialPage.navigationManager) {
                    serialPage.navigationManager.moveUp()
                    event.accepted = true
                    return
                }
            } else if (serialPage.focusSubArea === 2) {
                // 在按钮区域，使用默认处理
            }
        }
    }

    // ... 默认处理 ...
}
```

### Phase 7.8: 测试验证

**测试场景**：

1. **串口列表导航**：
   - 上下键在串口列表中移动
   - 左键返回到左侧类别
   - 右键进入参数区域

2. **参数区域导航**：
   - 右键从串口列表进入参数区域
   - 上下键在参数区域内移动焦点
   - 焦点指示器正确显示（蓝色边框）
   - Enter 键触发虚拟键盘
   - 输入完成后焦点恢复

3. **按钮区域导航**：
   - 下键从参数区域进入按钮区域
   - 左右键在按钮间移动
   - Enter 键触发按钮

4. **返回导航**：
   - 左键从参数区域返回串口列表
   - 左键从按钮区域返回参数区域

---

## 🎯 验收标准

### 基本功能

- [ ] 右键从串口列表进入参数区域
- [ ] 上下键在参数区域内移动焦点
- [ ] 焦点指示器正确显示（蓝色边框，3px）
- [ ] Enter 键触发虚拟键盘
- [ ] 输入完成后焦点恢复
- [ ] 左键从参数区域返回串口列表
- [ ] 下键从参数区域进入按钮区域
- [ ] 左键从按钮区域返回参数区域

### 边界情况

- [ ] 鼠标点击输入框后，键盘导航仍然正常
- [ ] 在参数区域第一个元素按上键，返回串口列表
- [ ] 在参数区域最后一个元素按下键，进入按钮区域
- [ ] 虚拟键盘弹出时，焦点不丢失
- [ ] 虚拟键盘关闭后，焦点恢复到输入框

### 性能要求

- [ ] 焦点切换响应时间 < 100ms
- [ ] 虚拟键盘弹出时间 < 200ms
- [ ] 无卡顿或延迟

---

## 📊 修改文件汇总

| 文件 | 修改内容 | 行数变化 |
|------|---------|---------|
| SerialPortControlPage.qml | 添加 NavigationManager、focusParamIndex、focusButtonIndex、triggerParamInput | +150 行 |
| SerialPortConfigPanel.qml | 移除整体焦点指示器、暴露子区域 Loader | +10 行 |
| SerialPortParamsSection.qml | 添加焦点函数、焦点指示器 | +50 行 |
| SerialPortSendSection.qml | 添加焦点函数、焦点指示器 | +30 行 |
| SerialPortReceiveSection.qml | 添加焦点函数、焦点指示器 | +20 行 |
| ModbusRegisterSection.qml | 添加焦点函数、焦点指示器 | +60 行 |
| DeviceSettingsDialog.qml | 修改上下键处理，支持 NavigationManager | +30 行 |
| **总计** | | **+350 行** |

---

## 💡 实施亮点

### 1. 参考成功经验

- 完全参考 MotorControlPage 的实现
- 使用相同的 NavigationManager 架构
- 保持代码风格一致

### 2. 简化方案

- 只导航到主要输入控件（13个）
- 按钮通过 Enter 键触发，不单独导航
- 减少复杂度，提高可维护性

### 3. 焦点指示器

- 每个输入控件单独显示焦点指示器
- 蓝色边框（3px）清晰可见
- 使用 z: 10 确保在最上层

### 4. 虚拟键盘集成

- triggerParamInput() 函数触发虚拟键盘
- 输入完成后焦点恢复
- 支持数字键盘和中文键盘

---

## 🔄 下一步

1. ⏳ Phase 7.1: 分析参数区域结构
2. ⏳ Phase 7.2: 添加 NavigationManager
3. ⏳ Phase 7.3: 实现 getParamFieldCount()
4. ⏳ Phase 7.4: 实现 triggerParamInput()
5. ⏳ Phase 7.5: 修改 SerialPortConfigPanel
6. ⏳ Phase 7.6: 修改 SerialPortParamsSection
7. ⏳ Phase 7.7: 修改 DeviceSettingsDialog
8. ⏳ Phase 7.8: 测试验证

---

## 📝 相关文档

1. [15-串口控制右侧区域导航完整规划.md](15-串口控制右侧区域导航完整规划.md) - 方案A/B/C对比
2. [20-FIX100.300.113-串口控制右侧区域导航功能完整实施总结.md](20-FIX100.300.113-串口控制右侧区域导航功能完整实施总结.md) - 方案B实施总结
3. MotorControlPage.qml - 参考实现

---

**创建日期**: 2026-02-04
**状态**: 📋 规划完成，待实施
**编写人员**: Claude Sonnet 4.5
