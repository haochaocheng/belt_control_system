# FIX 100.300.113 Phase 7.37 - Phase 1 完成 - 重构 SerialPortConfigPanel.qml

**日期**: 2026-02-04
**任务编号**: FIX 100.300.113 Phase 7.37 - Phase 1
**状态**: ✅ 完成

---

## 📋 任务概述

**目标**: 完全重构 SerialPortConfigPanel.qml，采用 Tab 架构（参考 MotorConfigPanel）

**原因**: ScrollView 包裹所有 4 个区域导致 Qt 的 ensureVisible() 触发不必要的滚动，焦点丢失

**解决方案**: 改为 Tab 切换，每个 Tab 内部独立管理滚动

---

## ✅ 完成内容

### 1. 添加标题栏

```qml
// ========== 标题栏 ==========
Rectangle {
    id: header
    anchors.top: parent.top
    anchors.left: parent.left
    anchors.right: parent.right
    height: 50
    color: "transparent"

    // 背景图片
    Image {
        anchors.fill: parent
        source: "../images/059.png"
        fillMode: Image.Stretch
        z: -1
    }

    Text {
        anchors.centerIn: parent
        text: "串口配置"
        font.pixelSize: 16
        font.weight: Font.Bold
        color: "#E0E0E0"
    }
}
```

**效果**:
- ✅ 显示"串口配置"标题
- ✅ 使用背景图片 059.png
- ✅ 高度 50px

### 2. 添加 Tab 栏（Flickable + Repeater）

```qml
// ========== Tab栏 ==========
Rectangle {
    id: tabBar
    anchors.top: header.bottom
    anchors.left: parent.left
    anchors.right: parent.right
    height: 50
    color: "#252b3d"
    border.color: "#3d4556"
    border.width: 1

    Flickable {
        id: tabFlickable
        anchors.fill: parent
        clip: true
        contentWidth: tabRow.width
        contentHeight: height
        flickableDirection: Flickable.HorizontalFlick
        boundsBehavior: Flickable.StopAtBounds

        // 自动滚动到当前Tab
        function scrollToTab(tabIndex) {
            var tabWidth = 120
            var targetX = tabIndex * tabWidth
            var centerX = targetX - (width / 2) + (tabWidth / 2)
            centerX = Math.max(0, Math.min(centerX, contentWidth - width))
            contentX = centerX
        }

        // 监听 currentTabIndex 变化
        Connections {
            target: root
            function onCurrentTabIndexChanged() {
                tabFlickable.scrollToTab(root.currentTabIndex)
            }
        }

        // 支持鼠标滚轮横向滚动
        MouseArea {
            anchors.fill: parent
            propagateComposedEvents: true

            onWheel: {
                var delta = wheel.angleDelta.y
                tabFlickable.contentX = Math.max(0, Math.min(
                    tabFlickable.contentX - delta,
                    tabFlickable.contentWidth - tabFlickable.width
                ))
                wheel.accepted = true
            }

            onPressed: mouse.accepted = false
        }

        Row {
            id: tabRow
            spacing: 0
            height: parent.height
            width: childrenRect.width

            Repeater {
                model: ["参数配置", "发送区", "接收区", "MODBUS寄存器"]

                Rectangle {
                    width: 120
                    height: 50
                    color: "transparent"

                    // 焦点指示器
                    Rectangle {
                        anchors.fill: parent
                        color: "transparent"
                        border.color: (root.focusSubArea === 1 && root.focusTabIndex === index)
                                      ? "#2196F3" : "transparent"
                        border.width: (root.focusSubArea === 1 && root.focusTabIndex === index) ? 3 : 0
                        radius: 4
                        z: 11
                    }

                    // 背景图片
                    Image {
                        anchors.fill: parent
                        fillMode: Image.Stretch
                        z: -1
                        source: root.currentTabIndex === index
                                ? "../images/DJHeadbutton2.png"
                                : "../images/DJHeadbutton1.png"
                    }

                    // 底部激活指示条
                    Rectangle {
                        visible: root.currentTabIndex === index
                        width: parent.width
                        height: 3
                        color: "#2196F3"
                        anchors.bottom: parent.bottom
                    }

                    Text {
                        text: modelData
                        anchors.verticalCenter: parent.verticalCenter
                        anchors.left: parent.left
                        anchors.leftMargin: 45
                        font.pixelSize: 14
                        font.weight: root.currentTabIndex === index ? Font.Bold : Font.Normal
                        color: root.currentTabIndex === index ? "#E0E0E0" : "#9E9E9E"
                    }

                    MouseArea {
                        anchors.fill: parent
                        onClicked: {
                            root.currentTabIndex = index
                            root.focus = true
                        }
                    }
                }
            }
        }

        // 横向滚动条指示器
        Rectangle {
            anchors.bottom: parent.bottom
            anchors.left: parent.left
            height: 3
            width: parent.width * (parent.width / tabRow.width)
            color: "#2196F3"
            opacity: 0.5
            x: tabFlickable.contentX * (parent.width / tabRow.width)
            visible: tabRow.width > parent.width
        }
    }
}
```

**效果**:
- ✅ 4 个 Tab：参数配置、发送区、接收区、MODBUS寄存器
- ✅ 每个 Tab 宽度 120px
- ✅ 支持横向滚动（Flickable）
- ✅ 支持鼠标滚轮横向滚动
- ✅ 自动滚动到当前 Tab
- ✅ 焦点指示器（蓝色边框，3px）
- ✅ 背景图片切换（DJHeadbutton1.png / DJHeadbutton2.png）
- ✅ 底部激活指示条（蓝色，3px）
- ✅ 文本样式切换（激活时加粗，颜色变亮）

### 3. 添加 StackLayout 切换 Tab 内容

```qml
// ========== 内容区域 ==========
Rectangle {
    id: contentArea
    anchors.top: tabBar.bottom
    anchors.left: parent.left
    anchors.right: parent.right
    anchors.bottom: parent.bottom
    color: "transparent"

    StackLayout {
        anchors.fill: parent
        currentIndex: root.currentTabIndex

        // 0: 参数配置
        Loader {
            id: paramsTabLoader
            active: root.currentTabIndex === 0
            source: "SerialPortParamsSection.qml"  // ✅ 暂时使用旧文件名，Phase 2 会重命名

            onLoaded: {
                console.log("✅ [SerialPortConfigPanel] SerialPortParamsSection 加载成功")
                if (item) {
                    // 传递当前串口信息
                    item.currentSerialPort = Qt.binding(function() {
                        return root.currentSerialPort
                    })
                }
            }
        }

        // 1: 发送区
        Loader {
            id: sendTabLoader
            active: root.currentTabIndex === 1
            source: "SerialPortSendSection.qml"  // ✅ 暂时使用旧文件名，Phase 3 会重命名

            onLoaded: {
                console.log("✅ [SerialPortConfigPanel] SerialPortSendSection 加载成功")
                if (item) {
                    // 传递当前串口信息
                    item.currentSerialPort = Qt.binding(function() {
                        return root.currentSerialPort
                    })
                }
            }
        }

        // 2: 接收区
        Loader {
            id: receiveTabLoader
            active: root.currentTabIndex === 2
            source: "SerialPortReceiveSection.qml"  // ✅ 暂时使用旧文件名，Phase 3 会重命名

            onLoaded: {
                console.log("✅ [SerialPortConfigPanel] SerialPortReceiveSection 加载成功")
                if (item) {
                    // 传递当前串口信息
                    item.currentSerialPort = Qt.binding(function() {
                        return root.currentSerialPort
                    })
                }
            }
        }

        // 3: MODBUS寄存器
        Loader {
            id: modbusTabLoader
            active: root.currentTabIndex === 3
            source: "ModbusRegisterSection.qml"  // ✅ 暂时使用旧文件名，Phase 3 会重命名

            onLoaded: {
                console.log("✅ [SerialPortConfigPanel] ModbusRegisterSection 加载成功")
                if (item) {
                    // 传递当前串口信息
                    item.currentSerialPort = Qt.binding(function() {
                        return root.currentSerialPort
                    })
                }
            }
        }
    }
}
```

**效果**:
- ✅ 使用 StackLayout 切换 Tab 内容
- ✅ 使用 Loader 延迟加载（active: root.currentTabIndex === index）
- ✅ 使用 Qt.binding() 保持双向绑定
- ✅ 暂时使用旧文件名（Phase 2 和 Phase 3 会重命名）

### 4. 添加 getCurrentTab() 函数

```qml
// ========== 获取当前 Tab 的引用 ==========
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.37]: 添加 getCurrentTab() 函数
function getCurrentTab() {
    switch(root.currentTabIndex) {
    case 0:
        return paramsTabLoader.item
    case 1:
        return sendTabLoader.item
    case 2:
        return receiveTabLoader.item
    case 3:
        return modbusTabLoader.item
    default:
        return null
    }
}
```

**效果**:
- ✅ 提供统一接口获取当前 Tab 的引用
- ✅ 供 SerialPortControlPage 调用

### 5. 添加新属性

```qml
// ========== 公开属性 ==========
property var currentSerialPort: null  // 当前串口信息

// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.37]: 新增 Tab 相关属性
property int currentTabIndex: 0  // 当前 Tab 索引（0=参数配置, 1=发送区, 2=接收区, 3=MODBUS寄存器）

// 焦点管理属性
property int focusSubArea: 0  // 0=列表 1=Tab栏 2=参数 3=按钮
property int focusTabIndex: -1  // Tab 焦点索引
property int focusParamIndex: -1  // 参数焦点索引

// 虚拟键盘引用
property var virtualKeyboard: null

// ========== 信号 ==========
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.37]: 添加焦点请求信号
signal requestFocusParamIndex(int paramIndex)
```

**效果**:
- ✅ currentTabIndex: 当前 Tab 索引
- ✅ focusSubArea: 焦点区域（0=列表 1=Tab栏 2=参数 3=按钮）
- ✅ focusTabIndex: Tab 焦点索引
- ✅ focusParamIndex: 参数焦点索引
- ✅ virtualKeyboard: 虚拟键盘引用
- ✅ requestFocusParamIndex: 焦点请求信号

### 6. 移除旧代码

```qml
// ========== 旧代码备份（2026-02-04 Phase 7.37 之前）==========
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.37]: 移除 ScrollView 架构
// 原因：ScrollView 包裹所有 4 个区域导致 Qt 的 ensureVisible() 触发不必要的滚动
// 旧代码：
// ScrollView {
//     id: scrollView
//     anchors.fill: parent
//     clip: true
//     ScrollBar.horizontal.policy: ScrollBar.AlwaysOff
//
//     Connections {
//         target: scrollView.contentItem
//         function onContentYChanged() { ... }
//         function onFlickStarted() { ... }
//         function onFlickEnded() { ... }
//         function onMovementStarted() { ... }
//         function onMovementEnded() { ... }
//         function onInteractiveChanged() { ... }
//     }
//
//     ColumnLayout {
//         width: scrollView.width
//         spacing: 16
//
//         Loader { id: paramsSection; source: "SerialPortParamsSection.qml" }
//         Loader { id: sendSection; source: "SerialPortSendSection.qml" }
//         Loader { id: receiveSection; source: "SerialPortReceiveSection.qml" }
//         Loader { id: modbusSection; source: "ModbusRegisterSection.qml" }
//     }
// }
//
// ✅ 2026-02-04 [FIX 100.300.113 Phase 7.5]: 暴露子区域的 Loader，供 triggerParamInput 访问
// property alias paramsSection: paramsSection
// property alias sendSection: sendSection
// property alias receiveSection: receiveSection
// property alias modbusSection: modbusSection
```

**效果**:
- ✅ 移除 ScrollView 架构
- ✅ 移除 Connections（监听 ScrollView 事件）
- ✅ 移除 property alias（旧的 Loader 引用）
- ✅ 保留旧代码作为注释，便于回溯

---

## 📊 代码变化汇总

| 文件 | 修改内容 | 行数变化 |
|------|---------|---------|
| SerialPortConfigPanel.qml | 完全重构，采用 Tab 架构 | -160 行, +364 行 |

**总计**：+204 行（净增加）

---

## 🎯 预期效果

### 布局结构

```
SerialPortConfigPanel
├── 标题栏（50px）
│   └── "串口配置"
├── Tab栏（50px）
│   ├── 参数配置
│   ├── 发送区
│   ├── 接收区
│   └── MODBUS寄存器
└── 内容区域（StackLayout）
    ├── SerialPortParamsSection（Tab 0）
    ├── SerialPortSendSection（Tab 1）
    ├── SerialPortReceiveSection（Tab 2）
    └── ModbusRegisterSection（Tab 3）
```

### 焦点管理

- ✅ Tab 栏支持焦点导航（focusSubArea === 1）
- ✅ 焦点指示器清晰可见（蓝色边框，3px）
- ✅ 每个 Tab 内容独立管理焦点

### 滚动行为

- ✅ Tab 栏支持横向滚动（Flickable）
- ✅ 每个 Tab 内容独立管理滚动（Phase 2 会添加 ScrollView）
- ✅ **不会触发 Qt 的 ensureVisible() 导致焦点丢失**

---

## 🔍 验证方法

### 测试步骤

1. **打开 QDS**：
   - 运行 main_qds.qml
   - 打开设备设置对话框
   - 切换到串口控制页面

2. **测试 Tab 切换**：
   - 点击不同的 Tab
   - 检查 Tab 背景图片是否切换
   - 检查底部激活指示条是否显示
   - 检查 Tab 内容是否正确切换

3. **测试横向滚动**：
   - 使用鼠标滚轮横向滚动 Tab 栏
   - 检查 Tab 栏是否正确滚动

4. **测试焦点指示器**：
   - 使用键盘导航到 Tab 栏
   - 检查焦点指示器是否清晰可见（蓝色边框，3px）

---

## 📝 相关文档

1. [60-FIX100.300.113-Phase7.37-移除ScrollView解决焦点丢失问题.md](60-FIX100.300.113-Phase7.37-移除ScrollView解决焦点丢失问题.md) - Phase 7.37 总体方案
2. [61-串口控制界面重构实施文档-完全复制电机控制架构.md](61-串口控制界面重构实施文档-完全复制电机控制架构.md) - 完整实施计划
3. MotorConfigPanel.qml - 参考实现

---

## 🎯 下一步

**Phase 2**: 创建 SerialPortParamsTab.qml（从 SerialPortParamsSection.qml 重命名）

**任务内容**：
1. 重命名 SerialPortParamsSection.qml → SerialPortParamsTab.qml
2. 添加 ScrollView 包裹参数区域
3. 添加 focusParamIndex 属性
4. 添加 virtualKeyboard 属性
5. 添加 requestFocusParamIndex 信号
6. 添加 triggerParamInput() 函数
7. 添加 getParamFieldCount() 函数

---

**创建日期**: 2026-02-04
**完成日期**: 2026-02-04
**状态**: ✅ 完成
**编写人员**: Claude Sonnet 4.5
