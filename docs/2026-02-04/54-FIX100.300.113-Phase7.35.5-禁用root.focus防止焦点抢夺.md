# FIX 100.300.113 Phase 7.35.5 - 禁用 root.focus 防止焦点抢夺

**日期**: 2026-02-04
**任务编号**: FIX 100.300.113 Phase 7.35.5
**状态**: ✅ 完成

---

## 🐛 问题描述

用户反馈（第二十三次测试）：
> "测试完成，在COM1焦点上，只按向下键"

### Phase 7.35.4.1 测试结果分析

从日志 line 298-322 发现了**根本原因**：

```
line 298: [DeviceSettingsDialog root] activeFocus 变化: false  ← root 失去焦点
line 299: [串口名称] activeFocus 变化: true  ← serialNameText 获得焦点
line 306:    - serialNameText.activeFocus (调用后): true  ✅ forceActiveFocus() 成功
line 312: [焦点追踪] Window.activeFocusItem 变化: QQuickText(...)  ← 焦点确实在 Text 上
line 315: [串口名称] activeFocus 变化: false  ← 焦点丢失
line 322: [焦点追踪] Window.activeFocusItem 变化: null  ← 焦点变成 null！
```

**关键发现**：

1. ✅ `forceActiveFocus()` 成功设置了焦点（line 306）
2. ✅ `Window.activeFocusItem` 确实变成了 serialNameText（line 312）
3. ❌ 但是焦点立即丢失（line 315）
4. ❌ **焦点变成了 `null`**（line 322）- 没有任何元素有焦点！

---

## 🔍 根本原因分析

### 焦点变成 null 的原因

**焦点变成 `null` 通常发生在：**

1. **FocusScope 失去了焦点**
2. **父元素被销毁或隐藏**
3. **有两个元素都有 `focus: true`，Qt 无法决定谁应该获得焦点**

### DeviceSettingsDialog 的焦点冲突

**当前焦点结构**：

```qml
FocusScope {
    id: dialogFocusScope
    focus: true  // ← FocusScope 有 focus

    Rectangle {
        id: root
        objectName: "deviceSettingsDialog"
        focus: true  // ← root 也有 focus！

        // ... 内容
        SerialPortParamsSection {
            Text {
                id: serialNameText
                focus: true  // ← serialNameText 也有 focus！
            }
        }
    }
}
```

**问题流程**：

1. 用户按右键
2. `serialNameText.forceActiveFocus()` 被调用
3. serialNameText 获得焦点（activeFocus: true）
4. **Qt 检测到 Rectangle root 也有 `focus: true`**
5. **Qt 的焦点管理系统尝试重新分配焦点**
6. **由于冲突，Qt 将焦点设置为 `null`**
7. serialNameText 失去焦点（activeFocus: false）

### 为什么 root 需要 focus: true？

从注释看（DeviceSettingsDialog.qml line 266）：
```qml
// ✅ 2026-01-28 [FIX 100.300.100]: FocusScope 内的 Rectangle 需要 focus
focus: true
```

这是为了让 Rectangle 能够接收键盘事件（Keys.onUpPressed 等）。

**但是**，当子元素（如 serialNameText）获得焦点时，root 的 `focus: true` 会导致焦点冲突。

---

## ✅ 解决方案

### 在设置焦点前禁用 root.focus

**修改文件**: SerialPortParamsSection.qml

**修改位置**: focusSerialName 函数（line 20-38）

**修改内容**：

```qml
function focusSerialName() {
    console.log("🔍 [SerialPortParamsSection] focusSerialName 开始")
    console.log("   - serialNameText.activeFocus (调用前):", serialNameText.activeFocus)

    // ✅ 2026-02-04 [FIX 100.300.113 Phase 7.35.5]: 先禁用 DeviceSettingsDialog root 的 focus
    // 防止 root 抢夺焦点
    var dialog = serialNameText
    while (dialog && dialog.objectName !== "deviceSettingsDialog") {
        dialog = dialog.parent
    }
    if (dialog) {
        console.log("🔍 [SerialPortParamsSection] 找到 DeviceSettingsDialog，禁用 root.focus")
        dialog.focus = false
    }

    serialNameText.forceActiveFocus()
    console.log("   - serialNameText.activeFocus (调用后):", serialNameText.activeFocus)
    console.log("✅ [SerialPortParamsSection] 串口名称获得焦点")
}
```

**工作原理**：

1. 向上遍历父元素，找到 `objectName === "deviceSettingsDialog"` 的元素（即 Rectangle root）
2. 设置 `dialog.focus = false`，禁用 root 的 focus 属性
3. 然后调用 `serialNameText.forceActiveFocus()`
4. 这样就不会有焦点冲突，serialNameText 可以保持焦点

**为什么这样可以工作**：

- Rectangle root 的 `focus: true` 只是为了接收键盘事件
- 当子元素获得焦点时，键盘事件仍然会冒泡到 root
- 所以禁用 root.focus 不会影响键盘事件处理
- 但可以避免焦点冲突

---

## 📊 代码变化汇总

| 文件 | 修改内容 | 行数变化 |
|------|---------|---------|
| SerialPortParamsSection.qml | focusSerialName: 添加禁用 root.focus 的逻辑 | +11 行 |

**总计**：+11 行

---

## 🎯 预期效果

### 修复前

```
line 299: [串口名称] activeFocus 变化: true  ← 获得焦点
line 312: [焦点追踪] Window.activeFocusItem 变化: QQuickText(...)
line 315: [串口名称] activeFocus 变化: false  ← 立即丢失
line 322: [焦点追踪] Window.activeFocusItem 变化: null  ← 焦点变成 null
```

### 修复后（预期）

```
🔍 [SerialPortParamsSection] focusSerialName 开始
   - serialNameText.activeFocus (调用前): false
🔍 [SerialPortParamsSection] 找到 DeviceSettingsDialog，禁用 root.focus
🔍 [DeviceSettingsDialog root] activeFocus 变化: false  ← root 失去焦点
🔍 [串口名称] activeFocus 变化: true  ← serialNameText 获得焦点
   - serialNameText.activeFocus (调用后): true
✅ [SerialPortParamsSection] 串口名称获得焦点
🔍 [焦点追踪] Window.activeFocusItem 变化: QQuickText(...)  ← 焦点保持在 Text 上
（焦点不再丢失）
```

---

## 🔍 后续优化

如果这个修复有效，应该：

1. **将相同的逻辑应用到所有焦点函数**：
   - focusBaudRate()
   - focusDevicePath()
   - focusDataBits()
   - focusSerialType()
   - 等等...

2. **或者创建一个通用的辅助函数**：
   ```qml
   function disableDialogFocus() {
       var dialog = serialNameText
       while (dialog && dialog.objectName !== "deviceSettingsDialog") {
           dialog = dialog.parent
       }
       if (dialog) {
           dialog.focus = false
       }
   }
   ```

3. **考虑在 DeviceSettingsDialog 层面修复**：
   - 监听子元素获得焦点的事件
   - 自动禁用 root.focus
   - 当子元素失去焦点时，重新启用 root.focus

---

## 📝 测试步骤

1. **打开 QDS**：
   - 运行 main_qds.qml
   - 打开设备设置对话框
   - 切换到串口控制页面

2. **测试焦点导航**：
   - 右键进入参数区域
   - 查看调试日志

3. **验证修复**：
   - 检查是否有 "找到 DeviceSettingsDialog，禁用 root.focus" 日志
   - 检查 serialNameText.activeFocus 是否保持为 true
   - 检查 Window.activeFocusItem 是否保持为 QQuickText
   - **检查焦点是否不再变成 null**

4. **测试下键导航**：
   - 在 COM1 焦点上按下键
   - 检查焦点是否移动到设备路径（/dev/ttyS0）
   - 检查焦点指示器是否可见

---

## 📝 相关文档

1. [53-FIX100.300.113-Phase7.35.4-添加FocusScope焦点追踪.md](53-FIX100.300.113-Phase7.35.4-添加FocusScope焦点追踪.md) - Phase 7.35.4 调试
2. [52-FIX100.300.113-Phase7.35.3-添加焦点丢失追踪调试.md](52-FIX100.300.113-Phase7.35.3-添加焦点丢失追踪调试.md) - Phase 7.35.3 调试
3. SerialPortParamsSection.qml - 串口参数配置区域
4. DeviceSettingsDialog.qml - 设备设置对话框

---

## 🎯 验收标准

- [x] 添加禁用 root.focus 的逻辑
- [x] 添加调试日志
- [ ] 在 QDS 中测试，焦点不再丢失
- [ ] 焦点指示器可见
- [ ] 下键导航正常工作
- [ ] 如果有效，应用到所有焦点函数

---

**创建日期**: 2026-02-04
**完成日期**: 2026-02-04
**状态**: ✅ 代码完成，等待测试
**编写人员**: Claude Sonnet 4.5
