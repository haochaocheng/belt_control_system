# FIX 100.300.110 - AnalogInputPage GridLayout 重构完整指南

## 任务概述
将 AnalogInputPage.qml 的参数区域从 RowLayout（两列 ColumnLayout）改为 GridLayout（4列表格布局），与 SwitchInputPage 保持一致。

## 重构原因
- 统一界面布局风格
- 提高代码可维护性
- 优化参数字段的视觉对齐
- 简化导航系统的索引管理

## 重构范围
**目标文件**: `src/qml/components/device_info/pages/AnalogInputPage.qml`

**需要修改的部分**:
1. 参数区域布局（Line 290-1077）
2. `triggerParamInput()` 函数（Line 1361-1442）
3. `getParamFieldCount()` 函数（Line 1356-1358）

**不需要修改的部分**:
- 左侧列表区域（Line 55-205）
- 标题栏（Line 216-240）
- 底部按钮区域（Line 1088-1192）
- `loadProtectionData()` 函数（Line 1200-1253）
- `saveProtectionData()` 函数（Line 1256-1314）
- `Component.onCompleted` 函数（Line 1317-1350）

## 参数字段映射表

| 新索引 | 字段名 | 组件类型 | 组件ID | 列位置 | 行位置 | 特殊说明 |
|--------|--------|----------|--------|--------|--------|----------|
| 0 | 保护名称 | CustomTextField | nameField | 左(0,1) | 行0 | - |
| 1 | 保护延时 | CustomSpinBox | delaySpin | 右(2,3) | 行0 | 0.1秒精度 |
| 2 | 模块类型 | CustomComboBox | moduleTypeCombo | 左(0,1) | 行1 | 自动设置寄存器地址 |
| 3 | 播放次数 | CustomSpinBox | playCountSpin | 右(2,3) | 行1 | - |
| 4 | 寄存器地址 | CustomSpinBox | registerAddressSpin | 左(0,1) | 行2 | - |
| 5 | 播放时长 | CustomSpinBox | durationSpin | 右(2,3) | 行2 | 0.1秒精度 |
| 6 | 通道编号 | CustomSpinBox | channelSpin | 左(0,1) | 行3 | - |
| 7 | 语音报警 | RadioButton组 | ttsRadio/fileRadio | 右(2,3) | 行3 | Row布局包装 |
| 8 | 上限值 | CustomSpinBox | upperLimitSpin | 左(0,1) | 行4 | - |
| 9 | TTS文字 | CustomTextField | ttsTextField | 右(2,3) | 行4 | visible: ttsRadio.checked |
| 10 | 下限值 | CustomSpinBox | lowerLimitSpin | 左(0,1) | 行5 | - |
| 11 | 音频文件 | CustomTextField | audioField | 右(2,3) | 行5 | visible: fileRadio.checked |
| 12 | 单位 | CustomComboBox | unitCombo | 左(0,1) | 行6 | editable: true |
| 13 | 量程 | CustomSpinBox | rangeSpin | 右(2,3) | 行6 | - |

## GridLayout 配置

```qml
// ✅ 2026-01-31 [FIX 100.300.110]: 改为 GridLayout 布局，与 SwitchInputPage 保持一致
GridLayout {
    width: paramScrollView.width * 0.9
    columns: 4
    columnSpacing: 10
    rowSpacing: 12
```

## 标准字段模板

### 普通输入字段（TextField/SpinBox/ComboBox）

```qml
// 参数索引: X - 字段名（列位置，行位置）
Text {
    text: "字段名:"
    font.pixelSize: 21
    color: "#9E9E9E"
    Layout.column: 0或2  // 左列0，右列2
    Layout.row: 行号
    Layout.preferredWidth: 160
    horizontalAlignment: Text.AlignRight
}

Item {
    Layout.column: 1或3  // 左列1，右列3
    Layout.row: 行号
    Layout.fillWidth: true
    Layout.maximumWidth: 300
    implicitHeight: 组件ID.implicitHeight

    DeviceInfo.CustomXXX {
        id: 组件ID
        anchors.fill: parent
        keyboardManager: root.keyboardManager
        // 其他属性...
    }

    Rectangle {
        anchors.fill: parent
        color: "transparent"
        border.color: (root.focusSubArea === 1 && root.focusParamIndex === X) ? "#2196F3" : "transparent"
        border.width: (root.focusSubArea === 1 && root.focusParamIndex === X) ? 3 : 0
        radius: 4
        z: 10
    }
}
```

### RadioButton 组字段（语音报警）

```qml
// 参数索引: 7 - 语音报警（右列，行3）
Text {
    text: "语音报警:"
    font.pixelSize: 21
    color: "#9E9E9E"
    Layout.column: 2
    Layout.row: 3
    Layout.preferredWidth: 160
    horizontalAlignment: Text.AlignRight
}

Item {
    Layout.column: 3
    Layout.row: 3
    Layout.fillWidth: true
    Layout.maximumWidth: 300
    implicitHeight: voiceAlarmRow.implicitHeight

    RowLayout {
        id: voiceAlarmRow
        anchors.fill: parent
        spacing: 10

        RadioButton {
            id: ttsRadio
            text: "文字转语音"
            checked: true
            font.pixelSize: 12
            // ... indicator 和 contentItem 定义
        }

        RadioButton {
            id: fileRadio
            text: "音频文件"
            checked: false
            font.pixelSize: 12
            // ... indicator 和 contentItem 定义
        }
    }

    Rectangle {
        anchors.fill: parent
        color: "transparent"
        border.color: (root.focusSubArea === 1 && root.focusParamIndex === 7) ? "#2196F3" : "transparent"
        border.width: (root.focusSubArea === 1 && root.focusParamIndex === 7) ? 3 : 0
        radius: 4
        z: 10
    }
}
```

## 完整的 GridLayout 代码

由于代码量较大（约800行），这里提供完整的 GridLayout 结构框架：

```qml
GridLayout {
    width: paramScrollView.width * 0.9
    columns: 4
    columnSpacing: 10
    rowSpacing: 12

    // ========== 第一行：保护名称（左）、保护延时（右）==========
    // 参数索引: 0 - 保护名称
    // ... 标签和输入框

    // 参数索引: 1 - 保护延时
    // ... 标签和输入框

    // ========== 第二行：模块类型（左）、播放次数（右）==========
    // 参数索引: 2 - 模块类型
    // ... 标签和输入框

    // 参数索引: 3 - 播放次数
    // ... 标签和输入框

    // ========== 第三行：寄存器地址（左）、播放时长（右）==========
    // 参数索引: 4 - 寄存器地址
    // ... 标签和输入框

    // 参数索引: 5 - 播放时长
    // ... 标签和输入框

    // ========== 第四行：通道编号（左）、语音报警（右）==========
    // 参数索引: 6 - 通道编号
    // ... 标签和输入框

    // 参数索引: 7 - 语音报警（RadioButton组）
    // ... 标签和 RadioButton 组

    // ========== 第五行：上限值（左）、TTS文字（右）==========
    // 参数索引: 8 - 上限值
    // ... 标签和输入框

    // 参数索引: 9 - TTS文字（条件显示）
    // ... 标签和输入框（visible: ttsRadio.checked）

    // ========== 第六行：下限值（左）、音频文件（右）==========
    // 参数索引: 10 - 下限值
    // ... 标签和输入框

    // 参数索引: 11 - 音频文件（条件显示）
    // ... 标签和输入框（visible: fileRadio.checked）

    // ========== 第七行：单位（左）、量程（右）==========
    // 参数索引: 12 - 单位
    // ... 标签和输入框

    // 参数索引: 13 - 量程
    // ... 标签和输入框

}  // GridLayout 结束
```

## 函数更新

### 1. getParamFieldCount() 函数

**位置**: Line 1356-1358

**修改前**:
```qml
function getParamFieldCount() {
    return 16  // 模拟量输入有16个参数字段（左列8个 + 右列8个）
}
```

**修改后**:
```qml
function getParamFieldCount() {
    return 14  // ✅ 2026-01-31 [FIX 100.300.110]: 改为14个参数字段（GridLayout 4列布局）
}
```

### 2. triggerParamInput() 函数

**位置**: Line 1361-1442

**修改内容**: 更新 switch-case 映射，使用新的参数索引（0-13）

**修改后**:
```qml
function triggerParamInput(paramIndex) {
    console.log("✅ [AnalogInputPage] 触发参数输入 - 索引:", paramIndex)

    var inputField = null
    var inputMode = "numeric"  // 默认数字模式

    switch(paramIndex) {
    case 0:  // 保护名称
        inputField = nameField
        inputMode = "chinese"
        break
    case 1:  // 保护延时
        inputField = delaySpin
        inputMode = "numeric"
        break
    case 2:  // 模块类型
        inputField = moduleTypeCombo
        inputMode = "english"
        break
    case 3:  // 播放次数
        inputField = playCountSpin
        inputMode = "numeric"
        break
    case 4:  // 寄存器地址
        inputField = registerAddressSpin
        inputMode = "numeric"
        break
    case 5:  // 播放时长
        inputField = durationSpin
        inputMode = "numeric"
        break
    case 6:  // 通道编号
        inputField = channelSpin
        inputMode = "numeric"
        break
    case 7:  // 语音报警类型（RadioButton 组，切换选中状态）
        if (ttsRadio.checked) {
            fileRadio.checked = true
        } else {
            ttsRadio.checked = true
        }
        console.log("✅ [AnalogInputPage] 切换语音报警类型:", ttsRadio.checked ? "文字转语音" : "音频文件")
        return  // RadioButton 不需要打开虚拟键盘
    case 8:  // 上限值
        inputField = upperLimitSpin
        inputMode = "numeric"
        break
    case 9:  // TTS文字
        inputField = ttsTextField
        inputMode = "chinese"
        break
    case 10:  // 下限值
        inputField = lowerLimitSpin
        inputMode = "numeric"
        break
    case 11:  // 音频文件
        inputField = audioField
        inputMode = "english"
        break
    case 12:  // 单位
        inputField = unitCombo
        inputMode = "english"
        break
    case 13:  // 量程
        inputField = rangeSpin
        inputMode = "numeric"
        break
    default:
        console.warn("⚠️ [AnalogInputPage] 未知的参数索引:", paramIndex)
        return
    }

    // 打开虚拟键盘
    if (virtualKeyboard && inputField) {
        console.log("✅ [AnalogInputPage] 打开 Qt 虚拟键盘 - 控件:", inputField, "模式:", inputMode)
        virtualKeyboard.openForField(inputField, function(newValue) {
            console.log("✅ [AnalogInputPage] 虚拟键盘输入完成:", newValue)
        }, inputMode, root)
    } else {
        console.warn("⚠️ [AnalogInputPage] 虚拟键盘或输入控件不可用")
    }
}
```

## 执行步骤

### 步骤1: 备份原文件
```powershell
Copy-Item "src/qml/components/device_info/pages/AnalogInputPage.qml" `
          "src/qml/components/device_info/pages/AnalogInputPage.qml.backup-$(Get-Date -Format 'yyyyMMdd-HHmmss')"
```

### 步骤2: 替换参数区域
1. 找到 Line 290-1077（旧的 RowLayout 参数区域）
2. 删除整个旧的参数区域
3. 插入新的 GridLayout 参数区域（参考上面的完整代码）

### 步骤3: 更新函数
1. 更新 `getParamFieldCount()` 函数（Line 1356-1358）
2. 更新 `triggerParamInput()` 函数（Line 1361-1442）

### 步骤4: 验证
1. 编译检查：确保没有语法错误
2. 运行时检查：
   - 所有14个参数字段正确显示
   - 焦点指示器正确响应（索引0-13）
   - 条件显示字段（TTS文字、音频文件）正确切换
   - 虚拟键盘正确打开
   - 数据加载和保存功能正常

## 注意事项

### 1. 组件ID保持不变
所有组件ID必须保持不变，以确保数据加载和保存功能正常：
- `nameField`
- `delaySpin`
- `moduleTypeCombo`
- `playCountSpin`
- `registerAddressSpin`
- `durationSpin`
- `channelSpin`
- `ttsRadio` / `fileRadio`
- `upperLimitSpin`
- `ttsTextField`
- `lowerLimitSpin`
- `audioField`
- `unitCombo`
- `rangeSpin`

### 2. 条件显示字段
TTS文字和音频文件字段需要保持 `visible` 绑定：
- TTS文字：`visible: ttsRadio.checked`
- 音频文件：`visible: fileRadio.checked`

### 3. RadioButton 组
语音报警字段使用 Row 布局包装 RadioButton 组，保持原有的样式和行为。

### 4. 焦点指示器
所有字段的焦点指示器必须使用新的索引（0-13），确保导航系统正确工作。

### 5. 不要修改其他部分
- 左侧列表区域
- 标题栏
- 底部按钮区域
- 数据加载和保存函数

## 完成标准

- [ ] 所有14个参数字段已添加到 GridLayout
- [ ] 焦点指示器索引正确（0-13）
- [ ] 条件显示字段的 visible 绑定正确
- [ ] 所有组件ID保持不变
- [ ] `triggerParamInput()` 函数已更新
- [ ] `getParamFieldCount()` 函数已更新
- [ ] 编译无错误
- [ ] 运行时布局正确
- [ ] 焦点导航正常工作
- [ ] 虚拟键盘正常打开
- [ ] 数据加载和保存功能正常

## 参考文件

- **SwitchInputPage.qml**: GridLayout 布局参考（Line 292-645）
- **BasicConfigTab.qml**: GridLayout 布局参考（Line 39-326）

## 相关文档

- [FIX 100.300.110 重构计划](./01-FIX100.300.110-AnalogInputPage-GridLayout重构计划.md)

## 总结

这是一个大型重构任务，涉及约800行代码的替换。建议使用IDE的重构工具或代码编辑器的多光标功能来提高效率。完成后，务必进行完整的测试，确保所有功能正常工作。
