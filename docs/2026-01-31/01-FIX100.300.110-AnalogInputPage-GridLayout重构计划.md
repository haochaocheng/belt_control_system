# FIX 100.300.110 - AnalogInputPage GridLayout 重构计划

## 任务概述
将 AnalogInputPage.qml 的参数区域从 RowLayout（两列 ColumnLayout）改为 GridLayout（4列表格布局）。

## 重构范围
- **目标文件**: `src/qml/components/device_info/pages/AnalogInputPage.qml`
- **重构区域**: Line 290-1077（参数区域）
- **参考文件**:
  - `SwitchInputPage.qml`（GridLayout布局参考）
  - `BasicConfigTab.qml`（GridLayout布局参考）

## 参数字段映射（新索引 0-13）

| 新索引 | 字段名 | 组件类型 | 列位置 | 行位置 | 旧索引 | 备注 |
|--------|--------|----------|--------|--------|--------|------|
| 0 | 保护名称 | TextField | 左(0,1) | 行0 | 0 | - |
| 1 | 保护延时 | SpinBox | 右(2,3) | 行0 | 1 | - |
| 2 | 模块类型 | ComboBox | 左(0,1) | 行1 | 2 | - |
| 3 | 播放次数 | SpinBox | 右(2,3) | 行1 | 3 | - |
| 4 | 寄存器地址 | SpinBox | 左(0,1) | 行2 | 4 | - |
| 5 | 播放时长 | SpinBox | 右(2,3) | 行2 | 5 | - |
| 6 | 通道编号 | SpinBox | 左(0,1) | 行3 | 6 | - |
| 7 | 语音报警 | RadioButton | 右(2,3) | 行3 | 7 | Row布局 |
| 8 | 上限值 | SpinBox | 左(0,1) | 行4 | 8 | - |
| 9 | TTS文字 | TextField | 右(2,3) | 行4 | 9 | visible: ttsRadio.checked |
| 10 | 下限值 | SpinBox | 左(0,1) | 行5 | 10 | - |
| 11 | 音频文件 | TextField | 右(2,3) | 行5 | 11 | visible: audioRadio.checked |
| 12 | 单位 | ComboBox | 左(0,1) | 行6 | 12 | - |
| 13 | 量程 | SpinBox | 右(2,3) | 行6 | 13 | - |

## GridLayout 配置
```qml
GridLayout {
    width: paramScrollView.width * 0.9
    columns: 4
    columnSpacing: 10
    rowSpacing: 12
}
```

## 标准字段模板
```qml
// 标签
Text {
    text: "字段名:"
    font.pixelSize: 21
    color: "#9E9E9E"
    Layout.column: 0或2  // 左列0，右列2
    Layout.row: 行号
    Layout.preferredWidth: 160
    horizontalAlignment: Text.AlignRight
}

// 输入框容器
Item {
    Layout.column: 1或3  // 左列1，右列3
    Layout.row: 行号
    Layout.fillWidth: true
    Layout.maximumWidth: 300
    implicitHeight: 组件.implicitHeight

    // 输入组件（CustomTextField/CustomSpinBox/CustomComboBox）

    // 焦点指示器
    Rectangle {
        anchors.fill: parent
        color: "transparent"
        border.color: (root.focusSubArea === 1 && root.focusParamIndex === 索引) ? "#2196F3" : "transparent"
        border.width: (root.focusSubArea === 1 && root.focusParamIndex === 索引) ? 3 : 0
        radius: 4
        z: 10
    }
}
```

## 函数更新

### triggerParamInput() 函数
- 更新参数索引映射（0-13）
- 保持原有的输入模式设置

### getParamFieldCount() 函数
- 返回 14（而不是之前的16）

## 执行步骤
1. ✅ 创建重构计划文档
2. ⏳ 备份原文件
3. ⏳ 替换 GridLayout 开头
4. ⏳ 添加所有14个参数字段
5. ⏳ 更新 triggerParamInput() 函数
6. ⏳ 更新 getParamFieldCount() 函数
7. ⏳ 测试验证

## 注意事项
- 保持所有组件ID不变（nameField, delaySpin等）
- 保持条件显示字段的 visible 绑定（TTS文字、音频文件）
- 保持 RadioButton 组的 Row 布局
- 不修改左侧列表区域
- 不修改标题栏
- 不修改底部按钮区域
- 不修改 loadProtectionData() 和 saveProtectionData() 函数

## 完成标准
- [ ] 所有14个参数字段已添加
- [ ] 焦点指示器索引正确（0-13）
- [ ] 条件显示字段的 visible 绑定正确
- [ ] 所有组件ID保持不变
- [ ] triggerParamInput() 函数已更新
- [ ] getParamFieldCount() 函数已更新
- [ ] 编译无错误
- [ ] 运行时布局正确
