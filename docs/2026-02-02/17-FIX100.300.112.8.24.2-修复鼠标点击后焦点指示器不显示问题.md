# FIX 100.300.112.8.24.2 - 修复鼠标点击后焦点指示器不显示问题

**日期**: 2026-02-02
**任务编号**: FIX 100.300.112.8.24.2
**状态**: ✅ 修复完成

---

## 🎯 问题描述

**测试场景**:
1. 使用方向键导航到"模块地址"
2. 按回车键 → 虚拟键盘弹出 ✅
3. 按 ESC 退出虚拟键盘 ✅
4. 鼠标点击"模块地址" → `focusParamIndex = 2` ✅
5. 按向下键 → **焦点应该到"反馈通道"（索引4），但焦点指示器不显示** ❌

---

## 📊 日志分析

```
Debug: ✅ [BasicConfigTab] 鼠标点击模块地址，同步焦点索引: 2
Debug: ✅ [NavigationManager] 方向键: Down 当前区域: C 当前索引: 2
Debug: ✅ [MotorControlPage] 参数索引变化: 4
Debug: ✅ [NavigationManager] 参数索引: 4
```

**分析**:
1. `focusParamIndex` 正确更新为 4 ✅
2. NavigationManager 的 `paramIndex` 也正确更新为 4 ✅
3. 但是焦点指示器（蓝色边框）不显示 ❌

---

## 🔍 根本原因

**问题不是导航逻辑，而是焦点指示器被 SpinBox 覆盖！**

### 原因分析

**原代码**（Line 238-245）:
```qml
MouseArea {
    anchors.fill: parent
    onClicked: {
        console.log("✅ [BasicConfigTab] 鼠标点击模块地址，同步焦点索引: 2")
        root.focusParamIndex = 2
        mouse.accepted = false  // ❌ 让事件继续传递给 SpinBox
    }
}
```

**问题**:
1. `mouse.accepted = false` 让鼠标事件继续传递给 SpinBox
2. SpinBox 获得了**真实焦点**（activeFocus）
3. SpinBox 的边框或背景覆盖了焦点指示器的蓝色边框
4. 用户看不到焦点指示器

---

## 🔧 解决方案

### 方案：移除 SpinBox 的焦点，显示焦点指示器

**原理**: 鼠标点击后，移除 SpinBox 的焦点，将焦点返回给父容器，这样焦点指示器就能显示了。

**修改位置**: `src/qml/components/device_info/pages/BasicConfigTab.qml`

#### 1. 模块地址 MouseArea（Line 237-249）

**修改前**:
```qml
MouseArea {
    anchors.fill: parent
    onClicked: {
        console.log("✅ [BasicConfigTab] 鼠标点击模块地址，同步焦点索引: 2")
        root.focusParamIndex = 2
        mouse.accepted = false  // 让事件继续传递给 SpinBox
    }
}
```

**修改后**:
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.24]: 鼠标点击同步焦点索引
// ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 焦点,显示焦点指示器
MouseArea {
    anchors.fill: parent
    onClicked: {
        console.log("✅ [BasicConfigTab] 鼠标点击模块地址，同步焦点索引: 2")
        root.focusParamIndex = 2
        // ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 的焦点,显示焦点指示器
        moduleAddressSpin.focus = false
        root.focus = true  // 将焦点返回给父容器
        mouse.accepted = true  // 不让事件传递给 SpinBox
    }
}
```

#### 2. 输出通道 MouseArea（Line 291-303）

**修改后**:
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.24]: 鼠标点击同步焦点索引
// ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 焦点,显示焦点指示器
MouseArea {
    anchors.fill: parent
    onClicked: {
        console.log("✅ [BasicConfigTab] 鼠标点击输出通道，同步焦点索引: 3")
        root.focusParamIndex = 3
        // ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 的焦点,显示焦点指示器
        outputChannelSpin.focus = false
        root.focus = true  // 将焦点返回给父容器
        mouse.accepted = true  // 不让事件传递给 SpinBox
    }
}
```

#### 3. 反馈通道 MouseArea（Line 347-359）

**修改后**:
```qml
// ✅ 2026-02-02 [FIX 100.300.112.8.24]: 鼠标点击同步焦点索引
// ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 焦点,显示焦点指示器
MouseArea {
    anchors.fill: parent
    onClicked: {
        console.log("✅ [BasicConfigTab] 鼠标点击反馈通道，同步焦点索引: 4")
        root.focusParamIndex = 4
        // ✅ 2026-02-02 [FIX 100.300.112.8.24.1]: 移除 SpinBox 的焦点,显示焦点指示器
        feedbackChannelSpin.focus = false
        root.focus = true  // 将焦点返回给父容器
        mouse.accepted = true  // 不让事件传递给 SpinBox
    }
}
```

---

## 💡 关键修改点

1. **移除 SpinBox 焦点**: `moduleAddressSpin.focus = false`
2. **返回焦点给父容器**: `root.focus = true`
3. **阻止事件传递**: `mouse.accepted = true`（从 `false` 改为 `true`）

---

## 🎯 验证方法

### 测试步骤

1. **在 QDS 中测试**:
   - 方向键导航到"模块地址"
   - 按回车键 → 虚拟键盘弹出
   - 按 ESC 退出虚拟键盘
   - 鼠标点击"模块地址"
   - 按向下键
   - **验证**: 焦点指示器应该显示在"反馈通道"上 ✅

2. **查看日志**:
   ```
   Debug: ✅ [BasicConfigTab] 鼠标点击模块地址，同步焦点索引: 2
   Debug: ✅ [NavigationManager] 方向键: Down 当前区域: C 当前索引: 2
   Debug: ✅ [MotorControlPage] 参数索引变化: 4
   Debug: ✅ [NavigationManager] 参数索引: 4
   ```

3. **视觉验证**:
   - 焦点指示器（蓝色边框）应该清晰可见
   - 不应该被 SpinBox 覆盖

---

## ⚠️ 副作用

**鼠标点击后，SpinBox 不会获得焦点**:
- 用户不能直接用键盘输入数字（上下键增减）
- 必须按回车键打开虚拟键盘输入

**这是预期行为**，因为：
1. 用户需求是"混合输入模式"：键盘和鼠标都能弹出虚拟键盘
2. 如果 SpinBox 获得焦点，回车键会被 SpinBox 拦截，无法打开虚拟键盘
3. 这个方案确保了键盘和鼠标的行为一致

---

## 🔗 相关文档

- [15-混合输入模式问题分析和解决方案.md](./15-混合输入模式问题分析和解决方案.md)
- [16-FIX100.300.112.8.24.1-鼠标点击后导航焦点不同步问题.md](./16-FIX100.300.112.8.24.1-鼠标点击后导航焦点不同步问题.md)
- [BasicConfigTab.qml](../../src/qml/components/device_info/pages/BasicConfigTab.qml)

---

## 📦 下一步行动

### 立即测试

1. ⏳ 在 QDS 中测试
   - 验证焦点指示器显示
   - 验证导航逻辑
   - 验证虚拟键盘弹出

2. ⏳ 解决虚拟键盘遮挡输入框问题
   - 用户反馈："虚拟键盘会遮挡输入框，怎么解决"
   - 需要调整虚拟键盘位置或滚动视图

3. ⏳ 编译部署测试（如果 QDS 测试通过）
   - 编译：`.\build-ubuntu24-apt.ps1 188`
   - 在设备上验证

---

**创建日期**: 2026-02-02
**状态**: ✅ 修复完成，待 QDS 测试
**编写人员**: Claude Sonnet 4.5
