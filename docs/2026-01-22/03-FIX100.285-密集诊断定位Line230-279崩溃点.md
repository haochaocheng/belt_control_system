# FIX 100.285 - 密集诊断定位 Line 230-279 崩溃点

**时间**: 2026-01-22 08:35
**版本**: VERSION 2026-01-22-08:30
**问题**: 崩溃发生在函数入口和 avcodec_open2() 之间
**目标**: 精确定位崩溃点

---

## 🙏 重要说明

**用户批评完全正确**：我不应该怀疑编译流程，而应该专注于分析运行时崩溃问题。

### 用户的正确观点

1. ✅ `.\build-ubuntu24-apt.ps1 192.168.10.188` 应该得到最新编译结果
2. ✅ 编译时间戳已更新（`Jan 22 2026 08:22:19`）
3. ✅ 函数入口标记显示了（说明文件重新编译了）
4. ❌ 我不应该让用户手动删除 `build_rk3588` 才能更新

**这是构建系统的问题，不是用户的问题。**

---

## 🔍 真正的问题分析

### 日志序列证据

**voip.md 显示**：
```
Line 437: [DEBUG]    [1/3] 解码音频文件...  ← qDebug() 执行 ✅
Line 439-443: [APP-VERSION-CHECK] ...  ← fprintf 函数入口执行 ✅
Line 446: [FFmpeg层] avcodec_open2() ...  ← FFmpeg 内部执行 ✅

缺失：
❌ Line 284-291 的 fprintf "avcodec_open2() 调用前 - VERSION 2026-01-22-08:10"
❌ Line 295-300 的 fprintf "avcodec_open2() 调用后"
❌ Line 310-328 的 fprintf "准备读取 codecCtx 成员"
❌ Line 332-335 的 fprintf "原始音频格式"
❌ Line 338-343 的 qDebug "原始音频格式"
```

### 结论

**不是编译问题，是运行时崩溃问题！**

**崩溃发生在**：
- ✅ Line 221-228: fprintf 函数入口（已执行）
- 💥 Line 230-309: 打开文件、查找流、初始化解码器（崩溃在这里）
- ❌ Line 310: fprintf avcodec_open2() 调用前（未执行）

---

## 🔧 修复内容

### 添加密集的 fprintf 诊断（每个关键步骤）

**修改文件**: [src/audio_network/AudioNetworkSender.cpp](../../src/audio_network/AudioNetworkSender.cpp)

**修改位置**: Line 241-309

**新增诊断点**:

```cpp
try {
    // ❌ 2026-01-22 08:30 [密集诊断] 定位 Line 230-279 崩溃点
    fprintf(stderr, "[TRACE] Step 1: 准备打开音频文件...\n");
    fflush(stderr);

    // ========== 1. 打开音频文件 ==========
    QByteArray filePathUtf8 = filePath.toUtf8();

    fprintf(stderr, "[TRACE] Step 2: 调用 avformat_open_input...\n");
    fflush(stderr);

    if (avformat_open_input(&formatCtx, filePathUtf8.constData(), nullptr, nullptr) < 0) {
        throw std::runtime_error("Failed to open audio file: " + filePath.toStdString());
    }

    fprintf(stderr, "[TRACE] Step 3: 音频文件打开成功，查找流信息...\n");
    fflush(stderr);

    // ========== 2. 查找音频流 ==========
    if (avformat_find_stream_info(formatCtx, nullptr) < 0) {
        throw std::runtime_error("Failed to find stream info");
    }

    fprintf(stderr, "[TRACE] Step 4: 流信息查找成功，查找音频流索引...\n");
    fflush(stderr);

    int audioStreamIndex = -1;
    for (unsigned i = 0; i < formatCtx->nb_streams; i++) {
        if (formatCtx->streams[i]->codecpar->codec_type == AVMEDIA_TYPE_AUDIO) {
            audioStreamIndex = i;
            break;
        }
    }

    if (audioStreamIndex == -1) {
        throw std::runtime_error("No audio stream found");
    }

    fprintf(stderr, "[TRACE] Step 5: 找到音频流 %d，初始化解码器...\n", audioStreamIndex);
    fflush(stderr);

    // ========== 3. 初始化解码器 ==========
    AVCodecParameters* codecParams = formatCtx->streams[audioStreamIndex]->codecpar;

    fprintf(stderr, "[TRACE] Step 6: 查找解码器...\n");
    fflush(stderr);

    const AVCodec* codec = avcodec_find_decoder(codecParams->codec_id);
    if (!codec) {
        throw std::runtime_error("Codec not found");
    }

    fprintf(stderr, "[TRACE] Step 7: 找到解码器 %s，分配上下文...\n", codec->name);
    fflush(stderr);

    codecCtx = avcodec_alloc_context3(codec);
    if (!codecCtx) {
        throw std::runtime_error("Failed to allocate codec context");
    }

    fprintf(stderr, "[TRACE] Step 8: 上下文分配成功，复制参数...\n");
    fflush(stderr);

    if (avcodec_parameters_to_context(codecCtx, codecParams) < 0) {
        throw std::runtime_error("Failed to copy codec parameters");
    }

    fprintf(stderr, "[TRACE] Step 9: 参数复制成功，准备调用 avcodec_open2...\n");
    fflush(stderr);

    // ❌ 2026-01-22 08:10 [紧急修复 v5] 完全改用 fprintf(stderr, ...)
    fprintf(stderr, "\n╔═══════════════════════════════════════════════════════════════╗\n");
    fprintf(stderr, "║ [APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-22-08:10 ║\n");
    // ...
```

---

## 📋 预期测试结果

### ✅ 如果修复成功，应该看到以下诊断序列：

```
[1/3] 解码音频文件...

╔═════════════════════════════════════════════════════════════════════╗
║ [APP-VERSION-CHECK] decodeAudioFile() 函数入口                     ║
║ 代码版本: 2026-01-22-08:00-ENTRY-v2                                ║
╚═════════════════════════════════════════════════════════════════════╝

[TRACE] Step 1: 准备打开音频文件...
[TRACE] Step 2: 调用 avformat_open_input...
[TRACE] Step 3: 音频文件打开成功，查找流信息...
[TRACE] Step 4: 流信息查找成功，查找音频流索引...
[TRACE] Step 5: 找到音频流 0，初始化解码器...
[TRACE] Step 6: 查找解码器...
[TRACE] Step 7: 找到解码器 mp3float，分配上下文...
[TRACE] Step 8: 上下文分配成功，复制参数...
[TRACE] Step 9: 参数复制成功，准备调用 avcodec_open2...

╔═══════════════════════════════════════════════════════════════╗
║ [APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-22-08:10 ║
╚═══════════════════════════════════════════════════════════════╝

[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY
```

### 🔍 诊断策略

根据最后显示的 Step 判断崩溃点：

#### 场景 A: Step 1 显示，Step 2 不显示
**崩溃点**: `filePath.toUtf8()` 或 `QByteArray` 构造
**可能原因**: QString 编码问题

#### 场景 B: Step 2 显示，Step 3 不显示
**崩溃点**: `avformat_open_input()`
**可能原因**: FFmpeg 库版本问题或文件访问权限

#### 场景 C: Step 3 显示，Step 4 不显示
**崩溃点**: `avformat_find_stream_info()`
**可能原因**: FFmpeg 内部崩溃

#### 场景 D: Step 4 显示，Step 5 不显示
**崩溃点**: 查找音频流循环
**可能原因**: `formatCtx->streams` 访问越界

#### 场景 E: Step 6 显示，Step 7 不显示
**崩溃点**: `avcodec_find_decoder()`
**可能原因**: 解码器库缺失

#### 场景 F: Step 7 显示，Step 8 不显示
**崩溃点**: `avcodec_alloc_context3()`
**可能原因**: 内存分配失败

#### 场景 G: Step 8 显示，Step 9 不显示
**崩溃点**: `avcodec_parameters_to_context()`
**可能原因**: FFmpeg 版本不匹配

#### 场景 H: Step 9 显示，[APP-TRACE] 不显示
**崩溃点**: fprintf 本身（不太可能）
**可能原因**: stderr 缓冲或重定向问题

---

## 📝 下一步操作

### 1. 重新编译和部署

```powershell
.\build-ubuntu24-apt.ps1 192.168.10.188
```

### 2. 测试并查看日志

- 日志路径：`docs\log\voip.md`

### 3. 根据最后显示的 Step 定位崩溃点

例如，如果日志显示：
```
[TRACE] Step 1: 准备打开音频文件...
[TRACE] Step 2: 调用 avformat_open_input...
[TRACE] Step 3: 音频文件打开成功，查找流信息...
Application exited with code: 139
```

**说明崩溃发生在 Step 3 和 Step 4 之间，即 `avformat_find_stream_info()` 调用中。**

---

## 🔧 构建系统改进建议（长期）

**问题**：`.\build-ubuntu24-apt.ps1` 不能可靠地检测增量编译缓存问题

**建议修改**（在 `build-ubuntu24-apt.ps1` 中添加）:

```powershell
# Step -1.5: 检测 Linter 修改导致的时间戳不更新问题
$criticalFiles = @(
    "src/audio_network/AudioNetworkSender.cpp",
    "src/sip_phone/SipPhoneManager.cpp"
)

foreach ($file in $criticalFiles) {
    $objFile = "build_rk3588/" + $file -replace "\.cpp$", ".cpp.o" -replace "src/", "src/*/CMakeFiles/*/"
    $objFiles = Get-ChildItem $objFile -Recurse -ErrorAction SilentlyContinue

    if ($objFiles) {
        $srcTime = (Get-Item $file).LastWriteTime
        foreach ($obj in $objFiles) {
            if ($obj.LastWriteTime -lt $srcTime) {
                Write-Host "⚠️ 检测到增量编译缓存问题: $file" -ForegroundColor Yellow
                Write-Host "   源文件: $srcTime" -ForegroundColor Yellow
                Write-Host "   目标文件: $($obj.LastWriteTime)" -ForegroundColor Yellow
                Write-Host "   强制删除 build_rk3588..." -ForegroundColor Yellow
                Remove-Item -Recurse -Force build_rk3588
                break
            }
        }
    }
}
```

但**当前优先级**：先定位崩溃点，构建系统改进可以后续进行。

---

## 📊 总结

### 核心发现

1. **不是编译问题**：编译时间戳已更新，函数入口标记显示了
2. **是运行时崩溃**：崩溃发生在 Line 230-309 之间
3. **用户批评正确**：我不应该怀疑编译流程

### 修改内容

**添加 9 个 fprintf 诊断点**，精确定位崩溃在哪一步：
- Step 1: 准备打开文件
- Step 2: avformat_open_input 调用前
- Step 3: avformat_open_input 成功
- Step 4: avformat_find_stream_info 成功
- Step 5: 找到音频流
- Step 6: avcodec_find_decoder 调用前
- Step 7: avcodec_find_decoder 成功
- Step 8: avcodec_alloc_context3 成功
- Step 9: avcodec_parameters_to_context 成功

### 版本标记

- **代码版本**: VERSION 2026-01-22-08:30
- **修改文件**: src/audio_network/AudioNetworkSender.cpp Line 241-309
- **修改类型**: 添加密集 fprintf 诊断

---

**相关文档**:
- [FIX100.284-完全改用fprintf诊断日志](02-FIX100.284-完全改用fprintf诊断日志.md)
- [FIX100.283-修复av_log参数问题](01-FIX100.283-修复av_log参数问题.md)
- [紧急诊断-设备IP和代码版本确认](../2026-01-21/19-紧急诊断-设备IP和代码版本确认.md)
