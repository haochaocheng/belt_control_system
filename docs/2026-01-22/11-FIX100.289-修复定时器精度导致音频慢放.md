# FIX 100.289 - 修复定时器精度导致音频播放慢放

**时间**: 2026-01-22 14:30
**版本**: FIX 100.289
**状态**: ✅ 代码修改完成，等待测试验证

---

## 🎯 问题描述

**症状**：
- 音频前 2 秒正常，后面越来越慢，类似慢放效果
- 音频不流畅，出现延迟累积现象

**测试场景**：
- 播放停车音频（252 帧，预期 5040ms）
- 实际播放耗时 6854ms（延迟 36%）

---

## 🔍 问题诊断

### 1. Opus 编码验证

**日志分析**（voip.md）：
```
PCM 大小: 161894 字节
预计帧数: 252 帧（每帧 20ms）
✅ Opus 编码完成
   总帧数: 252 帧
```

**结论**：✅ Opus 编码正常，无丢帧

---

### 2. 发送时间戳分析

**日志中的时间戳**：
```
📡 已发送: 0 / 252 帧（ 134 ms）
📡 已发送: 50 / 252 帧（ 1233 ms）   ← 间隔 1099ms（应该 1000ms）
📡 已发送: 100 / 252 帧（ 2522 ms）  ← 间隔 1289ms（应该 1000ms）
📡 已发送: 150 / 252 帧（ 3873 ms）  ← 间隔 1351ms（应该 1000ms）
📡 已发送: 200 / 252 帧（ 5281 ms）  ← 间隔 1408ms（应该 1000ms）
📡 已发送: 250 / 252 帧（ 6802 ms）  ← 间隔 1521ms（应该 1000ms）
```

**延迟统计**：

| 帧段 | 实际间隔 | 预期间隔 | 延迟 | 累积延迟率 |
|------|---------|---------|------|-----------|
| 0-50 | 1099ms | 1000ms | +99ms | +9.9% |
| 50-100 | 1289ms | 1000ms | +289ms | +28.9% |
| 100-150 | 1351ms | 1000ms | +351ms | +35.1% |
| 150-200 | 1408ms | 1000ms | +408ms | +40.8% |
| 200-250 | 1521ms | 1000ms | +521ms | +52.1% |

**问题发现**：❌ **延迟逐渐累积，越来越严重**

---

### 3. Wireshark 抓包分析

**前 40 帧间隔统计**（wir.md）：
```
15.3ms, 16.6ms, 20.5ms, 24.2ms, 28.5ms, 30.1ms, 30.6ms, 31.3ms, 33.4ms
0.7ms, 1.6ms, 2.6ms  ← 连续发送（补偿延迟）
```

**发现的问题**：
1. **帧间隔抖动严重**：15ms - 33ms（预期 20ms）
2. **延迟累积**：越发越慢，最严重时达到 33ms/帧（比预期慢 65%）
3. **突发补偿**：累积延迟后，连续发送多帧（0.7ms 间隔）

**总耗时对比**：
- 预期：252 帧 × 20ms = **5040ms**
- 实际：**6854ms**
- **延迟 36%**

---

## 🎯 根本原因

**`QTimer` 定时器精度不够**：
- 即使使用 `Qt::PreciseTimer`，在某些平台（尤其是嵌入式 Linux）上仍然无法保证精确的 20ms 间隔
- 定时器触发延迟累积，导致后面越来越慢
- 系统负载（音频播放、UI 更新）会进一步影响定时器精度

**旧版实现问题**：
```cpp
// ❌ 旧版：使用周期性定时器，依赖 QTimer 精度
m_frameTimer->start(20);  // 期望每 20ms 触发一次

void onFrameTimerTimeout() {
    sendNextFrame();  // 但实际触发间隔不稳定
}
```

**累积延迟示例**：
```
第 1 帧：应该 20ms，实际 24ms → 延迟 4ms
第 2 帧：应该 40ms，实际 48ms → 延迟 8ms
第 3 帧：应该 60ms，实际 76ms → 延迟 16ms
...
第 250 帧：应该 5000ms，实际 6800ms → 延迟 1800ms
```

---

## 💡 解决方案

**方案：绝对时间戳控制** ⭐

使用绝对时间戳计算每帧发送时机，而不是依赖定时器间隔：

### 核心思想

```cpp
// ✅ 新版：绝对时间戳控制
qint64 m_sendStartTime;  // 记录发送开始的绝对时间

void sendNextFrame() {
    // 1. 发送当前帧
    sendUdpDatagram();

    // 2. 计算下一帧的绝对发送时间
    qint64 nextFrameAbsoluteTime = m_sendStartTime + m_currentFrameIndex * 20;
    qint64 currentTime = m_playbackTimer.elapsed();
    qint64 delay = nextFrameAbsoluteTime - currentTime;

    // 3. 使用 singleShot 调度下一帧
    if (delay > 0) {
        QTimer::singleShot(delay, this, &AudioNetworkSender::sendNextFrame);
    } else {
        QTimer::singleShot(0, this, &AudioNetworkSender::sendNextFrame);
    }
}
```

**优势**：
- ✅ 不依赖定时器间隔精度
- ✅ 自动补偿累积延迟
- ✅ CPU 占用低
- ✅ 实现简单

**示例**：
```
第 1 帧：应该 20ms，实际 24ms → 下一帧延迟 16ms（20*2 - 24）
第 2 帧：应该 40ms，实际 40ms → 恢复正常
第 3 帧：应该 60ms，实际 60ms → 稳定
```

---

## 💻 代码修改

### 1. AudioNetworkSender.h - 添加成员变量

**修改位置**: Line 242-253

**添加内容**：
```cpp
// --- 播放状态 ---
QList<QByteArray> m_currentFrames;
int m_currentFrameIndex;
QTimer* m_frameTimer;              ///< ❌ 2026-01-22 14:30 [已废弃] 周期性定时器（精度不够）
bool m_isPlaying;

// --- 统计信息 ---
QString m_currentFileName;
int m_totalFrames;
QElapsedTimer m_playbackTimer;
// ✅ 2026-01-22 14:30 [绝对时间戳控制] 发送开始的绝对时间
qint64 m_sendStartTime;            ///< 发送开始时间戳（ms）
```

---

### 2. AudioNetworkSender.cpp - 构造函数

**修改位置**: Line 35-49

**添加初始化**：
```cpp
AudioNetworkSender::AudioNetworkSender(QObject *parent)
    : QObject(parent)
    , m_udpSocket(nullptr)
    , m_multicastAddress()
    , m_multicastPort(8800)
    , m_opusEncoder(nullptr)
    , m_opusBitrate(16000)
    , m_currentFrameIndex(0)
    , m_frameTimer(nullptr)
    , m_isPlaying(false)
    , m_totalFrames(0)
    , m_sendStartTime(0)               // ✅ 2026-01-22 14:30 初始化发送开始时间
{
    // ...
}
```

---

### 3. AudioNetworkSender.cpp - 播放逻辑

**修改位置**: Line 178-196

**旧代码（注释）**：
```cpp
// ❌ 旧版：启动周期性定时器
m_isPlaying = true;
m_frameTimer->start(20);
```

**新代码**：
```cpp
// ✅ 2026-01-22 14:30 [绝对时间戳控制] 记录发送开始时间，改用 singleShot
m_isPlaying = true;
m_sendStartTime = m_playbackTimer.elapsed();  // 记录发送开始的绝对时间

// 立即发送第一帧（不等待定时器）
sendNextFrame();

qDebug() << "   ✅ 发送已启动（绝对时间戳控制模式）";
```

---

### 4. AudioNetworkSender.cpp - 发送逻辑（核心修改）

**修改位置**: Line 757-822

**完全重构 `sendNextFrame()` 函数**：

```cpp
void AudioNetworkSender::sendNextFrame()
{
    // ✅ 2026-01-22 14:30 [绝对时间戳控制] 完全重构发送逻辑

    // 检查是否已播放完成
    if (!m_isPlaying || m_currentFrameIndex >= m_currentFrames.size()) {
        qDebug() << "   ✅ 所有帧发送完成，总耗时:" << m_playbackTimer.elapsed() << "ms";
        stopPlayback();
        emit playbackFinished();
        return;
    }

    // ========== 发送当前帧 ==========
    qint64 currentTime = m_playbackTimer.elapsed();
    const QByteArray& opusFrame = m_currentFrames[m_currentFrameIndex];

    // 发送 UDP 组播
    qint64 bytesSent = m_udpSocket->writeDatagram(
        opusFrame.constData(),
        opusFrame.size(),
        m_multicastAddress,
        m_multicastPort
    );

    if (bytesSent < 0) {
        qWarning() << "❌ UDP 发送失败（帧" << m_currentFrameIndex << "）:" << m_udpSocket->errorString();
        emit networkError(m_udpSocket->errorString());
    }

    // ========== 计算下一帧的绝对发送时间 ==========
    m_currentFrameIndex++;  // 先递增索引

    // 计算下一帧应该发送的绝对时间戳
    // 公式：startTime + frameIndex * 20ms
    qint64 nextFrameAbsoluteTime = m_sendStartTime + m_currentFrameIndex * 20;
    qint64 delay = nextFrameAbsoluteTime - currentTime;

    // ========== 调试日志（每 50 帧打印一次）==========
    if ((m_currentFrameIndex - 1) % 50 == 0) {
        qDebug() << "   📡 已发送:" << (m_currentFrameIndex - 1) << "/" << m_totalFrames
                 << "帧（" << currentTime << "ms）";
        qDebug() << "      当前帧大小:" << opusFrame.size() << "字节";
        qDebug() << "      下一帧延迟:" << delay << "ms（绝对时间:" << nextFrameAbsoluteTime << "ms）";
    }

    // 触发进度信号
    emit playbackProgress(m_currentFrameIndex, m_totalFrames);

    // ========== 调度下一帧发送 ==========
    if (delay > 0) {
        // 延迟发送（正常情况）
        QTimer::singleShot(delay, this, &AudioNetworkSender::sendNextFrame);
    } else {
        // 已经延迟了，立即发送
        if (delay < -10) {
            qWarning() << "⚠️ 严重延迟（帧" << m_currentFrameIndex << "）: 已延迟" << (-delay) << "ms";
        }
        QTimer::singleShot(0, this, &AudioNetworkSender::sendNextFrame);
    }
}
```

**关键改进**：
1. **绝对时间戳计算**：`nextFrameAbsoluteTime = m_sendStartTime + m_currentFrameIndex * 20`
2. **自动补偿延迟**：如果定时器触发晚了，下一帧会提前发送
3. **递归调用**：使用 `QTimer::singleShot()` 替代周期性定时器
4. **延迟监控**：如果严重延迟（>10ms），输出警告日志

---

### 5. AudioNetworkSender.cpp - 停止逻辑

**修改位置**: Line 204-216

**旧代码（注释）**：
```cpp
// ❌ 旧版：停止周期性定时器
m_frameTimer->stop();
```

**新代码**：
```cpp
// ✅ 2026-01-22 14:30 [绝对时间戳控制] 只需设置状态，singleShot 会自动停止
m_isPlaying = false;
m_currentFrames.clear();
m_currentFrameIndex = 0;
```

---

### 6. AudioNetworkSender.cpp - 废弃旧函数

**修改位置**: Line 227-239

**标记为废弃**：
```cpp
void AudioNetworkSender::onFrameTimerTimeout()
{
    // ❌ 2026-01-22 14:30 [已废弃] 不再使用此函数，改用 sendNextFrame() 递归调用
    // 保留此函数仅为兼容性，实际不会被调用
    qWarning() << "⚠️ onFrameTimerTimeout() 被调用（此函数已废弃，不应被调用）";
    // ...
}
```

---

## 📊 预期效果

### 旧版问题

**时间戳抖动**（Wireshark）：
```
15ms, 16ms, 24ms, 28ms, 33ms, 0.7ms, 1.6ms, ...
```
- 间隔不稳定
- 延迟累积
- 突发补偿

### 新版预期

**稳定的 20ms 间隔**：
```
20ms, 20ms, 20ms, 20ms, 20ms, ...
```
- 间隔稳定（±2ms）
- 无累积延迟
- 无突发补偿

**总耗时对比**：
- 旧版：6854ms（延迟 36%）
- 新版：**~5060ms**（误差 <1%）

---

## ✅ 预期测试结果

### 1. 运行时日志

**位置**: `docs\log\voip.md`

**应显示**：
```
✅ 发送已启动（绝对时间戳控制模式）
📡 已发送: 0 / 252 帧（ 120 ms）
   下一帧延迟: 20 ms（绝对时间: 140 ms）
📡 已发送: 50 / 252 帧（ 1120 ms）
   下一帧延迟: 20 ms（绝对时间: 1140 ms）
📡 已发送: 100 / 252 帧（ 2120 ms）
   下一帧延迟: 20 ms（绝对时间: 2140 ms）
...
✅ 所有帧发送完成，总耗时: 5060 ms
```

**对比旧版**：
- 旧版：1099ms, 1289ms, 1351ms...（延迟累积）
- 新版：1120ms, 2120ms, 3120ms...（稳定 1000ms 间隔）

### 2. Wireshark 抓包

**新版过滤器**：
```
ip.dst == 224.1.10.1 && udp.port == 8800
```

**预期抓包结果**：
- 帧间隔：**稳定 20ms ± 2ms**
- 无延迟累积
- 无突发补偿
- 总时长：**~5060ms**（误差 <1%）

### 3. 音频播放测试

**测试场景**：
- 播放停车音频（252 帧，5 秒）
- 观察音频播放速度

**预期结果**：
- ✅ 音频播放流畅
- ✅ 无慢放效果
- ✅ 语速正常
- ✅ 时长准确（~5 秒）

---

## 📋 测试步骤

### 1. 编译部署（手动执行）

```powershell
.\build-ubuntu24-apt.ps1 192.168.10.188
```

### 2. 查看运行日志

```powershell
cat docs\log\voip.md
```

**检查项**：
- ✅ "发送已启动（绝对时间戳控制模式）"
- ✅ 帧间隔稳定（每 50 帧间隔约 1000ms）
- ✅ 总耗时约 5060ms（误差 <5%）
- ❌ 无 "⚠️ 严重延迟" 警告

### 3. Wireshark 抓包验证

**过滤器**：
```
ip.dst == 224.1.10.1 && udp.port == 8800
```

**验证项**：
- ✅ 源 IP: 192.168.10.188
- ✅ 目标 IP: 224.1.10.1
- ✅ 帧间隔: ~20ms（误差 ±2ms）
- ✅ 总包数: 252 帧
- ✅ 总时长: ~5060ms

### 4. 音频播放测试

**测试步骤**：
1. 触发停车音频播放
2. 听取音频内容
3. 确认语速正常

**验证项**：
- ✅ 音频清晰
- ✅ 语速正常（不快不慢）
- ✅ 无卡顿
- ✅ 时长约 5 秒

---

## 🔧 技术细节

### 绝对时间戳计算公式

```cpp
// 第 i 帧应该发送的绝对时间
nextFrameAbsoluteTime = m_sendStartTime + i * 20

// 当前时间
currentTime = m_playbackTimer.elapsed()

// 需要延迟的时间
delay = nextFrameAbsoluteTime - currentTime
```

**示例**：
```
发送开始时间 m_sendStartTime = 100ms

第 0 帧：应该 100ms，当前 120ms → 延迟 -20ms → 立即发送
第 1 帧：应该 120ms，当前 120ms → 延迟 0ms → 立即发送
第 2 帧：应该 140ms，当前 122ms → 延迟 18ms → 等待 18ms
第 3 帧：应该 160ms，当前 140ms → 延迟 20ms → 等待 20ms
...
```

### QTimer::singleShot() 递归调用

**优势**：
- 每次调用都是独立的，不会累积误差
- 可以根据实际情况动态调整延迟
- CPU 占用低（只在需要时唤醒）

**对比周期性定时器**：
```cpp
// ❌ 周期性定时器：固定间隔，无法补偿延迟
QTimer::start(20);  // 每次都是 20ms，误差累积

// ✅ singleShot：动态调整，自动补偿
QTimer::singleShot(delay, ...);  // delay 根据实际情况计算
```

---

## 🔗 相关文档

- [FIX100.288-动态组播地址计算实施完成](10-FIX100.288-动态组播地址计算实施完成.md)
- [组播地址动态计算验证成功](09-组播地址动态计算验证成功.md)
- [音频网络传输实施计划](../2026-01-21/12-音频网络传输实施计划-最终版.md)

---

**实施版本**: FIX 100.289
**实施时间**: 2026-01-22 14:30
**实施状态**: ✅ 代码完成，等待测试验证
**测试命令**: `.\build-ubuntu24-apt.ps1 192.168.10.188`
