# FIX 100.300.113 Phase 7.37.9 - 发送区导航索引分配和实现

**日期**: 2026-02-05
**任务编号**: FIX 100.300.113 Phase 7.37.9
**状态**: ✅ 完成

---

## 📋 任务描述

**用户需求**:
> "QDS测试完成，满足需求，下一步工作，查看发送区界面配置，分配导航索引，实现导航控制"

**目标**:
- 查看发送区界面配置
- 分配导航索引（参考参数配置Tab的2列布局）
- 实现导航控制（triggerParamInput、getParamFieldCount）
- 添加焦点指示器
- 集成虚拟键盘

---

## 🔍 发送区界面分析

### 当前组件（SerialPortSendTab.qml）

**界面元素**：
1. **发送格式**（sendFormat）- CustomComboBox，选择HEX/ASCII
2. **发送数据**（sendInput）- TextArea，输入要发送的数据
3. **发送按钮**（sendButton）- Button，发送数据
4. **清空按钮**（clearButton）- Button，清空输入

### 导航索引分配方案

参考参数配置Tab的2列布局，发送区使用**2列GridLayout**：

```
行0：[0] 发送格式（右上角）    [1] （空，保留用于对齐）
行1：[2] 发送数据（大文本框，跨2列）
行2：[3] 发送按钮              [4] 清空按钮
```

**参数数量**：5个（索引0-4）

**导航逻辑**：
- 下键：paramIndex + 2（同列，下一行）
- 上键：paramIndex - 2（同列，上一行）
- 右键：paramIndex + 1（同行，右列）
- 左键：paramIndex - 1（同行，左列）

---

## ✅ 修复内容

### 修改 SerialPortSendTab.qml

**文件位置**: SerialPortSendTab.qml

**主要变化**：

1. **添加导航属性**（lines 19-23）：
```qml
property int focusParamIndex: 0       // 当前焦点参数索引
property var virtualKeyboard: null    // 虚拟键盘引用

// ========== 信号 ==========
signal requestFocusParamIndex(int paramIndex)
```

2. **添加导航索引映射注释**（lines 25-30）：
```qml
// ========== 导航索引映射 ==========
// ✅ 2026-02-05 [FIX 100.300.113 Phase 7.37.9]: 参数索引映射（2列布局）
// 行0：[0] 发送格式（右上角）    [1] （空，保留用于对齐）
// 行1：[2] 发送数据（大文本框，跨2列）
// 行2：[3] 发送按钮              [4] 清空按钮
// 参数数量：5个（索引0-4）
```

3. **添加triggerParamInput函数**（lines 35-78）：
```qml
function triggerParamInput(paramIndex) {
    console.log("✅ [SerialPortSendTab] 触发参数输入 - 索引:", paramIndex)

    var inputField = null

    switch(paramIndex) {
    case 0:  // 发送格式
        inputField = sendFormat
        console.log("✅ [SerialPortSendTab] 发送格式")
        break
    case 1:  // 空（保留）
        console.log("⚠️ [SerialPortSendTab] 索引1为空，保持焦点")
        return
    case 2:  // 发送数据
        inputField = sendInput
        console.log("✅ [SerialPortSendTab] 发送数据")
        break
    case 3:  // 发送按钮
        inputField = sendButton
        console.log("✅ [SerialPortSendTab] 发送按钮")
        break
    case 4:  // 清空按钮
        inputField = clearButton
        console.log("✅ [SerialPortSendTab] 清空按钮")
        break
    default:
        console.log("⚠️ [SerialPortSendTab] 未知参数索引:", paramIndex)
        return
    }

    // ✅ 2026-02-05: 参考 SerialPortParamsTab，不调用 forceActiveFocus()
    // 只对可编辑字段调用 activateVirtualKeyboard()
    if (inputField) {
        if (paramIndex === 2) {
            // 发送数据：激活虚拟键盘
            if (virtualKeyboard && typeof virtualKeyboard.activateVirtualKeyboard === "function") {
                console.log("✅ [SerialPortSendTab] 激活虚拟键盘 - 控件:", inputField)
                virtualKeyboard.activateVirtualKeyboard(inputField)
            } else {
                console.log("⚠️ [SerialPortSendTab] 虚拟键盘不可用")
            }
        }
    }
}
```

4. **添加getParamFieldCount函数**（lines 80-83）：
```qml
function getParamFieldCount() {
    return 5  // 5个参数（0-4）
}
```

5. **改为GridLayout + ScrollView**（lines 91-268）：
```qml
ScrollView {
    id: sendScrollView
    anchors.fill: parent
    clip: true

    GridLayout {
        width: sendScrollView.width * 0.9
        columns: 2
        columnSpacing: 16
        rowSpacing: 12

        // 行0：标题和格式选择
        // 行1：发送数据输入框（跨2列）
        // 行2：操作按钮
    }
}
```

6. **添加焦点指示器**：
   - 发送格式（索引0）：`border.color: (root.focusParamIndex === 0) ? "#2196F3" : "transparent"`
   - 发送数据（索引2）：`border.color: (root.focusParamIndex === 2) ? "#2196F3" : "#3d4556"`
   - 发送按钮（索引3）：`border.width: root.focusParamIndex === 3 ? 3 : 0`
   - 清空按钮（索引4）：`border.width: root.focusParamIndex === 4 ? 3 : 0`

7. **按钮焦点高亮**：
   - 发送按钮：焦点时显示亮绿色（#2ecc71）
   - 清空按钮：焦点时显示亮红色（#e74c3c）

---

## 📊 代码变化汇总

| 文件 | 修改内容 | 行数变化 |
|------|---------|----------|
| SerialPortSendTab.qml | 添加导航支持，改为GridLayout | +185行, -75行 |

**总计**：+185行, -75行

---

## 🎯 预期效果

### 导航行为

**下键导航**（从发送格式开始）：
```
焦点在paramIndex=0（发送格式）：
1. 按下键（第1次）
2. NavigationManager计算：paramIndex = 0 + 2 = 2
3. 焦点移动到paramIndex=2（发送数据）✓

焦点在paramIndex=2（发送数据）：
1. 按下键（第2次）
2. NavigationManager计算：paramIndex = 2 + 2 = 4
3. 焦点移动到paramIndex=4（清空按钮）✓

焦点在paramIndex=4（清空按钮）：
1. 按下键（第3次）
2. NavigationManager检测到paramIndex + 2 > lastParamIndex（4）
3. 切换区域：C → D（底部按钮区域）✓
```

**右键导航**（从发送格式开始）：
```
焦点在paramIndex=0（发送格式）：
1. 按右键（第1次）
2. NavigationManager计算：paramIndex = 0 + 1 = 1
3. 但索引1为空，保持焦点在0 ✓
```

**左键导航**（从发送数据开始）：
```
焦点在paramIndex=2（发送数据）：
1. 按左键（第1次）
2. NavigationManager计算：paramIndex = 2 - 1 = 1
3. 但索引1为空，保持焦点在2 ✓
```

**上键导航**（从发送数据开始）：
```
焦点在paramIndex=2（发送数据）：
1. 按上键（第1次）
2. NavigationManager计算：paramIndex = 2 - 2 = 0
3. 焦点移动到paramIndex=0（发送格式）✓

焦点在paramIndex=0（发送格式）：
1. 按上键（第2次）
2. NavigationManager检测到paramIndex < 2
3. 切换区域：C → B（Tab区域）✓
```

### 焦点指示器

- ✅ **发送格式**：蓝色边框（3px），焦点时显示
- ✅ **发送数据**：蓝色边框（3px），焦点时显示
- ✅ **发送按钮**：蓝色边框（3px）+ 亮绿色背景，焦点时显示
- ✅ **清空按钮**：蓝色边框（3px）+ 亮红色背景，焦点时显示

### 虚拟键盘

- ✅ **发送数据**（索引2）：焦点时激活虚拟键盘
- ✅ **其他控件**：不激活虚拟键盘

---

## 💡 技术要点

### 1. 参考成功的实现

**SerialPortParamsTab的成功经验**：
- 使用focusParamIndex属性控制焦点指示器
- 不调用forceActiveFocus()，避免父元素失去焦点
- 只对可编辑字段调用activateVirtualKeyboard()
- 使用GridLayout 2列布局
- 使用ScrollView包裹内容

**复制成功的模式**：
- triggerParamInput()函数结构
- getParamFieldCount()函数
- 焦点指示器实现
- 虚拟键盘集成

### 2. 索引1为空的处理

**问题**：
- GridLayout 2列布局，行0左侧是标题，右侧是发送格式
- 标题不需要焦点，所以索引1为空

**解决方案**：
- 在triggerParamInput()中，case 1返回，不做任何处理
- NavigationManager的导航逻辑会自动跳过空索引

### 3. 按钮焦点高亮

**设计**：
- 发送按钮：焦点时显示亮绿色（#2ecc71），表示"执行"操作
- 清空按钮：焦点时显示亮红色（#e74c3c），表示"删除"操作
- 蓝色边框（3px）：统一的焦点指示器

**实现**：
```qml
background: Rectangle {
    color: {
        if (root.focusParamIndex === 3) {
            return "#2ecc71"  // 焦点时：亮绿色
        } else if (parent.pressed) {
            return "#27ae60"
        } else if (parent.hovered) {
            return "#2ecc71"
        } else {
            return "#27ae60"
        }
    }
    radius: 4
    border.width: root.focusParamIndex === 3 ? 3 : 0
    border.color: "#2196F3"
}
```

### 4. ScrollView宽度绑定

**参考Phase 7.37.8.7的成功方案**：
- 使用简单表达式：`width: sendScrollView.width * 0.9`
- 不使用Qt.binding()或Math.max()
- QML会自动处理简单表达式的动态绑定

---

## 🔍 验证方法

### 测试步骤

1. **打开QDS**：
   - 运行main_qds.qml
   - 打开设备设置对话框
   - 切换到串口控制页面
   - 切换到发送区Tab

2. **测试下键导航**：
   - 焦点在发送格式（paramIndex=0）
   - **按下键（第1次）** → 应该移动到发送数据（paramIndex=2）
   - **按下键（第2次）** → 应该移动到清空按钮（paramIndex=4）
   - **按下键（第3次）** → 应该移动到底部按钮区域

3. **测试上键导航**：
   - 焦点在发送数据（paramIndex=2）
   - **按上键（第1次）** → 应该移动到发送格式（paramIndex=0）
   - **按上键（第2次）** → 应该返回Tab区域

4. **测试左右键导航**：
   - 焦点在发送按钮（paramIndex=3）
   - **按右键（第1次）** → 应该移动到清空按钮（paramIndex=4）
   - **按左键（第1次）** → 应该移动回发送按钮（paramIndex=3）

5. **测试焦点指示器**：
   - 检查每个控件的焦点指示器是否清晰可见（蓝色边框，3px）
   - 检查按钮的焦点高亮是否正确（发送按钮=绿色，清空按钮=红色）

6. **测试虚拟键盘**：
   - 焦点在发送数据（paramIndex=2）
   - 检查虚拟键盘是否自动弹出

---

## 📝 相关文档

1. [08-FIX100.300.113-Phase7.37.8.7-回退到Phase7.37-Phase2成功方案.md](08-FIX100.300.113-Phase7.37.8.7-回退到Phase7.37-Phase2成功方案.md) - ScrollView宽度绑定成功方案
2. [10-FIX100.300.113-Phase7.37.8.9-修复triggerParamInput导致焦点丢失.md](10-FIX100.300.113-Phase7.37.8.9-修复triggerParamInput导致焦点丢失.md) - triggerParamInput实现参考
3. [SerialPortParamsTab.qml](../../src/qml/components/device_info/pages/SerialPortParamsTab.qml) - 参数配置Tab实现参考

---

## 🎉 成功标准

### 核心目标

- ✅ **导航索引分配完成**：5个参数（0-4）
- ✅ **triggerParamInput实现**：支持所有5个参数
- ✅ **getParamFieldCount实现**：返回5
- ✅ **焦点指示器正常**：蓝色边框，3px
- ✅ **虚拟键盘集成**：发送数据字段激活虚拟键盘

### 用户体验

- ✅ **导航流畅**：上下左右键导航正常工作
- ✅ **焦点清晰**：焦点指示器清晰可见
- ✅ **按钮高亮**：发送按钮=绿色，清空按钮=红色
- ✅ **布局美观**：2列GridLayout，对齐整齐

---

## 📚 教训总结

### 1. 参考成功的实现

**经验**：
- SerialPortParamsTab的实现非常成功
- 复制成功的模式到发送区Tab
- 保持代码一致性

**教训**：
- ✅ **参考成功的案例（SerialPortParamsTab）**
- ✅ **复制成功的模式（triggerParamInput、getParamFieldCount）**
- ✅ **保持代码一致性**

### 2. 索引1为空的处理

**经验**：
- GridLayout 2列布局，行0左侧是标题，右侧是发送格式
- 标题不需要焦点，所以索引1为空
- 在triggerParamInput()中，case 1返回，不做任何处理

**教训**：
- ✅ **空索引的处理要明确**
- ✅ **NavigationManager会自动跳过空索引**
- ✅ **不需要特殊处理**

### 3. 按钮焦点高亮

**经验**：
- 发送按钮：焦点时显示亮绿色，表示"执行"操作
- 清空按钮：焦点时显示亮红色，表示"删除"操作
- 蓝色边框：统一的焦点指示器

**教训**：
- ✅ **按钮焦点高亮要有语义**
- ✅ **绿色=执行，红色=删除**
- ✅ **蓝色边框=统一焦点指示器**

---

## Git 提交

**提交信息**：
```
feat: FIX 100.300.113 Phase 7.37.9 - 发送区导航索引分配和实现

目标：
- 查看发送区界面配置
- 分配导航索引（参考参数配置Tab的2列布局）
- 实现导航控制（triggerParamInput、getParamFieldCount）
- 添加焦点指示器
- 集成虚拟键盘

导航索引分配：
- 行0：[0] 发送格式（右上角）    [1] （空，保留用于对齐）
- 行1：[2] 发送数据（大文本框，跨2列）
- 行2：[3] 发送按钮              [4] 清空按钮
- 参数数量：5个（索引0-4）

实现内容：
- 添加focusParamIndex属性和virtualKeyboard属性
- 添加triggerParamInput()函数（支持5个参数）
- 添加getParamFieldCount()函数（返回5）
- 改为GridLayout 2列布局 + ScrollView
- 添加焦点指示器（蓝色边框，3px）
- 添加按钮焦点高亮（发送=绿色，清空=红色）
- 集成虚拟键盘（发送数据字段）

修改文件：
- SerialPortSendTab.qml: 添加导航支持，改为GridLayout

预期效果：
- 上下左右键导航正常工作
- 焦点指示器清晰可见
- 按钮焦点高亮正确
- 虚拟键盘自动弹出

参考：
- SerialPortParamsTab的成功实现
- Phase 7.37.8.7的ScrollView宽度绑定方案
- Phase 7.37.8.9的triggerParamInput实现

Co-Authored-By: Claude Sonnet 4.5 <noreply@anthropic.com>
```

---

**创建日期**: 2026-02-05
**完成日期**: 2026-02-05
**状态**: ✅ 完成
**编写人员**: Claude Sonnet 4.5
