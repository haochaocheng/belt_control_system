# 代码版本不匹配问题诊断

**时间**: 2026-01-21 22:00
**问题**: 编译时间戳更新，但诊断代码未执行

## 📊 关键证据

### 1. 编译确认
- ✅ pjsip.md Line 1: `.\build-ubuntu24-apt.ps1 192.168.1.8`
- ✅ pjsip.md Line 558: `Building CXX object src/audio_network/CMakeFiles/audio_network.dir/AudioNetworkSender.cpp.o`
- ✅ pjsip.md Line 1060: `Target device: pi@192.168.1.8`
- ✅ voip.md Line 34: `📦 Compiled at: Jan 21 2026 21:51:03`

### 2. 日志矛盾
- ✅ voip.md Line 417: `[DEBUG]    [1/3] 解码音频文件...`
  → 来自 AudioNetworkSender.cpp:143

- ✅ voip.md Line 419-532: `[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY`
  → 来自 FFmpeg avcodec.c 源码

- ❌ **缺失**: `[APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-21-21:45`
  → 应该来自 AudioNetworkSender.cpp:274

## 🔍 核心矛盾

```
AudioNetworkSender.cpp 代码顺序:
┌─────────────────────────────────────────────────┐
│ Line 143: qDebug() << "[1/3] 解码音频文件..."; │ ← ✅ 显示了
│ Line 144: AudioData audio = decodeAudioFile(); │
│                                                 │
│ decodeAudioFile() 函数内部:                    │
│   Line 273: av_log() VERSION 2026-01-21-21:45  │ ← ❌ 没显示
│   Line 281: avcodec_open2(codecCtx, ...)       │ ← ✅ 执行了(FFmpeg日志显示)
└─────────────────────────────────────────────────┘
```

**不可能的情况**: Line 273 在 Line 281 之前，但是 Line 273 没执行，Line 281 执行了！

## 🚨 可能原因

### 猜测1: 崩溃发生在 Line 273 之前
- **证据**: Line 266-268 `avcodec_parameters_to_context()` 调用
- **反驳**: 如果在 Line 268 之前崩溃，avcodec_open2() 就不会被调用

### 猜测2: 代码版本不匹配
- **部署设备**: `pi@192.168.1.8`
- **测试设备**: 可能是其他设备（用户说的"188"）
- **问题**: voip.md 来自哪个设备？

### 猜测3: main.cpp 时间戳来源不同
- **voip.md Line 33**: `🕐 Build timestamp: 2025-12-28 10:23`
- **voip.md Line 34**: `📦 Compiled at: Jan 21 2026 21:51:03`
- **Line 33 是旧的!** 说明可能是缓存的 main.cpp

## 🔧 诊断方案

### 方案1: 在函数入口添加日志
修改 AudioNetworkSender.cpp Line 230（函数开始）:

```cpp
AudioData AudioNetworkSender::decodeAudioFile(const QString& filePath) {
    // ========== [诊断代码] 函数入口标记 ==========
    av_log(nullptr, AV_LOG_ERROR, "\n");
    av_log(nullptr, AV_LOG_ERROR, "╔═════════════════════════════════════════════════════════════════════╗\n");
    av_log(nullptr, AV_LOG_ERROR, "║ [APP-VERSION-CHECK] decodeAudioFile() 入口                         ║\n");
    av_log(nullptr, AV_LOG_ERROR, "║ 代码版本: 2026-01-21-22:00                                          ║\n");
    av_log(nullptr, AV_LOG_ERROR, "║ 文件路径: %s                          ║\n", qPrintable(filePath));
    av_log(nullptr, AV_LOG_ERROR, "╚═════════════════════════════════════════════════════════════════════╝\n");
    av_log(nullptr, AV_LOG_ERROR, "\n");

    AVFormatContext* formatCtx = nullptr;
    AVCodecContext* codecCtx = nullptr;

    try {
        // ========== 1. 打开音频文件 ==========
        // ... 原有代码
```

**如果这个日志也不显示，说明执行的是完全不同的代码！**

### 方案2: 强制全量重编译
```powershell
# 删除所有缓存
Remove-Item -Recurse -Force build_rk3588
Remove-Item -Recurse -Force .cmake/api

# 重新编译和部署
.\build-ubuntu24-apt.ps1 192.168.10.188  # 确认设备IP
```

### 方案3: 检查设备IP匹配
```powershell
# 确认用户的设备IP是多少
# pjsip.md 显示部署到 192.168.1.8
# 但用户说的是 "188"（192.168.10.188？）
```

## 📝 下一步操作

1. **确认设备IP**: 用户说的"188"到底是哪个IP？
2. **添加函数入口日志**: 在 decodeAudioFile() 最开始添加版本标记
3. **检查 main.cpp 时间戳**: 为什么 Line 33 显示 2025-12-28？

## ⚠️ 关键问题

**为什么 Line 33 和 Line 34 的时间戳不一致？**
```
🕐 Build timestamp: 2025-12-28 10:23 (codec_info NULL check)  ← 旧的！
📦 Compiled at: Jan 21 2026 21:51:03                          ← 新的
```

这说明两个时间戳来自不同的代码文件！
- Line 33 可能来自 main.cpp 的缓存版本
- Line 34 来自 CMake 编译时生成的配置

**结论**: 可能部分文件使用了旧版本！
