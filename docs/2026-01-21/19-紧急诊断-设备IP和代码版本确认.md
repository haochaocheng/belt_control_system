# FIX 100.282 - 紧急诊断：设备IP和代码版本确认

**时间**: 2026-01-21 22:05
**问题**: 诊断代码未执行，怀疑代码版本不匹配
**版本**: VERSION 2026-01-21-22:00-ENTRY

---

## 🚨 关键问题发现

### 1. 两个不同的时间戳

voip.md 显示：
```
Line 33: 🕐 Build timestamp: 2025-12-28 10:23 (codec_info NULL check)
Line 34: 📦 Compiled at: Jan 21 2026 21:51:03
```

**矛盾**:
- Line 33 显示 **2025-12-28** ← 旧版本！
- Line 34 显示 **2026-01-21** ← 新版本

**结论**: 可能部分代码使用了缓存的旧版本。

### 2. 设备IP不匹配

- **pjsip.md**: 部署到 `192.168.1.8`
- **用户说的**: "188"（可能是 `192.168.10.188`？）

**疑问**: voip.md 来自哪个设备？

### 3. 诊断日志缺失

- ✅ voip.md Line 417: `[DEBUG]    [1/3] 解码音频文件...`
  → AudioNetworkSender.cpp:143 输出成功

- ✅ voip.md Line 419-532: `[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY`
  → FFmpeg avcodec.c 输出成功

- ❌ **缺失**: `[APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-21-21:45`
  → AudioNetworkSender.cpp:274 **未执行**

**不合理**:
- Line 143 的 qDebug() 执行了
- Line 274 的 av_log() 没执行
- Line 281 的 avcodec_open2() 调用了（FFmpeg 日志显示）

**代码顺序**:
```
Line 143: qDebug() << "[1/3] 解码音频文件...";  ← ✅ 执行了
Line 144: AudioData audio = decodeAudioFile(filePath);
    ↓
decodeAudioFile() 函数内部:
    Line 274: av_log() VERSION 2026-01-21-21:45  ← ❌ 未执行
    Line 281: avcodec_open2(codecCtx, ...)       ← ✅ 执行了
```

---

## 🔧 已实施的诊断代码

### 修改位置
[AudioNetworkSender.cpp](../../src/audio_network/AudioNetworkSender.cpp) Line 219-228

### 代码内容
```cpp
AudioNetworkSender::AudioData AudioNetworkSender::decodeAudioFile(const QString& filePath)
{
    // ❌ 2026-01-21 22:00 [诊断代码] 函数入口版本标记
    av_log(nullptr, AV_LOG_ERROR, "\n");
    av_log(nullptr, AV_LOG_ERROR, "╔═════════════════════════════════════════════════════════════════════╗\n");
    av_log(nullptr, AV_LOG_ERROR, "║ [APP-VERSION-CHECK] decodeAudioFile() 函数入口                     ║\n");
    av_log(nullptr, AV_LOG_ERROR, "║ 代码版本: 2026-01-21-22:00-ENTRY                                   ║\n");
    av_log(nullptr, AV_LOG_ERROR, "║ 文件: %-60s║\n", qPrintable(filePath));
    av_log(nullptr, AV_LOG_ERROR, "╚═════════════════════════════════════════════════════════════════════╝\n");
    av_log(nullptr, AV_LOG_ERROR, "\n");

    // ... 原有代码
}
```

### 预期输出
```
[1/3] 解码音频文件...
╔═════════════════════════════════════════════════════════════════════╗
║ [APP-VERSION-CHECK] decodeAudioFile() 函数入口                     ║
║ 代码版本: 2026-01-21-22:00-ENTRY                                   ║
║ 文件: /app/AUDIO/1#PD/1号皮带启动.mp3                               ║
╚═════════════════════════════════════════════════════════════════════╝

[APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-21-21:45
```

---

## 📋 下一步操作步骤

### ⚠️ 在执行前，请先确认以下信息：

1. **设备IP**:
   - pjsip.md 显示部署到 `192.168.1.8`
   - 您说的"188"是指哪个IP？
     - `192.168.10.188` ？
     - 还是其他IP？

2. **测试设备**:
   - voip.md 日志来自哪个设备？
   - 是 `192.168.1.8` 还是 `192.168.10.188`？

3. **设备确认**:
   ```powershell
   # 请确认要部署的设备IP
   $DEVICE_IP = "192.168.10.188"  # 或 192.168.1.8
   ```

### 步骤 1: 清理所有缓存
```powershell
# 删除所有构建缓存（确保完全重新编译）
Remove-Item -Recurse -Force build_rk3588 -ErrorAction SilentlyContinue
Remove-Item -Recurse -Force .cmake/api -ErrorAction SilentlyContinue

Write-Host "✅ 缓存已清理" -ForegroundColor Green
```

### 步骤 2: 重新编译和部署
```powershell
# 请确认设备IP！！！
$DEVICE_IP = "192.168.10.188"  # ← 请修改为正确的IP

.\build-ubuntu24-apt.ps1 $DEVICE_IP
```

### 步骤 3: 测试并查看日志
```powershell
# SSH到设备（使用正确的IP）
ssh pi@192.168.10.188 ./run-ubuntu24-apt.sh

# 测试完成后，下载日志
scp pi@192.168.10.188:/tmp/belt-control.log docs/log/voip.md
```

### 步骤 4: 检查关键日志

**查找以下标记**（按顺序）:

1. ✅ 编译时间戳（应该是最新的）:
   ```
   📦 Compiled at: Jan 21 2026 22:XX:XX
   ```

2. ✅ 起车预警触发:
   ```
   [DEBUG] [AudioNetworkSender] 播放音频到网络: "1号皮带启动.mp3"
   [DEBUG]    [1/3] 解码音频文件...
   ```

3. **🔑 关键诊断标记**（应该出现）:
   ```
   ╔═════════════════════════════════════════════════════════════════════╗
   ║ [APP-VERSION-CHECK] decodeAudioFile() 函数入口                     ║
   ║ 代码版本: 2026-01-21-22:00-ENTRY                                   ║
   ╚═════════════════════════════════════════════════════════════════════╝
   ```

4. **🔑 第二个诊断标记**（应该出现）:
   ```
   ╔═══════════════════════════════════════════════════════════════╗
   ║ [APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-21-21:45 ║
   ╚═══════════════════════════════════════════════════════════════╝
   ```

---

## 🔍 诊断场景分析

### 场景 A: 两个标记都出现 ✅
```
[APP-VERSION-CHECK] decodeAudioFile() 函数入口
代码版本: 2026-01-21-22:00-ENTRY

[APP-TRACE] avcodec_open2() 调用前 - VERSION 2026-01-21-21:45
[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY
```

**结论**:
- 代码版本正确
- 继续分析崩溃真正原因

**下一步**:
- 分析 FFmpeg 日志，定位崩溃点

---

### 场景 B: 只有第一个标记出现 ⚠️
```
[APP-VERSION-CHECK] decodeAudioFile() 函数入口
代码版本: 2026-01-21-22:00-ENTRY

[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY  ← 直接跳到这里
```

**结论**:
- 函数入口代码是新的
- Line 274-279 的代码被跳过或崩溃

**可能原因**:
- Line 266-273 之间某行代码崩溃
- 编译器优化导致代码被删除

**下一步**:
- 在 Line 266 之前添加诊断日志
- 逐行定位崩溃点

---

### 场景 C: 两个标记都没出现 ❌
```
[1/3] 解码音频文件...
[FFmpeg层] avcodec_open2() - ULTRA EARLY ENTRY  ← 直接跳到这里
```

**结论**:
- **代码版本完全不对！**
- 执行的是旧版本代码

**可能原因**:
1. 部署到了错误的设备
2. Docker 镜像缓存没有更新
3. 二进制文件被覆盖

**下一步**:
1. 确认设备IP
2. 删除设备上的旧镜像：
   ```bash
   ssh pi@$DEVICE_IP "docker rmi belt-control:v3.5-apt --force"
   ```
3. 重新部署

---

### 场景 D: 崩溃发生在函数入口之前 💥
```
[1/3] 解码音频文件...
Application exited with code: 139  ← 立即崩溃
```

**结论**:
- 崩溃发生在 `decodeAudioFile()` 被调用时
- 可能是函数参数问题

**可能原因**:
- `filePath` 参数损坏
- 栈溢出
- 对象已销毁

**下一步**:
- 在 Line 144 调用前添加日志：
  ```cpp
  qDebug() << "   [DEBUG] 准备调用 decodeAudioFile()...";
  qDebug() << "   [DEBUG] filePath 有效性:" << !filePath.isEmpty();
  AudioData audio = decodeAudioFile(filePath);
  ```

---

## 📊 总结

### 关键疑问
1. **设备IP不匹配**: pjsip.md 显示 192.168.1.8，用户说的是"188"
2. **时间戳矛盾**: Line 33 是旧的，Line 34 是新的
3. **诊断代码缺失**: Line 274-279 的 av_log() 未执行

### 诊断策略
- ✅ 添加函数入口版本标记（Line 219-228）
- ⏳ 等待测试结果确认代码版本
- ⏳ 根据测试结果定位真正崩溃点

### 下一步
1. **用户确认设备IP**
2. **清理缓存并重新编译**
3. **测试并查看日志**
4. **根据诊断场景分析结果**

---

**参考文档**:
- [18-代码版本不匹配问题诊断.md](18-代码版本不匹配问题诊断.md)
- [17-首次测试崩溃-使用av_log诊断方案.md](17-首次测试崩溃-使用av_log诊断方案.md)
- [16-首次测试崩溃-qDebug缓冲问题诊断修复.md](16-首次测试崩溃-qDebug缓冲问题诊断修复.md)
