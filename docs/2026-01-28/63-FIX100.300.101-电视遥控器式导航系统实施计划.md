# FIX 100.300.101 - 电视遥控器式导航系统实施计划

**日期**: 2026-01-28
**目标**: 实现完整的电视遥控器式导航系统（只用上下左右键，回车键弹出虚拟键盘）
**状态**: 实施中 🚧

---

## 🎯 用户需求

### 最终需求（用户明确）

1. **导航方式**：像电视遥控器操作APP一样
   - ✅ 只使用上下左右键导航
   - ❌ 不使用 Tab 键
   - ✅ 所有元素在一个平面上，焦点可以自由移动

2. **虚拟键盘触发**：
   - 当焦点在输入框上时，按回车键弹出虚拟键盘
   - 不自动弹出，不用鼠标点击

3. **焦点指示**：
   - 清晰的焦点边框或高亮
   - 用户能明确看到当前焦点在哪里

---

## 📋 当前状态分析

### 已完成 ✅

1. **焦点管理**（FIX 100.300.100）：
   - 对话框弹出时焦点正确转移
   - MouseArea 模态遮罩阻止主界面接收事件
   - FocusScope 创建独立焦点作用域

2. **虚拟键盘基础设施**（Phase 1-4）：
   - 48 个输入组件已集成 keyboardManager
   - VirtualKeyboardManager 单例模式
   - 自动键盘模式选择（数字/英文）

### 当前键盘事件处理（Line 93-136）

```qml
Keys.onUpPressed: {
    // 上键：选择上一个类别
    if (root.currentCategory > 0) {
        root.currentCategory--
        root.currentBottomButtonIndex = 0
    }
}

Keys.onDownPressed: {
    // 下键：选择下一个类别
    if (root.currentCategory < 6) {
        root.currentCategory++
        root.currentBottomButtonIndex = 0
    }
}

Keys.onLeftPressed: {
    // 左键：选择上一个底部按钮
    var bottomButtons = getBottomButtons(root.currentCategory)
    if (bottomButtons.length > 0 && root.currentBottomButtonIndex > 0) {
        root.currentBottomButtonIndex--
    }
}

Keys.onRightPressed: {
    // 右键：选择下一个底部按钮
    var bottomButtons = getBottomButtons(root.currentCategory)
    if (bottomButtons.length > 0 && root.currentBottomButtonIndex < bottomButtons.length - 1) {
        root.currentBottomButtonIndex++
    }
}

Keys.onReturnPressed: {
    // 回车键：触发当前选中的底部按钮
    var bottomButtons = getBottomButtons(root.currentCategory)
    if (bottomButtons.length > 0 && root.currentBottomButtonIndex < bottomButtons.length) {
        console.log("触发底部按钮:", bottomButtons[root.currentBottomButtonIndex])
    }
}
```

### 问题分析 ❌

1. **导航逻辑不符合需求**：
   - 当前：上下键切换类别，左右键切换底部按钮
   - 需求：左右键在类别和输入区域之间切换，上下键在当前区域内导航

2. **缺少输入区域焦点管理**：
   - 当前只能导航到类别和底部按钮
   - 无法导航到右侧的输入框

3. **回车键功能不正确**：
   - 当前：触发底部按钮
   - 需求：当焦点在输入框时弹出虚拟键盘

4. **缺少焦点指示器**：
   - 用户无法看到当前焦点在哪里

---

## 🔧 实施方案

### 方案架构

**三区域导航模型**：

```
┌─────────────────────────────────────────────────────┐
│  顶部按钮区域（关闭、保存、重置）                      │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│  左侧    │  右侧内容区域                             │
│  类别    │  （输入框、下拉框、SpinBox等）             │
│  列表    │                                          │
│          │                                          │
│          │                                          │
├──────────┴──────────────────────────────────────────┤
│  底部按钮区域（保存配置、恢复默认等）                  │
└─────────────────────────────────────────────────────┘
```

**焦点区域定义**：

1. **区域 0**：顶部按钮区域（关闭、保存、重置）
2. **区域 1**：左侧类别列表（基本配置、开关量输入等）
3. **区域 2**：右侧内容区域（输入框、下拉框等）
4. **区域 3**：底部按钮区域（保存配置、恢复默认等）

### 导航规则

#### 1. 左右键导航（区域切换）

- **右键**：
  - 区域 0 → 区域 1（顶部按钮 → 左侧类别）
  - 区域 1 → 区域 2（左侧类别 → 右侧内容）
  - 区域 2 → 区域 3（右侧内容 → 底部按钮）
  - 区域 3 → 区域 1（底部按钮 → 左侧类别，循环）

- **左键**：
  - 区域 1 → 区域 0（左侧类别 → 顶部按钮）
  - 区域 2 → 区域 1（右侧内容 → 左侧类别）
  - 区域 3 → 区域 2（底部按钮 → 右侧内容）
  - 区域 0 → 区域 3（顶部按钮 → 底部按钮，循环）

#### 2. 上下键导航（区域内导航）

- **区域 0**（顶部按钮）：
  - 上下键在 3 个按钮之间切换（关闭、保存、重置）

- **区域 1**（左侧类别）：
  - 上下键在 7 个类别之间切换

- **区域 2**（右侧内容）：
  - 上下键在当前页面的输入组件之间切换
  - 需要每个页面提供可聚焦组件列表

- **区域 3**（底部按钮）：
  - 上下键在底部按钮之间切换

#### 3. 回车键功能

- **区域 0**（顶部按钮）：触发按钮点击
- **区域 1**（左侧类别）：切换到该类别
- **区域 2**（右侧内容）：
  - **如果焦点在输入框**：弹出虚拟键盘 ✅
  - **如果焦点在按钮**：触发按钮点击
- **区域 3**（底部按钮）：触发按钮点击

---

## 📝 实施步骤

### Step 1: 添加焦点区域状态管理

**修改位置**：`DeviceSettingsDialog.qml` Line 60-65

**添加属性**：

```qml
// ========== 焦点区域管理 ==========
// ✅ 2026-01-28 [FIX 100.300.101]: 电视遥控器式导航系统
property int currentFocusArea: 1  // 当前焦点区域 (0:顶部 1:左侧类别 2:右侧内容 3:底部)
property int currentTopButtonIndex: 0  // 顶部按钮索引 (0:关闭 1:保存 2:重置)
property int currentContentItemIndex: 0  // 右侧内容区域当前焦点项索引
```

### Step 2: 修改键盘事件处理

**修改位置**：`DeviceSettingsDialog.qml` Line 93-136

**新的键盘事件处理**：

```qml
Keys.onLeftPressed: {
    // 左键：切换到左侧区域
    console.log("✅ [导航] 左键 - 当前区域:", currentFocusArea)

    switch(currentFocusArea) {
    case 0:  // 顶部按钮 → 底部按钮（循环）
        currentFocusArea = 3
        break
    case 1:  // 左侧类别 → 顶部按钮
        currentFocusArea = 0
        break
    case 2:  // 右侧内容 → 左侧类别
        currentFocusArea = 1
        break
    case 3:  // 底部按钮 → 右侧内容
        currentFocusArea = 2
        break
    }

    console.log("✅ [导航] 左键后区域:", currentFocusArea)
}

Keys.onRightPressed: {
    // 右键：切换到右侧区域
    console.log("✅ [导航] 右键 - 当前区域:", currentFocusArea)

    switch(currentFocusArea) {
    case 0:  // 顶部按钮 → 左侧类别
        currentFocusArea = 1
        break
    case 1:  // 左侧类别 → 右侧内容
        currentFocusArea = 2
        currentContentItemIndex = 0  // 重置内容区域索引
        break
    case 2:  // 右侧内容 → 底部按钮
        currentFocusArea = 3
        break
    case 3:  // 底部按钮 → 左侧类别（循环）
        currentFocusArea = 1
        break
    }

    console.log("✅ [导航] 右键后区域:", currentFocusArea)
}

Keys.onUpPressed: {
    // 上键：在当前区域内向上导航
    console.log("✅ [导航] 上键 - 当前区域:", currentFocusArea)

    switch(currentFocusArea) {
    case 0:  // 顶部按钮
        if (currentTopButtonIndex > 0) {
            currentTopButtonIndex--
        }
        break
    case 1:  // 左侧类别
        if (currentCategory > 0) {
            currentCategory--
        }
        break
    case 2:  // 右侧内容
        if (currentContentItemIndex > 0) {
            currentContentItemIndex--
        }
        break
    case 3:  // 底部按钮
        if (currentBottomButtonIndex > 0) {
            currentBottomButtonIndex--
        }
        break
    }
}

Keys.onDownPressed: {
    // 下键：在当前区域内向下导航
    console.log("✅ [导航] 下键 - 当前区域:", currentFocusArea)

    switch(currentFocusArea) {
    case 0:  // 顶部按钮（3个按钮：关闭、保存、重置）
        if (currentTopButtonIndex < 2) {
            currentTopButtonIndex++
        }
        break
    case 1:  // 左侧类别（7个类别）
        if (currentCategory < 6) {
            currentCategory++
        }
        break
    case 2:  // 右侧内容
        var maxIndex = getContentItemCount(currentCategory)
        if (currentContentItemIndex < maxIndex - 1) {
            currentContentItemIndex++
        }
        break
    case 3:  // 底部按钮
        var bottomButtons = getBottomButtons(currentCategory)
        if (currentBottomButtonIndex < bottomButtons.length - 1) {
            currentBottomButtonIndex++
        }
        break
    }
}

Keys.onReturnPressed: {
    // 回车键：根据当前区域执行不同操作
    console.log("✅ [导航] 回车键 - 当前区域:", currentFocusArea)

    switch(currentFocusArea) {
    case 0:  // 顶部按钮：触发按钮点击
        triggerTopButton(currentTopButtonIndex)
        break
    case 1:  // 左侧类别：切换类别（已自动切换，无需额外操作）
        console.log("✅ [导航] 当前类别:", getCategoryName(currentCategory))
        break
    case 2:  // 右侧内容：弹出虚拟键盘（如果是输入框）
        triggerContentItem(currentCategory, currentContentItemIndex)
        break
    case 3:  // 底部按钮：触发按钮点击
        var bottomButtons = getBottomButtons(currentCategory)
        if (currentBottomButtonIndex < bottomButtons.length) {
            console.log("✅ [导航] 触发底部按钮:", bottomButtons[currentBottomButtonIndex])
        }
        break
    }
}
```

### Step 3: 添加辅助函数

**修改位置**：`DeviceSettingsDialog.qml` Line 707 之后

**新增函数**：

```qml
// ✅ 2026-01-28 [FIX 100.300.101]: 获取右侧内容区域的可聚焦项数量
function getContentItemCount(categoryIndex) {
    switch(categoryIndex) {
    case 0:  // 基本配置
        return 5  // 示例：5个输入项
    case 1:  // 开关量输入
        return 9  // 9个输入组件
    case 2:  // 模拟量输入
        return 13  // 13个输入组件
    case 3:  // 电机控制
        return 3  // 基本配置 Tab 有 3 个 SpinBox
    case 4:  // 制动器控制
        return 9  // 9个输入组件
    case 5:  // 张紧控制
        return 14  // 14个输入组件
    case 6:  // 逻辑控制
        return 0  // 待实现
    default:
        return 0
    }
}

// ✅ 2026-01-28 [FIX 100.300.101]: 触发顶部按钮
function triggerTopButton(buttonIndex) {
    switch(buttonIndex) {
    case 0:  // 关闭
        console.log("✅ [导航] 触发：关闭")
        root.visible = false
        break
    case 1:  // 保存
        console.log("✅ [导航] 触发：保存")
        // 保存逻辑（待实现）
        break
    case 2:  // 重置
        console.log("✅ [导航] 触发：重置")
        // 重置逻辑（待实现）
        break
    }
}

// ✅ 2026-01-28 [FIX 100.300.101]: 触发右侧内容区域的项
function triggerContentItem(categoryIndex, itemIndex) {
    console.log("✅ [导航] 触发内容项 - 类别:", categoryIndex, "索引:", itemIndex)

    // 通知当前页面触发指定索引的输入组件
    // 每个页面需要实现 triggerInputItem(index) 方法
    var currentPage = getCurrentPage(categoryIndex)
    if (currentPage && typeof currentPage.triggerInputItem === "function") {
        currentPage.triggerInputItem(itemIndex)
    } else {
        console.warn("⚠️ [导航] 当前页面未实现 triggerInputItem 方法")
    }
}

// ✅ 2026-01-28 [FIX 100.300.101]: 获取当前页面实例
function getCurrentPage(categoryIndex) {
    switch(categoryIndex) {
    case 0:
        return basicConfigPageLoader.item
    case 1:
        return switchInputPageLoader.item
    case 2:
        return analogInputPageLoader.item
    case 3:
        return motorControlPageLoader.item
    case 4:
        return brakeControlPageLoader.item
    case 5:
        return tensionControlPageLoader.item
    case 6:
        return null  // 逻辑控制待实现
    default:
        return null
    }
}
```

### Step 4: 添加焦点指示器

**修改位置**：各个按钮和输入组件

**顶部按钮焦点指示**（Line 206-287）：

```qml
Button {
    text: "关闭"
    // ...
    background: Rectangle {
        color: "transparent"
        border.color: (currentFocusArea === 0 && currentTopButtonIndex === 0) ? "#2196F3" : "#3d4556"
        border.width: (currentFocusArea === 0 && currentTopButtonIndex === 0) ? 3 : 2
        radius: 2
    }
}
```

**左侧类别焦点指示**（Line 338-410）：

```qml
Button {
    // ...
    background: Rectangle {
        color: "transparent"
        border.color: (currentFocusArea === 1 && root.currentCategory === index) ? "#2196F3" : "#3d4556"
        border.width: (currentFocusArea === 1 && root.currentCategory === index) ? 3 : 1
        radius: 2
    }
}
```

**底部按钮焦点指示**（Line 679-701）：

```qml
Button {
    // ...
    background: Rectangle {
        color: (currentFocusArea === 3 && root.currentBottomButtonIndex === index) ? "#00AA00" : "#555555"
        border.color: (currentFocusArea === 3 && root.currentBottomButtonIndex === index) ? "#2196F3" : "#888888"
        border.width: (currentFocusArea === 3 && root.currentBottomButtonIndex === index) ? 3 : 1
        radius: 4
    }
}
```

### Step 5: 修改各个页面支持导航

**需要修改的页面**：

1. `SwitchInputPage.qml` - 9个输入组件
2. `AnalogInputPage.qml` - 13个输入组件
3. `BasicConfigTab.qml` - 3个 SpinBox
4. `BrakeControlPage.qml` - 9个输入组件
5. `TensionControlPage.qml` - 14个输入组件

**每个页面需要添加**：

```qml
// ✅ 2026-01-28 [FIX 100.300.101]: 导航支持
property int currentFocusIndex: 0  // 当前焦点索引

// ✅ 2026-01-28 [FIX 100.300.101]: 触发指定索引的输入组件
function triggerInputItem(index) {
    console.log("✅ [页面导航] 触发输入项:", index)

    // 根据索引弹出虚拟键盘
    var inputComponent = getInputComponent(index)
    if (inputComponent && inputComponent.keyboardManager) {
        inputComponent.keyboardManager.showKeyboard(inputComponent)
    }
}

// ✅ 2026-01-28 [FIX 100.300.101]: 获取指定索引的输入组件
function getInputComponent(index) {
    // 返回对应索引的输入组件引用
    // 每个页面根据自己的布局实现
}
```

---

## 🧪 测试计划

### 测试步骤

1. **区域切换测试**：
   - [ ] 左右键在 4 个区域之间切换
   - [ ] 焦点指示器正确显示当前区域

2. **区域内导航测试**：
   - [ ] 顶部按钮：上下键在 3 个按钮之间切换
   - [ ] 左侧类别：上下键在 7 个类别之间切换
   - [ ] 右侧内容：上下键在输入组件之间切换
   - [ ] 底部按钮：上下键在按钮之间切换

3. **回车键功能测试**：
   - [ ] 顶部按钮：回车键触发按钮点击
   - [ ] 左侧类别：回车键切换类别
   - [ ] 右侧内容：回车键弹出虚拟键盘（输入框）
   - [ ] 底部按钮：回车键触发按钮点击

4. **焦点指示器测试**：
   - [ ] 焦点边框清晰可见
   - [ ] 焦点切换时边框正确更新

5. **虚拟键盘测试**：
   - [ ] 焦点在输入框时按回车键弹出键盘
   - [ ] 键盘模式自动选择（数字/英文）
   - [ ] 键盘输入数据正确

---

## 📊 预期结果

修复后：

- ✅ 左右键在 4 个区域之间切换（顶部、左侧、右侧、底部）
- ✅ 上下键在当前区域内导航
- ✅ 回车键弹出虚拟键盘（焦点在输入框时）
- ✅ 焦点指示器清晰可见
- ✅ 完全不需要 Tab 键
- ✅ 像电视遥控器一样操作

---

## 📝 实施顺序

1. **Phase 1**：修改 DeviceSettingsDialog.qml 的键盘事件处理 ✅
2. **Phase 2**：添加焦点指示器 ✅
3. **Phase 3**：修改各个页面支持导航（5个页面）
4. **Phase 4**：QDS 测试验证
5. **Phase 5**：设备部署测试

---

## 🔗 相关文档

- [FIX 100.300.100 - 模态遮罩方案最终修复](./62-FIX100.300.100-模态遮罩方案最终修复.md)
- [虚拟键盘实施完成总结](./57-虚拟键盘实施完成总结-Phase1-4全部完成.md)
- [QDS 快速测试指南](./60-QDS快速测试指南-虚拟键盘功能.md)
