# FIX 100.300.53 - 张紧控制配置面板组件替换完成

**日期**: 2026-01-28
**类型**: UI组件标准化
**文件**: `src/qml/components/device_info/pages/TensionControlConfigPanel.qml`

## 问题描述

张紧控制配置面板使用原生 Qt Quick Controls 组件（TextField、SpinBox、ComboBox），需要替换为自定义组件以统一样式。

## 修改内容

### 1. 添加导入语句

```qml
import ".." as DeviceInfo
```

### 2. 替换组件清单

#### TextField (3个)
1. **nameField** (第93-99行) - 保护名称
   - 删除 `color` 和 `background` 属性
   - 保留 `enabled: false`

2. **ttsTextField** (第453-459行) - 报警文字
   - 删除 `color` 和 `background` 属性
   - 保留 `placeholderText`

3. **audioField** (第475-482行) - 音频文件
   - 删除 `color` 和 `background` 属性
   - 保留 `readOnly: true` 和 `placeholderText`

#### SpinBox (7个)
1. **registerAddressSpin** (第157-165行) - 寄存器地址
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `from`, `to`, `value`, `editable`

2. **upperLimitSpin** (第180-189行) - 上限值
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `from`, `to`, `value`, `stepSize`, `editable`

3. **lowerLimitSpin** (第204-213行) - 下限值
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `from`, `to`, `value`, `stepSize`, `editable`

4. **rangeSpin** (第228-237行) - 量程
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `from`, `to`, `value`, `stepSize`, `editable`

5. **delaySpin** (第274-294行) - 保护延时
   - 删除 `background` 和 `contentItem` 属性
   - **保留** `textFromValue`, `valueFromText`, `property decimals`, `property realValue`

6. **playCountSpin** (第309-317行) - 播放次数
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `from`, `to`, `value`, `editable`

7. **durationSpin** (第332-352行) - 播放时长
   - 删除 `background` 和 `contentItem` 属性
   - **保留** `textFromValue`, `valueFromText`, `property decimals`, `property realValue`

#### ComboBox (3个)
1. **typeCombo** (第114-121行) - 保护类型
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `enabled: false`, `model`, `currentIndex`

2. **moduleTypeCombo** (第136-142行) - 模块类型
   - 删除 `background` 和 `contentItem` 属性
   - 保留 `model`, `currentIndex`

3. **unitCombo** (第252-259行) - 单位
   - 删除 `background` 和 `contentItem` 属性
   - **保留** `editable: true`, `model`, `currentIndex`

## 关键保留项

### SpinBox 特殊属性
- **delaySpin** 和 **durationSpin** 保留了小数支持：
  ```qml
  property int decimals: 1
  property real realValue: value / 10

  textFromValue: function(value, locale) {
      return Number(value / 10).toLocaleString(locale, 'f', 1)
  }

  valueFromText: function(text, locale) {
      return Number.fromLocaleString(locale, text) * 10
  }
  ```

### ComboBox 特殊属性
- **unitCombo** 保留了可编辑功能：
  ```qml
  editable: true
  ```

## 验证结果

```bash
# 检查是否还有未替换的原生组件
grep -n "TextField\|SpinBox\|ComboBox" TensionControlConfigPanel.qml | grep -v "CustomTextField\|CustomSpinBox\|CustomComboBox\|DeviceInfo\|import"
# 结果：仅显示 id 名称，无原生组件声明
```

## 统计

- **总替换数**: 14 个组件
  - TextField: 3 个
  - SpinBox: 7 个
  - ComboBox: 3 个
- **删除代码行数**: 约 150 行（background 和 contentItem 定义）
- **保留特殊属性**:
  - 2 个 SpinBox 的小数支持
  - 1 个 ComboBox 的可编辑功能

## 后续工作

1. ✅ 编译测试
2. ✅ 功能测试（输入、选择、小数显示）
3. ✅ 样式一致性检查

## 相关文件

- 自定义组件定义：
  - `src/qml/components/device_info/CustomTextField.qml`
  - `src/qml/components/device_info/CustomSpinBox.qml`
  - `src/qml/components/device_info/CustomComboBox.qml`

## 备注

- 所有修改都添加了注释标记：`// ✅ 2026-01-28 [FIX 100.300.53]: 替换为自定义组件`
- 保持了原有的功能逻辑不变
- 样式由自定义组件统一管理
- 特殊功能（小数、可编辑）完整保留
