# FIX 100.300.79 - 开关量输入页面组件替换完成

**日期**: 2026-01-28
**类型**: UI 组件统一
**状态**: ✅ 完成

## 问题描述

需要将 `SwitchInputPage.qml` 中的所有原生输入组件替换为自定义组件，以保持整个应用的 UI 风格统一。

## 修改内容

### 1. 添加 DeviceInfo 模块导入

**文件**: `src/qml/components/device_info/pages/SwitchInputPage.qml`

```qml
import QtQuick 2.15
import QtQuick.Controls 2.15
import QtQuick.Layouts 1.15
import ".." as DeviceInfo  // ✅ 新增
```

### 2. 替换所有 TextField 为 CustomTextField

共替换 **3 个** TextField：

#### 2.1 保护名称输入框（第 273-277 行）
```qml
// ✅ 替换前：原生 TextField + 自定义样式
TextField {
    id: nameField
    Layout.fillWidth: true
    font.pixelSize: 14
    background: Rectangle { ... }
    color: "#E0E0E0"
}

// ✅ 替换后：CustomTextField（样式已内置）
DeviceInfo.CustomTextField {
    id: nameField
    Layout.fillWidth: true
}
```

#### 2.2 TTS 文字输入框（第 567-571 行）
```qml
// ✅ 替换前
TextField {
    id: ttsTextField
    placeholderText: "输入报警文字内容..."
    Layout.fillWidth: true
    font.pixelSize: 12
    background: Rectangle { ... }
    color: "#ecf0f1"
}

// ✅ 替换后
DeviceInfo.CustomTextField {
    id: ttsTextField
    placeholderText: "输入报警文字内容..."
    Layout.fillWidth: true
}
```

#### 2.3 音频文件路径输入框（第 588-593 行）
```qml
// ✅ 替换前
TextField {
    id: audioField
    placeholderText: "选择音频文件..."
    Layout.fillWidth: true
    readOnly: true
    font.pixelSize: 12
    background: Rectangle { ... }
    color: "#ecf0f1"
}

// ✅ 替换后
DeviceInfo.CustomTextField {
    id: audioField
    placeholderText: "选择音频文件..."
    Layout.fillWidth: true
    readOnly: true
}
```

### 3. 替换所有 SpinBox 为 CustomSpinBox

共替换 **5 个** SpinBox：

#### 3.1 寄存器地址（第 330-336 行）
```qml
// ✅ 替换前：原生 SpinBox + 自定义样式
SpinBox {
    id: registerAddressSpin
    from: 0
    to: 255
    editable: true
    Layout.fillWidth: true
    background: Rectangle { ... }
    contentItem: TextInput { ... }
}

// ✅ 替换后：CustomSpinBox（样式已内置）
DeviceInfo.CustomSpinBox {
    id: registerAddressSpin
    from: 0
    to: 255
    editable: true
    Layout.fillWidth: true
}
```

#### 3.2 通道编号（第 352-358 行）
```qml
// ✅ 替换前
SpinBox {
    id: channelSpin
    from: 0
    to: 7
    editable: true
    Layout.fillWidth: true
    background: Rectangle { ... }
    contentItem: TextInput { ... }
}

// ✅ 替换后
DeviceInfo.CustomSpinBox {
    id: channelSpin
    from: 0
    to: 7
    editable: true
    Layout.fillWidth: true
}
```

#### 3.3 保护延时（第 381-400 行）
```qml
// ✅ 替换前
SpinBox {
    id: delaySpin
    from: 0
    to: 600
    value: 10
    stepSize: 1
    editable: true
    Layout.fillWidth: true

    property int decimals: 1
    property real realValue: value / 10

    textFromValue: function(value, locale) { ... }
    valueFromText: function(text, locale) { ... }

    background: Rectangle { ... }
    contentItem: TextInput { ... }
}

// ✅ 替换后（保留自定义属性和函数）
DeviceInfo.CustomSpinBox {
    id: delaySpin
    from: 0
    to: 600
    value: 10
    stepSize: 1
    editable: true
    Layout.fillWidth: true

    property int decimals: 1
    property real realValue: value / 10

    textFromValue: function(value, locale) { ... }
    valueFromText: function(text, locale) { ... }
}
```

#### 3.4 播放次数（第 416-423 行）
```qml
// ✅ 替换前
SpinBox {
    id: playCountSpin
    from: 1
    to: 99
    value: 3
    editable: true
    Layout.fillWidth: true
    background: Rectangle { ... }
    contentItem: TextInput { ... }
}

// ✅ 替换后
DeviceInfo.CustomSpinBox {
    id: playCountSpin
    from: 1
    to: 99
    value: 3
    editable: true
    Layout.fillWidth: true
}
```

#### 3.5 播放时长（第 439-458 行）
```qml
// ✅ 替换前
SpinBox {
    id: durationSpin
    from: 1
    to: 600
    value: 50
    stepSize: 5
    editable: true
    Layout.fillWidth: true

    property int decimals: 1
    property real realValue: value / 10

    textFromValue: function(value, locale) { ... }
    valueFromText: function(text, locale) { ... }

    background: Rectangle { ... }
    contentItem: TextInput { ... }
}

// ✅ 替换后（保留自定义属性和函数）
DeviceInfo.CustomSpinBox {
    id: durationSpin
    from: 1
    to: 600
    value: 50
    stepSize: 5
    editable: true
    Layout.fillWidth: true

    property int decimals: 1
    property real realValue: value / 10

    textFromValue: function(value, locale) { ... }
    valueFromText: function(text, locale) { ... }
}
```

### 4. 替换 ComboBox 为 CustomComboBox

共替换 **1 个** ComboBox：

#### 4.1 模块类型选择（第 293-313 行）
```qml
// ✅ 替换前：原生 ComboBox + 自定义样式
ComboBox {
    id: moduleTypeCombo
    Layout.fillWidth: true

    model: ["输入模块1", "输入模块2", "输入模块3", "输入模块4", "输出模块", "主模块"]

    background: Rectangle { ... }
    contentItem: Text { ... }

    onCurrentTextChanged: {
        // 根据模块类型自动设置寄存器地址
        if (currentText === "输入模块1") {
            registerAddressSpin.value = 2
        } else if (currentText === "输入模块2") {
            registerAddressSpin.value = 3
        }
        // ...
    }
}

// ✅ 替换后：CustomComboBox（样式已内置）
DeviceInfo.CustomComboBox {
    id: moduleTypeCombo
    Layout.fillWidth: true

    model: ["输入模块1", "输入模块2", "输入模块3", "输入模块4", "输出模块", "主模块"]

    onCurrentTextChanged: {
        // 根据模块类型自动设置寄存器地址
        if (currentText === "输入模块1") {
            registerAddressSpin.value = 2
        } else if (currentText === "输入模块2") {
            registerAddressSpin.value = 3
        }
        // ...
    }
}
```

### 5. 更新 qmldir 注册文件

**文件**: `src/qml/components/device_info/qmldir`

```qml
singleton IndustrialTheme 1.0 IndustrialTheme.qml
CustomComboBox 1.0 CustomComboBox.qml
CustomTextField 1.0 CustomTextField.qml  // ✅ 新增
CustomSpinBox 1.0 CustomSpinBox.qml      // ✅ 新增
```

## 替换统计

| 组件类型 | 替换数量 | 行数减少 |
|---------|---------|---------|
| TextField → CustomTextField | 3 | ~45 行 |
| SpinBox → CustomSpinBox | 5 | ~120 行 |
| ComboBox → CustomComboBox | 1 | ~20 行 |
| **总计** | **9** | **~185 行** |

## 关键改进

### 1. 代码简化
- ✅ 移除了所有 `background` 自定义样式（~90 行）
- ✅ 移除了所有 `contentItem` 自定义样式（~60 行）
- ✅ 移除了重复的字体和颜色设置（~35 行）

### 2. 样式统一
- ✅ 所有输入组件使用 `034.png` 作为背景
- ✅ 统一的字体大小（21px）
- ✅ 统一的高度（60px）
- ✅ 统一的焦点边框效果

### 3. 功能保留
- ✅ 保留所有 `id` 属性
- ✅ 保留所有事件处理器（`onCurrentTextChanged`、`onClicked` 等）
- ✅ 保留自定义属性（`decimals`、`realValue`）
- ✅ 保留自定义函数（`textFromValue`、`valueFromText`）

### 4. 可维护性提升
- ✅ 样式集中管理在自定义组件中
- ✅ 修改样式只需修改一处
- ✅ 代码更简洁易读

## 验证要点

### 1. 功能验证
- [ ] 保护名称输入框正常输入
- [ ] 模块类型下拉框正常选择
- [ ] 寄存器地址 SpinBox 正常调整
- [ ] 通道编号 SpinBox 正常调整
- [ ] 保护延时 SpinBox 显示小数（0.1 秒精度）
- [ ] 播放次数 SpinBox 正常调整
- [ ] 播放时长 SpinBox 显示小数（0.1 秒精度）
- [ ] TTS 文字输入框正常输入
- [ ] 音频文件路径输入框只读

### 2. 样式验证
- [ ] 所有输入组件使用 034.png 背景
- [ ] 焦点时显示蓝色边框
- [ ] 字体大小统一为 21px
- [ ] 组件高度统一为 60px

### 3. 交互验证
- [ ] 模块类型切换时自动设置寄存器地址
- [ ] SpinBox 上下按钮正常工作
- [ ] SpinBox 可编辑输入
- [ ] ComboBox 下拉列表正常显示

## 相关文件

### 修改的文件
1. `src/qml/components/device_info/pages/SwitchInputPage.qml` - 主要修改
2. `src/qml/components/device_info/qmldir` - 注册自定义组件

### 依赖的自定义组件
1. `src/qml/components/device_info/CustomTextField.qml` - 自定义输入框
2. `src/qml/components/device_info/CustomSpinBox.qml` - 自定义数字调节器
3. `src/qml/components/device_info/CustomComboBox.qml` - 自定义下拉框

### 背景图片
- `src/qml/components/device_info/images/034.png` - 输入组件背景

## 技术细节

### CustomTextField 特性
- 高度：60px（1.5 倍原生）
- 字体：21px（1.5 倍原生）
- 背景：034.png 拉伸填充
- 焦点边框：2px 蓝色（#2196F3）
- 支持 `placeholderText`、`readOnly` 等原生属性

### CustomSpinBox 特性
- 高度：60px（1.5 倍原生）
- 字体：21px（1.5 倍原生）
- 背景：034.png 拉伸填充
- 焦点边框：2px 蓝色（#2196F3）
- 上下按钮：右侧垂直排列
- 支持 `textFromValue`、`valueFromText` 自定义函数

### CustomComboBox 特性
- 高度：60px（1.5 倍原生）
- 字体：21px（1.5 倍原生）
- 背景：034.png 拉伸填充
- 焦点边框：2px 蓝色（#2196F3）
- 下拉箭头：右侧居中
- 下拉列表：深色主题（#2d3548）

## 后续工作

### 1. 其他页面组件替换
- [ ] `AnalogInputPage.qml` - 模拟量输入页面
- [ ] `MotorConfigPanel.qml` - 电机配置面板
- [ ] `BrakeConfigPanel.qml` - 制动器配置面板
- [ ] `TensionControlConfigPanel.qml` - 张力控制配置面板

### 2. 样式微调（如需要）
- [ ] 调整焦点边框颜色
- [ ] 调整字体大小
- [ ] 调整组件高度

### 3. 功能增强（可选）
- [ ] 添加输入验证提示
- [ ] 添加错误状态样式
- [ ] 添加禁用状态样式

## 总结

✅ **成功替换了 SwitchInputPage.qml 中的所有原生输入组件**：
- 3 个 TextField → CustomTextField
- 5 个 SpinBox → CustomSpinBox
- 1 个 ComboBox → CustomComboBox

✅ **代码简化**：减少约 185 行重复样式代码

✅ **样式统一**：所有输入组件使用相同的背景图片和样式

✅ **功能完整**：保留所有原有功能和事件处理器

✅ **可维护性提升**：样式集中管理，修改更方便
