# FIX 100.299 - Phase 1 完成 - TTS后端支持

**日期**: 2026-01-23
**版本**: VERSION 2026-01-23-09:30
**状态**: ✅ Phase 1 完成，待编译测试

---

## 📋 实施概述

根据用户需求，实施语音管理功能的第一阶段：TTS模型动态配置后端支持。

**用户需求**:
1. 先实施TTS模型动态配置UI，找到合适的语音
2. 创建语音管理界面（作为主界面新页面）
3. 集成STT功能，识别现有音频文件内容
4. 实现批量生成功能（集成到界面，不使用单独脚本）

---

## ✅ Phase 1 完成内容

### 1. 创建 TTSConfigManager 类

**文件**:
- [src/control/TTSConfigManager.h](../../src/control/TTSConfigManager.h)
- [src/control/TTSConfigManager.cpp](../../src/control/TTSConfigManager.cpp)

**功能**:
- 单例模式配置管理器
- 支持三种场景独立配置：
  - StartupWarning (起车预警)
  - FaultAlarm (故障报警)
  - Test (测试/参数设置界面)
- 配置持久化（QSettings）
- 模型信息管理（7个模型）

**核心方法**:
```cpp
// 场景配置
int modelIndex(Scene scene) const;
void setModelIndex(Scene scene, int index);
int speakerId(Scene scene) const;
void setSpeakerId(Scene scene, int id);
double rate(Scene scene) const;
void setRate(Scene scene, double rate);
double volume(Scene scene) const;
void setVolume(Scene scene, double volume);

// 模型信息
QString modelName(int index) const;
int maxSpeakerId(int modelIndex) const;
QStringList modelNames() const;
```

**模型信息表**:
| 索引 | 模型名称 | 说话人数量 | 路径 |
|------|---------|-----------|------|
| 0 | vits-zh-aishell3 | 174 (0-173) | /app/tts_models/vits-zh-aishell3 |
| 1 | vits-zh-hf-fanchen-wnj | 1 (0) | /app/tts_models/vits-zh-hf-fanchen-wnj |
| 2 | vits-zh-hf-fanchen-C | 187 (0-186) | /app/tts_models/vits-zh-hf-fanchen-C |
| 3 | vits-zh-hf-theresa | 804 (0-803) | /app/tts_models/vits-zh-hf-theresa |
| 4 | vits-zh-hf-eula | 804 (0-803) | /app/tts_models/vits-zh-hf-eula |
| 5 | sherpa-onnx-vits-zh-ll | 5 (0-4) | /app/tts_models/sherpa-onnx-vits-zh-ll |
| 6 | vits-melo-tts-zh_en | 1 (0) | /app/tts_models/vits-melo-tts-zh_en |

---

### 2. 修改 SherpaOnnxTTS 类

**文件**:
- [src/control/SherpaOnnxTTS.h](../../src/control/SherpaOnnxTTS.h:118-154)
- [src/control/SherpaOnnxTTS.cpp](../../src/control/SherpaOnnxTTS.cpp:19-21,328-329,621-669)

**新增成员变量**:
```cpp
int m_speakerId;        // 说话人ID
int m_maxSpeakerId;     // 当前模型最大说话人ID
QString m_sceneName;    // 场景名称
```

**新增公共方法**:
```cpp
// 设置说话人ID
void setSpeakerId(int speakerId);
int speakerId() const { return m_speakerId; }

// 动态切换模型
bool switchModel(const QString &modelDir);

// 获取当前模型信息
QString currentModel() const { return m_modelDir; }
int maxSpeakerId() const { return m_maxSpeakerId; }

// 设置场景名称
void setSceneName(const QString &sceneName);
QString sceneName() const { return m_sceneName; }

// 测试TTS（用于UI测试按钮）
void testTTS(const QString &text);
```

**新增信号**:
```cpp
// 模型切换完成信号
void modelSwitched(bool success, const QString &modelName);

// 模型加载进度信号
void modelLoadingProgress(const QString &message);
```

**关键修改**:
1. **构造函数初始化** (Line 19-21):
   ```cpp
   , m_speakerId(0)           // 默认说话人0
   , m_maxSpeakerId(173)      // 默认aishell3的最大值
   , m_sceneName("unknown")   // 默认场景名
   ```

2. **synthesize() 使用可配置说话人ID** (Line 328-329):
   ```cpp
   // ✅ 2026-01-23 09:30 [FIX 100.299] 使用可配置的说话人ID（原来硬编码为0）
   synthCmd["speaker_id"] = m_speakerId;
   ```

3. **switchModel() 实现** (Line 628-655):
   - 停止当前TTS服务
   - 重新初始化新模型
   - 发送进度信号和完成信号

---

### 3. 更新 CMakeLists.txt

**文件**: [src/control/CMakeLists.txt](../../src/control/CMakeLists.txt:16,33)

**修改**:
```cmake
set(CONTROL_SOURCES
    # ... 现有文件 ...
    TTSConfigManager.cpp
)

set(CONTROL_HEADERS
    # ... 现有文件 ...
    TTSConfigManager.h
)
```

---

## 📊 实施效果

### 配置管理
- ✅ 三种场景独立配置（起车预警、故障报警、测试）
- ✅ 配置持久化（重启后保留）
- ✅ 模型信息集中管理

### TTS引擎增强
- ✅ 支持动态切换模型（无需重启应用）
- ✅ 支持动态切换说话人ID
- ✅ 场景名称标识（便于日志追踪）
- ✅ 测试TTS功能（UI测试按钮）

### 信号机制
- ✅ 模型切换进度反馈
- ✅ 模型切换成功/失败通知

---

## 🔧 技术细节

### 单例模式
```cpp
TTSConfigManager* TTSConfigManager::instance()
{
    static TTSConfigManager *inst = new TTSConfigManager();
    return inst;
}
```

### 场景配置结构
```cpp
struct SceneConfig {
    int modelIndex;
    QString modelPath;
    int speakerId;
    double rate;
    double volume;
};

QMap<Scene, SceneConfig> m_configs;
```

### 配置持久化
```cpp
// 保存配置
m_settings->setValue("startup_warning/modelIndex", config.modelIndex);
m_settings->setValue("startup_warning/speakerId", config.speakerId);
m_settings->setValue("startup_warning/rate", config.rate);
m_settings->setValue("startup_warning/volume", config.volume);
m_settings->sync();
```

### 模型切换流程
```cpp
bool SherpaOnnxTTS::switchModel(const QString &modelDir)
{
    emit modelLoadingProgress("正在停止当前TTS服务...");

    // 停止当前TTS服务
    if (m_ttsProcess->state() != QProcess::NotRunning) {
        stop();
        QThread::msleep(500);
    }

    emit modelLoadingProgress("正在加载新模型...");

    // 重新初始化
    bool success = initialize(modelDir);

    if (success) {
        emit modelLoadingProgress("模型加载成功");
        emit modelSwitched(true, modelDir);
    } else {
        emit modelLoadingProgress("模型加载失败");
        emit modelSwitched(false, modelDir);
    }

    return success;
}
```

---

## 📝 下一步操作

### Phase 2: 创建 AudioManagementController 类（进行中）

**功能**:
- 管理音频文件扫描
- 协调TTS和STT功能
- 批量生成控制
- 进度管理

**文件**:
- `src/control/AudioManagementController.h`
- `src/control/AudioManagementController.cpp`

### Phase 3: 创建语音管理界面 QML

**功能**:
- TTS模型配置UI
- 音频文件管理UI
- 批量操作UI
- 进度显示

**文件**:
- `src/qml/pages/VoiceManagement.qml`
- `src/qml/components/voice_management/TTSConfigSection.qml`
- `src/qml/components/voice_management/AudioFileSection.qml`

### Phase 4: 集成 STT 功能

**功能**:
- 下载Sherpa-ONNX ASR模型
- 创建SherpaOnnxASR类
- 实现音频文件识别

### Phase 5: 实现批量生成功能

**功能**:
- 扫描AUDIO目录
- 批量识别音频内容
- 批量生成TTS语音

---

## ✅ 实施检查清单

- [x] 创建 TTSConfigManager.h
- [x] 创建 TTSConfigManager.cpp
- [x] 修改 SherpaOnnxTTS.h 添加新方法
- [x] 修改 SherpaOnnxTTS.cpp 实现新方法
- [x] 修改 SherpaOnnxTTS.cpp 使用可配置说话人ID
- [x] 更新 CMakeLists.txt
- [ ] 编译测试验证功能
- [ ] 创建 AudioManagementController 类
- [ ] 创建语音管理界面 QML
- [ ] 集成 STT 功能
- [ ] 实现批量生成功能

---

## 🚀 编译和测试

### 编译命令
```powershell
# 用户手动执行
.\build-ubuntu24-apt.ps1 192.168.10.188
```

### 测试验证
1. **配置持久化测试**:
   - 修改TTS配置
   - 重启应用
   - 确认配置保留

2. **模型切换测试**:
   - 切换到不同模型
   - 查看日志确认切换成功
   - 测试语音合成

3. **说话人ID测试**:
   - 设置不同说话人ID
   - 测试语音合成
   - 确认声音变化

---

## 📚 相关文档

- [TTS动态配置实施计划](02-TTS动态配置实施计划-FIX100.299.md)
- [TTS模型动态配置UI方案](../技术决策/TTS模型动态配置UI方案.md)
- [TTS延时优化方案](03-TTS延时优化方案-FIX100.300.md)
- [TTS智能缓存机制实施总结](05-FIX100.300.1完成-TTS智能缓存机制实施总结.md)

---

## 🎉 总结

**FIX 100.299 Phase 1 已完成**，实现了TTS模型动态配置的后端支持：

✅ **核心改进**:
1. TTSConfigManager 类提供场景化配置管理
2. SherpaOnnxTTS 支持动态模型切换和说话人ID配置
3. 配置持久化机制（QSettings）
4. 模型切换进度反馈机制

✅ **预期效果**:
- 用户可以在UI界面动态切换TTS模型
- 用户可以测试不同说话人的声音
- 配置在重启后保留
- 为语音管理界面提供完整后端支持

✅ **实施优势**:
- 场景化配置（起车预警、故障报警、测试独立配置）
- 无需重启应用即可切换模型
- 完整的信号反馈机制
- 为后续UI实现奠定基础

**等待用户手动编译测试验证效果，然后继续 Phase 2。**
