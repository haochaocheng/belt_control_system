# 添加焦点调试日志

**日期**: 2026-01-30
**问题编号**: FIX 100.300.104.5
**状态**: ✅ 已完成

---

## 🎯 目的

添加详细的调试日志来追踪焦点流向，帮助定位对话框关闭后主界面导航键不工作的根本原因。

---

## 📝 修改内容

### 修改 1：Screen01.qml - 焦点状态追踪

**文件**：`src/qml/Input1/Input1Content/Screen01.qml`

#### 1.1 增强焦点变化监控

```qml
// ✅ 2026-01-30 [调试]: 详细追踪焦点变化
onActiveFocusChanged: {
    console.log("🔍 [Screen01] ========== 焦点状态变化 ==========")
    console.log("🔍 [Screen01] activeFocus:", activeFocus ? "✅ 获得焦点" : "❌ 失去焦点")
    console.log("🔍 [Screen01] focus 属性:", focus)
    console.log("🔍 [Screen01] parent:", parent)
    console.log("🔍 [Screen01] parent.objectName:", parent ? parent.objectName : "无")
    console.log("🔍 [Screen01] =====================================")
}

onFocusChanged: {
    console.log("🔍 [Screen01] focus 属性变化:", focus)
}
```

#### 1.2 对话框打开时的焦点追踪

```qml
function openDeviceSettings() {
    console.log("🔍 [Screen01] ========== 打开对话框 ==========")
    console.log("🔍 [Screen01] 当前选中索引:", selectedIndex)
    console.log("🔍 [Screen01] 打开前 - activeFocus:", activeFocus)
    console.log("🔍 [Screen01] 打开前 - focus:", focus)

    // 创建对话框
    var component = Qt.createComponent("../../components/device_info/DeviceSettingsDialog.qml")
    if (component.status === Component.Ready) {
        var dialog = component.createObject(root, {})
        if (dialog) {
            console.log("🔍 [Screen01] 对话框创建成功")
            console.log("🔍 [Screen01] dialog.parent === root:", dialog.parent === root)
            console.log("🔍 [Screen01] dialog.parent:", dialog.parent)

            dialog.visible = true
            dialog.z = 1000

            console.log("🔍 [Screen01] 对话框已显示")
            console.log("🔍 [Screen01] 打开后 - activeFocus:", activeFocus)
            console.log("🔍 [Screen01] 打开后 - focus:", focus)
            console.log("🔍 [Screen01] =====================================")
        }
    }
}
```

### 修改 2：DeviceSettingsDialog.qml - 关闭时的焦点追踪

**文件**：`src/qml/components/device_info/DeviceSettingsDialog.qml`

#### 2.1 ESC 键关闭

```qml
Keys.onEscapePressed: {
    console.log("🔍 [DeviceSettingsDialog] ========== ESC 键关闭对话框 ==========")
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.visible:", root.visible)
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.parent:", root.parent)
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.parent.objectName:", root.parent ? root.parent.objectName : "无")
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.parent.activeFocus:", root.parent ? root.parent.activeFocus : "无")

    root.visible = false

    console.log("🔍 [DeviceSettingsDialog] 关闭后 - root.visible:", root.visible)

    if (root.parent) {
        console.log("🔍 [DeviceSettingsDialog] 准备恢复焦点到父容器...")
        console.log("🔍 [DeviceSettingsDialog] root.parent:", root.parent)
        console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
        console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)

        root.parent.forceActiveFocus()

        console.log("🔍 [DeviceSettingsDialog] forceActiveFocus() 调用完成")
        console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
        console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)
    } else {
        console.error("🔍 [DeviceSettingsDialog] ❌ root.parent 为 null，无法恢复焦点！")
    }
    console.log("🔍 [DeviceSettingsDialog] =====================================")
}
```

#### 2.2 关闭按钮点击

```qml
onClicked: {
    console.log("🔍 [DeviceSettingsDialog] ========== 关闭按钮被点击 ==========")
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.visible:", root.visible)
    console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.parent:", root.parent)

    root.visible = false

    console.log("🔍 [DeviceSettingsDialog] 关闭后 - root.visible:", root.visible)

    if (root.parent) {
        console.log("🔍 [DeviceSettingsDialog] 准备恢复焦点到父容器...")
        console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
        console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)

        root.parent.forceActiveFocus()

        console.log("🔍 [DeviceSettingsDialog] forceActiveFocus() 调用完成")
        console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
        console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)
    }
    console.log("🔍 [DeviceSettingsDialog] =====================================")
}
```

#### 2.3 导航触发关闭

```qml
function triggerTopButton(buttonIndex) {
    switch(buttonIndex) {
    case 0:  // 关闭
        console.log("🔍 [DeviceSettingsDialog] ========== 导航触发关闭 ==========")
        console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.visible:", root.visible)
        console.log("🔍 [DeviceSettingsDialog] 关闭前 - root.parent:", root.parent)

        root.visible = false

        console.log("🔍 [DeviceSettingsDialog] 关闭后 - root.visible:", root.visible)

        if (root.parent) {
            console.log("🔍 [DeviceSettingsDialog] 准备恢复焦点到父容器...")
            console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
            console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)

            root.parent.forceActiveFocus()

            console.log("🔍 [DeviceSettingsDialog] forceActiveFocus() 调用完成")
            console.log("🔍 [DeviceSettingsDialog] root.parent.focus:", root.parent.focus)
            console.log("🔍 [DeviceSettingsDialog] root.parent.activeFocus:", root.parent.activeFocus)
        }
        console.log("🔍 [DeviceSettingsDialog] =====================================")
        break
    // ...
    }
}
```

---

## 🔍 调试信息说明

### 日志标识

所有调试日志使用 `🔍` 标识，便于在日志中快速定位。

### 关键信息

1. **焦点状态**：
   - `focus` - 组件的 focus 属性
   - `activeFocus` - 组件是否真正拥有焦点

2. **父容器信息**：
   - `parent` - 父容器对象
   - `parent.objectName` - 父容器名称
   - `parent.activeFocus` - 父容器焦点状态

3. **操作时机**：
   - 打开前/打开后
   - 关闭前/关闭后
   - forceActiveFocus() 调用前/调用后

---

## 📊 预期日志输出

### 正常流程（如果焦点恢复正确）

```
🔍 [Screen01] ========== 打开对话框 ==========
🔍 [Screen01] 打开前 - activeFocus: true
🔍 [Screen01] 对话框创建成功
🔍 [Screen01] dialog.parent === root: true
🔍 [Screen01] 打开后 - activeFocus: false
🔍 [Screen01] =====================================

🔍 [Screen01] ========== 焦点状态变化 ==========
🔍 [Screen01] activeFocus: ❌ 失去焦点
🔍 [Screen01] =====================================

[用户按 ESC 键]

🔍 [DeviceSettingsDialog] ========== ESC 键关闭对话框 ==========
🔍 [DeviceSettingsDialog] 关闭前 - root.parent.activeFocus: false
🔍 [DeviceSettingsDialog] 准备恢复焦点到父容器...
🔍 [DeviceSettingsDialog] root.parent.focus: true
🔍 [DeviceSettingsDialog] root.parent.activeFocus: false
🔍 [DeviceSettingsDialog] forceActiveFocus() 调用完成
🔍 [DeviceSettingsDialog] root.parent.focus: true
🔍 [DeviceSettingsDialog] root.parent.activeFocus: true  ← 应该是 true
🔍 [DeviceSettingsDialog] =====================================

🔍 [Screen01] ========== 焦点状态变化 ==========
🔍 [Screen01] activeFocus: ✅ 获得焦点  ← 应该获得焦点
🔍 [Screen01] =====================================
```

### 异常流程（如果焦点恢复失败）

```
🔍 [DeviceSettingsDialog] forceActiveFocus() 调用完成
🔍 [DeviceSettingsDialog] root.parent.activeFocus: false  ← 仍然是 false！
🔍 [DeviceSettingsDialog] =====================================

[Screen01 没有收到焦点变化事件]
```

---

## 🎯 下一步

1. **请求用户测试**：
   - 打开设备设置对话框
   - 按 ESC 键关闭
   - 尝试使用导航键
   - 提供完整的日志输出

2. **分析日志**：
   - 检查 `dialog.parent === root` 是否为 true
   - 检查 `forceActiveFocus()` 调用后 `activeFocus` 是否变为 true
   - 检查 Screen01 是否收到焦点变化事件

3. **根据日志定位问题**：
   - 如果 `dialog.parent !== root`，说明父容器设置错误
   - 如果 `forceActiveFocus()` 后 `activeFocus` 仍为 false，说明焦点恢复失败
   - 如果 Screen01 没有收到焦点变化事件，说明焦点没有真正返回

---

## 📝 相关文档

1. [FIX100.300.104-修复虚拟键盘关闭后焦点问题.md](06-修复虚拟键盘关闭后焦点问题.md)
2. [FIX100.300.104.2-修复ESC键使用Shortcut方案.md](07-修复ESC键使用Shortcut方案.md)
3. [FIX100.300.104.3-修复对话框关闭后焦点恢复.md](08-修复对话框关闭后焦点恢复.md)
4. [FIX100.300.104.4-修复对话框父容器设置.md](09-修复对话框父容器设置.md)

---

**完成日期**: 2026-01-30
**状态**: ✅ 已完成
**下一步**: 请求用户测试并提供日志输出
