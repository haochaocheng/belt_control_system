# FIX 100.300.106.5 - 8个Tab文件改为两列布局

**日期**: 2026-01-30
**状态**: ✅ 完成
**影响文件**: 8个Tab文件

---

## 修改概述

将以下8个Tab文件从单列布局改为两列布局,参考 `CurrentProtectionTab.qml` 的模式:

1. ✅ `FrontBearingTempTab.qml` - 前轴承温度保护
2. ✅ `RearBearingTempTab.qml` - 后轴承温度保护
3. ✅ `PhaseAWindingTab.qml` - A相绕组保护
4. ✅ `PhaseBWindingTab.qml` - B相绕组保护
5. ✅ `PhaseCWindingTab.qml` - C相绕组保护
6. ✅ `MotorTempTab.qml` - 电机温度保护
7. ✅ `XAxisVibrationTab.qml` - X轴振动保护
8. ✅ `YAxisVibrationTab.qml` - Y轴振动保护

---

## 修改内容

### 1. 添加导入语句
```qml
import QtQuick.Layouts 1.15
```

### 2. 更新注释
```qml
// ✅ 2026-01-30 [FIX 100.300.106.5]: 改为两列布局,参考 BasicConfigTab
```

### 3. 修改布局模式
```qml
readonly property string layoutMode: "two-column"  // 从 "single-column" 改为 "two-column"
```

### 4. 替换布局结构

**旧结构** (单列):
```qml
ScrollView {
    Column {
        // 9个参数字段垂直排列
    }
}
```

**新结构** (两列):
```qml
ScrollView {
    ColumnLayout {
        RowLayout {
            // 左列 (ColumnLayout)
            ColumnLayout {
                // 索引 0, 2, 4, 6, 8 (5个字段)
            }

            // 右列 (ColumnLayout)
            ColumnLayout {
                // 索引 1, 3, 5, 7 (4个字段)
            }
        }
    }
}
```

### 5. 参数字段分配

**左列** (5个字段):
- 索引 0: 是否投入 (RadioButton组)
- 索引 2: 动作保护类型 (只读)
- 索引 4: 温度量程 (只读)
- 索引 6: 温度下限 (只读,带单位)
- 索引 8: 输入点选择 (只读)

**右列** (4个字段):
- 索引 1: 报警类型 (RadioButton组)
- 索引 3: 故障保护类型 (只读)
- 索引 5: 温度上限 (只读,带单位)
- 索引 7: 过滤干扰延时 (只读)

### 6. 每个参数字段使用 RowLayout 包装

```qml
RowLayout {
    Layout.fillWidth: true
    Layout.preferredHeight: 40
    spacing: 10

    Text {
        text: "参数名称:"
        font.pixelSize: 14
        color: "#9E9E9E"
        Layout.preferredWidth: 120
        horizontalAlignment: Text.AlignRight
    }

    // 焦点指示器容器
    Item {
        Layout.fillWidth: true
        Layout.maximumWidth: 300
        Layout.preferredHeight: 40

        // 焦点指示器
        Rectangle {
            anchors.fill: parent
            color: "transparent"
            border.color: (root.focusParamIndex === X) ? "#2196F3" : "transparent"
            border.width: (root.focusParamIndex === X) ? 3 : 0
            radius: 4
            z: 10
        }

        // 参数控件 (RadioButton组 或 CustomReadOnlyField)
    }
}
```

---

## 实施步骤

### Step 1: 手动修改第一个文件
- 修改 `FrontBearingTempTab.qml` 作为模板

### Step 2: 创建批量转换脚本
- 创建 `scripts/2026-01-30/01-convert-tabs-to-two-column.ps1`
- 使用 `FrontBearingTempTab.qml` 作为模板
- 批量生成剩余6个文件

### Step 3: 修复注释
- 创建 `scripts/2026-01-30/02-fix-tab-comments.ps1`
- 修复每个文件的标题注释

### Step 4: 验证
- 验证所有文件的导入语句
- 验证布局模式设置
- 验证注释正确性

---

## 验证结果

### 导入语句验证
```bash
✅ 所有8个文件都包含: import QtQuick.Layouts 1.15
```

### 布局模式验证
```bash
✅ 所有8个文件的 layoutMode 都设置为 "two-column"
```

### 注释验证
```bash
✅ FrontBearingTempTab.qml: [前轴承温度保护] 前轴承温度保护配置
✅ RearBearingTempTab.qml: [后轴承温度保护] 后轴承温度保护配置
✅ PhaseAWindingTab.qml: [A相绕组保护] A相绕组保护配置
✅ PhaseBWindingTab.qml: [B相绕组保护] B相绕组保护配置
✅ PhaseCWindingTab.qml: [C相绕组保护] C相绕组保护配置
✅ MotorTempTab.qml: [电机温度保护] 电机温度保护配置
✅ XAxisVibrationTab.qml: [X轴振动保护] X轴振动保护配置
✅ YAxisVibrationTab.qml: [Y轴振动保护] Y轴振动保护配置
```

---

## 文件路径

所有修改的文件位于:
```
e:\2025\3_gongkongji\belt_control_system\src\qml\components\device_info\pages\
```

辅助脚本位于:
```
e:\2025\3_gongkongji\belt_control_system\scripts\2026-01-30\
  - 01-convert-tabs-to-two-column.ps1  # 批量转换脚本
  - 02-fix-tab-comments.ps1            # 修复注释脚本
```

---

## 布局效果

### 单列布局 (旧)
```
┌─────────────────────────────┐
│ 是否投入                    │
│ 报警类型                    │
│ 动作保护类型                │
│ 故障保护类型                │
│ 温度量程                    │
│ 温度上限                    │
│ 温度下限                    │
│ 过滤干扰延时                │
│ 输入点选择                  │
└─────────────────────────────┘
```

### 两列布局 (新)
```
┌──────────────────┬──────────────────┐
│ 是否投入         │ 报警类型         │
│ 动作保护类型     │ 故障保护类型     │
│ 温度量程         │ 温度上限         │
│ 温度下限         │ 过滤干扰延时     │
│ 输入点选择       │                  │
└──────────────────┴──────────────────┘
```

---

## 优势

1. **空间利用率提升**: 两列布局更好地利用横向空间
2. **视觉平衡**: 左右对称,视觉效果更好
3. **一致性**: 与 `CurrentProtectionTab.qml` 保持一致
4. **导航兼容**: 保持所有焦点指示器和导航函数不变

---

## 注意事项

1. **焦点索引不变**: 参数字段的索引顺序保持不变 (0-8)
2. **导航函数不变**: `getParamFieldCount()` 和 `triggerParamInput()` 保持不变
3. **焦点指示器不变**: 所有焦点指示器逻辑保持不变
4. **只读字段**: 所有9个参数字段都是只读的,使用 `CustomReadOnlyField` 或 RadioButton组

---

## 总结

✅ **成功完成**: 8个Tab文件已全部转换为两列布局
✅ **验证通过**: 所有文件的导入、布局模式和注释都正确
✅ **保持兼容**: 导航系统和焦点指示器完全兼容
✅ **代码质量**: 使用批量脚本确保一致性和准确性

---

**修改完成时间**: 2026-01-30 14:15
