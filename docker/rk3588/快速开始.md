# 🚀 快速开始 - 5分钟部署指南

## ✅ 工控机已完成安装

✅ **192.168.10.170 已安装好 Docker 环境！**
- Docker版本: 20.10.24
- docker-compose版本: 1.29.2
- 用户权限: 已配置

---

## 📦 员工操作（超简单）

### 方式一：一键部署（最简单）

**只需要一个命令！**

1. 打开 PowerShell 或 CMD
2. 进入目录:
   ```cmd
   cd e:\2025\3_gongkongji\belt_control_system\docker\rk3588
   ```

3. 运行一键部署:
   ```cmd
   deploy-all.bat
   ```

4. 等待完成（约10-15分钟）

**就这么简单！** 脚本会自动:
- ✅ 构建Docker镜像
- ✅ 保存镜像文件
- ✅ 传输到工控机
- ✅ 自动部署
- ✅ 启动服务

---

### 方式二：查看状态

部署完成后,查看运行状态:

**方法 1：双击运行**
```
双击: view-status.bat
```

**方法 2：命令行**
```cmd
ssh pi@192.168.10.170 "cd /opt/belt_control && docker-compose ps"
```

---

### 方式三：查看日志

**方法 1：双击运行**
```
双击: view-logs.bat
```

**方法 2：命令行**
```cmd
ssh pi@192.168.10.170 "cd /opt/belt_control && docker-compose logs -f"
```
按 Ctrl+C 退出

---

## 🔄 日常维护（简单命令）

### 重启服务
```cmd
ssh pi@192.168.10.170 "cd /opt/belt_control && docker-compose restart"
```

### 停止服务
```cmd
ssh pi@192.168.10.170 "cd /opt/belt_control && docker-compose down"
```

### 启动服务
```cmd
ssh pi@192.168.10.170 "cd /opt/belt_control && docker-compose up -d"
```

---

## 📂 相关文件

| 文件 | 用途 |
|------|------|
| `deploy-all.bat` | **一键部署脚本（最常用）** |
| `view-status.bat` | 查看运行状态 |
| `view-logs.bat` | 查看实时日志 |
| `员工操作手册.md` | 详细操作文档 |

---

## ⚠️ 注意事项

1. **首次部署需要时间** - 约10-15分钟,请耐心等待
2. **网络要求** - 确保电脑和工控机在同一网络
3. **密码** - SSH密码是 `pi`
4. **不要中断** - 部署过程中不要关闭窗口

---

## ✅ 检查清单

部署前确认:
- [ ] 工控机已开机
- [ ] 能ping通 192.168.10.170
- [ ] Docker已安装（已完成✅）

部署后验证:
- [ ] `view-status.bat` 显示 Up
- [ ] `view-logs.bat` 无严重错误
- [ ] 工控机屏幕有界面显示

---

## 📞 遇到问题？

1. **查看日志**: `view-logs.bat`
2. **重启服务**: `docker-compose restart`
3. **联系技术支持**: 提供日志信息

---

## 🎓 培训重点

员工只需要学会：
1. 运行 `deploy-all.bat` （部署）
2. 运行 `view-status.bat` （查看状态）
3. 运行 `view-logs.bat` （查看日志）

**3个批处理文件,双击运行即可！**
